# SR Lumapas Attendance App — Technical-Debt Cleanup Roadmap

**Status:** Active working reference  
**Live site:** https://srlumapas.netlify.app/  
**Live version:** v0.7  
**Cleanup baseline:** v1.0  
**Last updated:** 24 September 2026

## Purpose

This file is the canonical source-of-truth roadmap for the Attendance App cleanup. Its canonical repository location is `nrnd-pixel/SR-Lumapas-Attendance/ATTENDANCE_TECH_DEBT_CLEANUP_ROADMAP.md` on GitHub `main`. At the start of every Attendance cleanup session, fetch this file from GitHub and check whether an active cleanup PR contains a newer copy. Update the roadmap in the same working branch/PR whenever the baseline, risks, phase status, acceptance criteria, or release plan materially change. A separate ChatGPT Project Source upload is not required once this file is merged.

## Core rules

- Treat the Attendance App as a working production system.
- Keep v0.7 live and unchanged while v1.0 is cleaned up.
- Do not rewrite from scratch.
- Preserve behaviour unless a change is explicit.
- Work in small, reversible checkpoints.
- Impact-map before coding.
- Add real browser regression coverage before structural refactors.
- Prefer Browser → controlled RPC → tables as the application boundary.
- Do not combine major refactoring, security redesign, UI redesign, and new feature work in one checkpoint.
- Do not promote/merge without explicit approval after verification.

## Current baseline evidence

- 15 active classes.
- 319 active pupils.
- 3A historical baseline: 137 registers / 3,425 attendance records.
- v1.0 frontend package contains `index.html`, `netlify.toml`, and `README.txt`.
- v1.0 `index.html`: about 1,178 lines / 84.6 KB.
- Original v1.0 frontend is a single embedded ES-module; cleanup `main` now loads the same application code from `assets/js/main.js` after Phase 3A.
- 171 unique DOM IDs; no duplicate IDs found in the audited v1.0 source.
- 18 unique Attendance RPCs are called by v1.0 and all 18 currently exist in the live backend.
- No direct browser table access was found in v1.0; the frontend uses RPCs.
- No Attendance-specific global wrapper chain was found.
- No repository-side SQL definitions, Playwright suite, or verifier suite are included in the original v1.0 ZIP.
- GitHub Actions integrity CI is now merged on `main` as `.github/workflows/phase0a-integrity.yml`; the Phase 0B backend-contract workflow is also merged as `.github/workflows/phase0b-backend-contract.yml`.
- Phase 0A's original frozen `index.html` is anchored at merge commit `c578b4240a2bd9899db602fa818bda99bd6ff3cd` with SHA-256 `199d93f81c1cdb1742544f0dbde3f9da2e264b9d2dc16e4ce93cd53140387086`. From Phase 2 onward, the integrity gate must verify that immutable historical baseline while separately syntax-checking the current working frontend; controlled runtime changes must not rewrite the historical baseline.
- The prior Maths App protocol concepts such as `validateStudentAccess`, `finishPractice`, `cloud.rpc`, `MutationObserver`, `shuffle`, `config.js`, and `v40-release.js` are **not applicable** to Attendance unless they later appear in the repository.

## Confirmed production bug to fix in cleanup

### Password-recovery routing race — CONFIRMED

Observed behaviour:

Forgot Password → reset email → reset link → recovery form briefly appears → app routes directly to Admin/Teacher screen.

Cause:

`PASSWORD_RECOVERY` shows the recovery form, but startup also calls `getSession()`. The recovery link creates a valid session, so normal session routing calls `enterApp()` and overwrites the recovery screen.

Required cleaned behaviour:

Forgot Password → reset email → reset link → stay on Set New Password → enter/confirm new password → update password → sign out recovery session → return to normal login → sign in using new password.

Acceptance coverage must prove:
- normal login still works;
- normal refresh while signed in still works;
- reset link stays on recovery screen;
- mismatched passwords are rejected;
- successful reset signs out the recovery session;
- old password fails;
- new password works;
- teacher/admin permissions and class assignments are preserved.

Phase 2A implements the routing/session correction and is merged on the cleanup `main` via PR #8. Browser coverage proves recovery-screen priority, confirmation mismatch rejection, password update, recovery-session sign-out, return to normal login, old-password rejection, new-password acceptance, preservation of teacher class scope, and no Attendance RPC entry until normal post-reset sign-in. Credential-transition checks use the synthetic Auth harness rather than a real production teacher credential; hosted Auth configuration was separately re-verified in Phase 2D. This cleanup work is not itself production promotion.

## Roadmap

### Phase 0A — Freeze canonical frontend baseline
**Status:** Complete.
**Goal:** Establish the exact v1.0 source before any functional change.

Actions:
- Put exact v1.0 source into a canonical Git repository.
- Commit it unchanged.
- Record exact `main` SHA.
- Preserve v1.0 ZIP/hash and Netlify config.
- Record DOM/API baseline and known-good data/report fixtures.

**Exit:** v1.0 frontend is reproducible from version control with zero functional changes.

### Phase 0B — Reconstruct backend source
**Status:** Complete.
**Goal:** Make the Attendance Supabase backend reproducible.

Actions:
- Capture Attendance schema, tables, functions, RPCs, grants, RLS, Auth redirect settings, and migrations under version control.
- Verify every active frontend RPC has a matching repository SQL definition.
- Record current production data-count fixtures separately from migrations.

**Exit:** backend structure can be recreated without reverse-engineering the live project.

### Phase 1 — Regression safety net
**Status:** Complete.
**Goal:** Protect current behaviour before refactoring.

Required real browser scenarios:
- teacher sign in/sign out;
- forgot-password recovery;
- teacher signup → email verification → Pending Approval;
- admin approve/reject and Attendance-only enable/disable;
- teacher sees assigned class only; admin sees all classes;
- load/save daily register;
- Mark All Present and exception editing;
- no-change save creates no false correction;
- correction requires reason and audits only changed pupils;
- Transfer In / Transfer Out / Move Class;
- monthly statistics;
- admin school dashboard;
- Term/YTD reports;
- CSV export.

**Exit:** core workflows pass repeatably in automated browser tests.

### Phase 2 — Correctness fixes
**Status:** Complete and merged on cleanup `main`. Phase 2A password recovery, Phase 2B pending-signup account binding, Phase 2C stale async-response protection, and Phase 2D hosted Auth URL/redirect verification are complete.
**Goal:** Fix known defects before structural cleanup.

Actions:
- Fix confirmed password-recovery routing race.
- Prevent stale async responses from overwriting newer class/date/report selections.
- Replace/harden browser-wide pending teacher-signup `localStorage` so pending state is bound to the correct account/session.
- Re-verify Supabase Auth Site URL and redirect allow-list for `https://srlumapas.netlify.app/`.

Phase 2 checkpoint split:
- **2A — Password recovery correctness:** recovery takes startup priority; confirmation is validated; password is updated; recovery session is signed out; normal sign-in is required afterward. **Complete and merged.**
- **2B — Pending signup account binding:** bind browser-persisted pending signup state to the intended account/session and prevent cross-account reuse on shared devices. **Complete and merged via PR #10.** The pending record is versioned and bound to the Supabase Auth user ID, is written only after successful signup, is read/cleared only for the matching signed-in user, and legacy/unbound state fails closed rather than being attributed to another account. A different account can neither auto-submit, pre-fill from, nor delete another user's pending signup state.
- **2C — Stale async-response protection:** protect attendance, monthly statistics, dashboard, report-option, and Term/YTD rendering from older responses overwriting newer selections. **Complete and merged via PR #12.** The frontend uses per-surface monotonically increasing request serials plus captured selection keys so only the newest matching response may update UI/state. Report class/type/term/as-of changes also invalidate any in-flight rendered report/export state. No RPC, SQL, grant, RLS, DOM-ID, Science, or Netlify configuration change was required.
- **2D — Auth URL/redirect re-verification:** **Complete and merged via PR #14.** User-provided live Supabase Dashboard evidence on 13 September 2026 confirms Site URL `https://srlumapas.netlify.app/` and exactly one Redirect URL `https://srlumapas.netlify.app/**`. These values match the Phase 0B repository record and the current frontend contract, which sends `window.location.origin + '/'` for both signup verification and password recovery. No hosted Auth configuration mutation was required. Current Supabase guidance prefers exact production redirect paths where practical, but narrowing the existing working wildcard is a separate hardening decision rather than a Phase 2 correctness fix. Netlify preview-host redirects remain a later pilot/staging concern.

**Phase 2B acceptance coverage:**
- successful email-confirmation signup stores a versioned pending record bound to the created Auth user ID;
- the same user returning after verification can still auto-submit the exact name/class/role and then clear that user's pending record;
- a failed signup creates no pending record;
- a different unauthorized account on the same browser cannot auto-submit or pre-fill another user's pending data;
- a different account manually submitting its own gate request does not delete another user's pending record;
- an already-authorized different account does not delete another user's pending record;
- legacy/unbound pending state is discarded rather than attributed to the signed-in user;
- no Supabase RPC signature, grant, RLS, table, Science, or Netlify configuration change is required for Phase 2B.

**Phase 2C acceptance coverage:**
- an older attendance class/date response cannot replace a newer roster/banner/selection;
- an A → B → A attendance sequence proves request serials reject the first A response even when its selection key becomes current again;
- older monthly-statistics and admin-dashboard responses cannot replace newer class/month selections;
- older report-option responses cannot repopulate terms for a class the user has already left;
- changing report class/type/term/as-of invalidates rendered report/export state, and an older Term/YTD response cannot replace a newer report;
- the synthetic Supabase browser harness can deterministically hold/release named RPC responses rather than relying on timing sleeps;
- existing recovery, signup isolation, attendance/correction, admin, missing-register, CSV, February 3A, and Term 1 3A assertions remain mandatory;
- no Supabase mutation or backend contract change is required.

**Phase 2D acceptance coverage:**
- fresh hosted Supabase Dashboard evidence confirms Site URL `https://srlumapas.netlify.app/`;
- fresh hosted Supabase Dashboard evidence confirms the sole Redirect URL `https://srlumapas.netlify.app/**`;
- signup verification sends `emailRedirectTo` to the current browser origin root;
- Forgot Password sends `redirectTo` to the current browser origin root;
- the browser redirect contract is asserted dynamically against `window.location.origin + '/'`, so it is not coupled to the Playwright test host;
- no Supabase/Auth setting, RPC, SQL, grant, RLS, Science, Netlify, DOM, production-data, or deployment mutation is required.

**Exit:** no known recovery, stale-response, cross-account pending-signup, or hosted Auth redirect-configuration correctness issue remains in the cleaned build.

### Phase 3 — Frontend modularisation
**Status:** Complete and merged on cleanup `main` through Phase 3B7F3.
**Goal:** Remove the single-file frontend bottleneck without changing behaviour.

Phase 3 checkpoint split:
- **3A — Extract main application module:** move the existing inline ES module verbatim to `assets/js/main.js` and replace it with one same-origin `<script type="module" src="./assets/js/main.js"></script>` loader. Preserve all function bodies/order, DOM IDs/selectors, Supabase calls, state semantics, recovery startup ordering, request-serial stale-response guards, attendance/correction behaviour, reporting behaviour, and admin behaviour. The Phase 0A integrity gate must prove byte-for-byte equivalence against exact pre-Phase-3 `main` `88fffe819a4b287bd05c48c0933ebf14c7d913d2` and syntax-check the external module. No SQL/RPC/grant/RLS/Auth/Science/Netlify configuration change is permitted. **Complete and merged via PR #16.**
- **3B1 — Supabase client boundary:** move only the pinned Supabase JS import and client-construction settings into `assets/js/supabase-client.js`, exporting `createAttendanceClient()`. Keep recovery-link detection and the timing of client creation in `main.js`; preserve the exact CDN version, public project URL/key, Auth options, DOM/API contracts, and all feature behaviour. Evolve Phase 0A so the historical Phase 3A extraction remains provable while current 3B1 is verified as an exact deterministic transformation. **Complete and merged via PR #18.**
- **3B2 — Shared state/request-serial boundary:** move only the existing mutable application `state`, the five-key `requestSerial`, and the existing `beginRequest()` / `isLatestRequest()` helpers into side-effect-free `assets/js/app-state.js`. Preserve every field/key, object identity/mutability, direct `requestSerial.periodReport` invalidation, password-recovery state ownership, DOM/API contracts, and all feature behaviour. Phase 0A must preserve the historical Phase 3B1 proof and verify 3B2 as an exact deterministic transformation. **Complete and merged via PR #20.**
- **3B3A — Shared date-helper boundary:** move only `bruneiToday()`, `displayDate()`, `formatMonthLabel()`, and `shortDay()` into side-effect-free `assets/js/date-helpers.js`, preserving the exact `Asia/Brunei` today semantics and UTC display formatting. Keep DOM helpers (`$`, `esc()`, `fillGroupedClasses()`), auth/recovery, feature logic, RPCs, selectors, and browser event wiring in `main.js`. Evolve Phase 0A so historical Phase 3B2 remains provable while current 3B3A is verified as an exact deterministic transformation. **Complete and merged via PR #22.**
- **3B3B — Shared DOM/UI-helper boundary:** move only `$`, `esc()`, and `fillGroupedClasses()` into side-effect-free `assets/js/ui-helpers.js`. Preserve exact DOM lookup, HTML escaping, class grouping/sorting, Prasekolah/year labels, compact/full option labels, blank-option handling, exclusion semantics, stable DOM IDs/selectors, and all feature behaviour. Add real-browser regressions for hostile-markup escaping and grouping/sorting/compact/exclusion behaviour. Evolve Phase 0A so historical Phase 3B3A remains provable while current Phase 3B3B is verified as an exact deterministic transformation. **Complete and merged via PR #24.**
- **3B4 — Auth session/password-recovery boundary:** move recovery-link detection, recovery-active state, shared entry-view hiding/login display, normal login/session startup, Forgot Password, password update, recovery-session sign-out, normal app sign-out, and `PASSWORD_RECOVERY` / `SIGNED_OUT` auth events into `assets/js/auth-session.js`. Reuse the existing single Supabase client and pass `enterApp` in as orchestration; preserve recovery priority over `getSession()` routing, the exact `window.location.origin + '/'` reset redirect, stable DOM IDs/selectors, and all existing visible messages/behaviour. Keep teacher signup, pending-signup `localStorage`, teacher gate/approval, `attendance_teacher_status`, and bootstrap ownership in `main.js`. Add browser regressions for recovery-session sign-out failure and external `SIGNED_OUT` routing. Evolve Phase 0A so historical Phase 3B3B remains provable while current Phase 3B4 is verified as an exact deterministic transformation. **Complete and merged via PR #26.**
- **3B5 — Attendance/corrections boundary:** move only the existing daily-register/correction ownership into `assets/js/attendance-register.js`: attendance-code/group conversion, original-value preservation and change detection, unsaved-change protection, Attendance busy/banner helpers, register load/render/edit controls, Mark All Present/Clear, save-readiness/correction UI, and `attendance_save_register` orchestration. Reuse the one existing Supabase client by passing it to `initAttendanceRegister()`; keep shared `state` and the `register` request serial in `app-state.js`, keep DOM/event wiring and application orchestration in `main.js`, and make no DOM-ID, RPC/SQL/grant/RLS/Auth/Science/Netlify change. Preserve untouched `PP`/`TM` legacy status codes, enrolment-date eligibility, class authorization, complete-roster checks, required absence notes, non-school-day blocking, genuine-change detection, correction reasons, audit semantics, and stale-response protection. Add browser regressions for untouched `PP`/`TM` preservation during a different-pupil correction, `OTHER` requiring a note, and non-school-day save blocking. Evolve Phase 0A so Phase 3B4 becomes historical evidence and Phase 3B5 is an exact deterministic extraction from signed base `dc274130989e32d354ff4f28d8cc3c302010b7e5`. **Complete and merged via PR #28.**
- **3B6 — Student-management boundary:** move only current student roster, filtering/rendering, movement-history rendering, Transfer In dialog/defaults, Transfer Out dialog, Move Class dialog, and the three existing movement-submit workflows into `assets/js/student-management.js`. Reuse the existing Supabase client by passing it to `initStudentManagement()` together with the existing shared `schoolId()`, `adminClasses()`, and `showAdminMsg()` helpers; keep those helpers, shared state object, panel/tab orchestration, generic `[data-close]` wiring, and event-listener registration in `main.js`. Preserve exact RPC payloads/messages/DOM IDs and backend-owned movement/history semantics. Add browser regressions for escaped hostile pupil text plus search/class filtering/action-button integrity, and for Transfer In Brunei-date/class/reporting-group statistics defaults. Evolve Phase 0A so signed Phase 3B5 runtime merge `8a3b8173a1e576a8eee39df721bcfcc7a98ce971` is historical evidence and current Phase 3B6 is an exact deterministic extraction from signed docs-closure base `b4fbee48cc91fb9e60f4163c0fd59d1363f5f9db`. **Complete and merged via PR #31.**
- **3B7A — Teacher-admin boundary:** move only in-app administrator teacher-management ownership into `assets/js/teacher-admin.js`: loading pending signup requests and approved teachers, rendering teacher/request cards and counts, approve/reject orchestration, and Attendance-only enable/disable orchestration. Reuse the existing Supabase client and inject the existing `adminClasses()` and `showAdminMsg()` helpers; keep those helpers, shared `state`, panel/tab orchestration, event-listener registration, teacher signup/gate logic, versioned pending-signup `localStorage`, `enterApp()`, and recovery/auth startup in their current owners. Preserve the exact `attendance_admin_teacher_requests`, `attendance_admin_teachers`, `attendance_admin_review_teacher_request`, and `attendance_admin_set_teacher_active` RPC payloads/messages and backend-owned authorization/audit semantics. Add a real-browser regression proving hostile teacher/request text is escaped as literal text while generated approval controls remain functional. Evolve Phase 0A so the signed Phase 3B6 runtime merge `a915ebf897a29285594bd274725f00c3dfc1bea9` is historical evidence and current Phase 3B7A is an exact deterministic extraction from signed docs-closure base `fbe65f87b5a6a106ce1953a9d22a26a6793c7635`. **Complete and merged via PR #33.**
- **3B7B — Teacher signup/gate/access boundary:** move only teacher signup options/request submission, versioned account-bound pending-signup `localStorage` handling, signup/gate rendering, and the `attendance_teacher_status` authorization check into `assets/js/teacher-access.js`. Reuse the existing Supabase client; keep `auth-session.js` byte-for-byte unchanged, keep the authorized `attendance_bootstrap` application startup and event-listener registration in `main.js`, and preserve exact RPC/Auth/DOM behaviour. Add a real-browser regression proving a rejected teacher request renders the admin note literally, pre-fills the correction form, resubmits the exact corrected class/role payload, and returns to Pending Approval. Evolve Phase 0A so signed Phase 3B7A runtime merge `f9fb4f88a98f5e9f03a1d33b92ebe69ea19691ca` becomes historical evidence and current Phase 3B7B is an exact deterministic extraction from signed docs-closure base `3259ad542b9b1816ab98aafa2a502b833ee69a65`. **Complete and merged via PR #35 as signed runtime commit `326e54ea0c529f538e68433f2f30b4588ec2fe71`; approved normalized head `69af4e3d8975f5a728223abd78bc3d5baca897fb` passed exact Phase 0A/0B and 45/45 Playwright before merge.**
- **3B7C — Monthly statistics boundary:** move only `setStatsBanner()`, `loadMonthlyStats()`, and `renderMonthlyStats()` into `assets/js/statistics.js`, reusing the existing Supabase client and shared `state.monthlyStats` / `requestSerial.monthlyStats`. Keep statistics panel orchestration and event-listener registration in `main.js`; preserve exact `attendance_monthly_class_stats` payloads, stale-response/selection-key protection, DOM IDs/selectors, missing-register semantics, and all reporting formulas. Add a real-browser regression proving incomplete gender data warns that male/female figures are partial while total attendance remains complete. Evolve Phase 0A so signed Phase 3B7B runtime merge `326e54ea0c529f538e68433f2f30b4588ec2fe71` becomes historical evidence and current 3B7C is an exact deterministic extraction from signed docs-closure base `1912512eb344b17fa0f219268becfdac31bba00d`. **Complete and merged via PR #37 as signed runtime commit `bf7379332a35f1ea9f859ea2ade6b6e67cdf3c73`; approved normalized head `0ee26371a67860c98c45d8c48b79b9a8ec3f2776` and final tree `91337aa06661e772876ebf2a419935d00bce098e` passed exact Phase 0A `34913511240`, Phase 0B `34913511220`, and Playwright `34913511216` with 46/46 Chromium tests in 18.1s before merge.**
- **3B7D — Admin dashboard boundary:** extract only the existing Dashboard banner/load/render ownership into `assets/js/admin-dashboard.js`. Reuse the existing Supabase client and shared `state.adminDashboard` / `requestSerial.adminDashboard`; inject existing `schoolId()` plus a narrow `openStatisticsForClass(classId, month)` callback so cross-panel Dashboard → Statistics routing remains owned by `main.js`. Keep Dashboard panel orchestration and event-listener registration in `main.js`; preserve exact `attendance_admin_school_dashboard` payloads, admin authorization, stale-response/selection-key protection, missing-register semantics, DOM IDs/selectors, and reporting formulas. Add a real-browser regression proving a Dashboard View action opens the selected class and dashboard month in Statistics and sends the exact monthly-statistics payload. Evolve Phase 0A so signed Phase 3B7C runtime merge `bf7379332a35f1ea9f859ea2ade6b6e67cdf3c73` becomes historical evidence and current Phase 3B7D is an exact deterministic extraction from signed docs-closure base `18ddf13a3292772646bb8885b2470f3e4a081f64`. **Complete and merged via PR #39 as signed runtime commit `52d4dd245e06cd0a3e42a5c7da15eeff121a9055`; approved normalized head `c7c161a44d3729b124494f137a9bc4e863bc46c8` and final tree `975fac2c13f0958aebafb4b304b33f7cd59aad05` passed exact Phase 0A `34920286845`, Phase 0B `34920286857`, and Playwright `34920286878` with 47/47 Chromium tests in 21.1s before merge.**
- **3B7E — Term/YTD reports and CSV boundary:** move only the existing report-selection invalidation, report-options loading, Term/YTD load/render ownership, CSV escaping, and browser-download ownership into `assets/js/period-reports.js`. Reuse the existing Supabase client and shared `state.reportOptions` / `state.periodReport` plus `requestSerial.reportOptions` / `requestSerial.periodReport`; keep Reports panel orchestration and event-listener registration in `main.js`; preserve exact `attendance_class_report_options` and `attendance_class_period_report` payloads, stale-response/selection-key protection, DOM IDs/selectors, missing-register semantics, official 3A Term 1 fixture, and CSV contents/download naming. Browser download APIs (`Blob`, `URL.createObjectURL`, temporary anchor click/removal, delayed `URL.revokeObjectURL`) move unchanged. Add a real-browser regression proving that changing a loaded report period immediately clears the old report and disables CSV export until reload. Evolve Phase 0A so signed Phase 3B7D runtime merge `52d4dd245e06cd0a3e42a5c7da15eeff121a9055` becomes historical evidence and current Phase 3B7E is an exact deterministic extraction from signed/docs-closure base `c59dd2965b6ab2c6aef0e6340df8ebac6e73ef78`. **Complete and merged via PR #41 as signed runtime commit `e9a4338c0723becc5dcd3f30fd207ad28efdd47b`; approved normalized head `f45cc8b585a2bbcae09699f1cd8b4c2f019e00b4` and final tree `6805cf738e93c22f047a5ea224590c0453c712b7` passed exact Phase 0A `34930538204`, Phase 0B `34930538202`, and Playwright `34930538178` with 48/48 Chromium tests in 20.2s before merge.**
- **3B7F+ — Remaining ownership split:** after statistics/dashboard/reporting ownership is separately verified and merged, impact-map the remaining orchestration/event wiring in small reversible checkpoints. Do not combine dependency migration, CSP hardening, backend security changes, UI redesign, stale-roster/teacher-list/access-check correctness work, or Phase 4 reporting consolidation with these structural moves.
- **3B7F1 — Application bootstrap/scope boundary:** move only `enterApp()`, `schoolId()`, `adminClasses()`, and `showAdminMsg()` from `assets/js/main.js` into new `assets/js/app-bootstrap.js`. Reuse the one existing Supabase client and inject only the existing `switchPanel()` callback. Keep `switchPanel()`, `openStatisticsForClass()`, all DOM event-listener registration, and the `beforeunload` unsaved-change guard in `main.js`. Preserve exact `attendance_bootstrap` behavior/returned-field consumption, teacher/admin scope, stable DOM IDs/selectors/messages, Brunei date defaults, saved-class preference, initial register loading, recovery/auth ordering, and all backend-owned authorization. Add real-browser regressions proving the saved class is preferred across Attendance/Statistics/Reports selectors and an authorized account with zero assigned classes warns without calling `attendance_load_register`. Evolve Phase 0A so signed docs-closure base `09f86472da499811af416dee8176c58ca5e5e06c` remains historical evidence and the current split is verified as an exact deterministic transformation. **Complete and merged via PR #43 as signed runtime commit `3c72aca9e043b734640b1d728d1910cf02632af3`; approved normalized head `0a83eb4218f2dea14711d8293227b8bccbb0579a` and final tree `4c842562129abcc773e83cd31c80658121640f25` passed exact Phase 0A `34965425313`, Phase 0B `34965425341`, and Playwright `34965425320` with 50/50 Chromium tests in 19.6s on Node `v22.23.2`, npm `10.9.8`, with 0 vulnerabilities before merge.**
- **3B7F2 — Application navigation boundary:** move only `switchPanel(name)` and `openStatisticsForClass(classId, month)` from `assets/js/main.js` into new `assets/js/app-navigation.js`. Keep every top-level DOM event listener, generic `[data-close]` dialog wiring, and the `beforeunload` unsaved-change guard in `main.js`. Preserve the six panel/tab IDs, one-visible-panel/one-active-tab behavior, Dashboard/Statistics/Reports/Students/Teachers lazy loaders, Statistics/Reports class fallback, Dashboard → Statistics class/month routing, existing downstream RPC payloads, Auth/recovery startup, and backend authorization. Add one browser navigation-contract regression covering those behaviors and proving return to Attendance causes no extra RPC. Evolve Phase 0A so Phase 3B7F1 becomes immutable historical evidence and the current split is verified as an exact deterministic transformation from signed/docs-closure base `cf939c3582d4db2c2be7a503b4355cfd9066af55`. **Complete and merged via PR #45 as signed runtime commit `03207a339cf5b4c0836bcdc5fccd95f451adbfd4`; approved normalized head `cb131514a7b90b81d52882455281a25de6b3609f` and final tree `63297bbfca93db74bc4f4236e513aff322a2e7cc` passed exact Phase 0A `34987342629`, Phase 0B `34987342602`, and Playwright `34987342606` with 51/51 Chromium tests in 21.2s on Node `v22.23.2`, npm `10.9.8`, with 0 vulnerabilities before merge.**
- **3B7F3 — Composition-root lifecycle closure:** keep `assets/js/main.js` as the small application composition root rather than extracting listener glue solely to reduce file size. Preserve module initialization, static DOM event registration, generic `[data-close]` dialog wiring, the cross-application unsaved-attendance `beforeunload` guard, and Auth startup in `main.js`. Add focused real-browser coverage proving a clean register does not cancel `beforeunload`, an unsaved Attendance edit does, and all three student-movement dialogs close through the generic `[data-close]` contract. **Complete and merged via PR #47 as signed commit `07505b7de962ecc46bc06ce4c51cfa47d94dcdd0`; approved head `a76229c3f720b6e6351a1b73f1cdef83da703fbf` and final tree `7497d486a82a58db82e604668065f62ebe6f3db9` passed exact Phase 0A `35053697452`, Phase 0B `35053697405`, and Playwright `35053697404` with 53/53 Chromium tests in 18.8s on Node `v22.23.2`, npm `10.9.8`, with 0 vulnerabilities before merge. No runtime JavaScript changed.**

Target ownership:
- config/environment;
- Supabase API client;
- auth/password recovery;
- shared state;
- shared UI helpers;
- attendance/corrections;
- student management;
- teacher access;
- statistics;
- admin dashboard;
- Term/YTD reports/export.

Actions:
- split ownership into maintained modules;
- preserve stable DOM contracts;
- manage Supabase JS dependency reproducibly;
- reduce inline JS/CSS enough to support a tighter CSP later.

**Exit:** equivalent browser behaviour with clear module ownership.

### Phase 4 — Reporting consolidation
**Status:** Complete and merged on cleanup `main`. Phase 4A reporting fixtures, Phase 4B1 shared reporting source, Phase 4B1V isolated execution validation, Phase 4B1P production/repository reconciliation, and Phase 4B2 frontend reporting-population selectors are complete.
**Goal:** Prevent formula drift across reports while supporting department-dependent reporting populations.

Reporting populations:
- **Whole class (including SEN)** — all active, date-eligible enrolments. This is the current/default production reporting behaviour and preserves the established official fixtures.
- **Non-SEN pupils only** — active, date-eligible enrolments where `include_in_class_stats = true`. The flag is treated as the explicit inclusion boundary for this reporting population; reporting must not infer SEN status from free-text group labels.
- The selected population must apply consistently to numerator, denominator, roster count, gender totals, daily/monthly aggregates, Dashboard totals, Term/YTD reports, and CSV exports.
- Missing-register semantics remain identical in both populations: a missing register is never zero attendance and contributes neither attendance nor possible pupil-days.

Phase 4 checkpoint split:
- **4A — Reporting equivalence safety net:** freeze production-derived Whole-Class and Non-SEN aggregate fixtures before changing reporting SQL. Add a read-only aggregate verifier with no pupil/teacher identifiers; align synthetic browser fixtures with current live term metadata/gender totals; keep runtime RPCs, DOM, grants/RLS/Auth/Science/Netlify/production data unchanged. **Complete and merged via PR #49 as signed `main` commit `c205249763ca5a1d81affe32ff90ec8ed8d865f2`.**
- **4B1 — Shared reporting calculation model/backend API:** introduce one internal day-level calculation model, preserve all three existing reporting RPC signatures as Whole-Class compatibility wrappers, and add uniquely named population-aware `*_v2` RPCs for cleanup v1.0. The internal helper must be `STABLE SECURITY INVOKER` with direct client execution revoked; public RPCs retain the existing authenticated/service-role `SECURITY DEFINER` authorization boundaries. Dashboard latest-day values must derive from the same shared daily facts rather than a separate count path. **Repository implementation is complete and merged via PR #50 as signed `main` commit `ecea2fc99a79c4af07d3b70bca3dcb240c57aacc`.**
- **4B1V — Isolated v13 execution validation:** execute the reconstructed baseline plus v13 in an isolated local Supabase stack with synthetic data only, and verify migration history, catalog contracts, protected population fixtures, shared-engine equivalence, and legacy/v2 public API compatibility before production application. **Complete and merged via PR #53 as signed `main` commit `40521ab9cb4383d6454f288c54b890c5edc1528d`; approved exact head `f49ef79f9411f0b04167ff66c702d1a9c8748463` passed the local DB gate plus Phase 0A/0B and 53/53 Playwright tests.**
- **4B1P — Production v13 application and repository reconciliation:** after explicit user approval, the exact reviewed v13 SQL was applied to production and Supabase recorded it as `20260917090547 attendance_v13_shared_reporting_model`. Post-application checks confirmed the expected seven reporting functions/grants, exact legacy Whole-Class compatibility, all four protected Whole-Class/Non-SEN 3A fixtures, and all 15 Dashboard classes. Repository metadata was then reconciled to that live ledger version without changing SQL bytes or reapplying production state. **Complete and merged via PR #55 as signed `main` commit `61e7f9db8c3250147f589f700bd6d47e7131568a`; approved exact head `52d1a4afaba830eb04ef40de99a0a2ba7c6791fb`, tree `dc0479a4429e1f39c891753fa3486156608d19fc`, Phase 0A `35206715686`, Phase 0B `35206715458`, Playwright `35206715593` with 53/53 tests in 21.4s, and Phase 4B1V Local DB Validation `35206715525` all passed before merge.**
- **4B2 — Frontend reporting-population selector:** add explicit Whole-Class/Non-SEN selectors to Monthly Statistics, Admin Dashboard, Term/YTD Reports, Dashboard → Statistics routing, and CSV export. Whole Class remains the initial/default behavior through the legacy compatibility wrappers; Non-SEN uses the production v13 `*_v2` APIs. Population is part of reporting stale-response keys; Dashboard → Statistics carries population; report-population changes invalidate loaded report/CSV state; Whole-Class CSV naming remains backward-compatible while Non-SEN exports are explicitly suffixed. **Complete and merged via PR #56 as signed `main` commit `209f5d2945988f014120ce8a81e327ab3a732140`; approved exact head `4877ee7d08be7a1b7c2266eeb76224e74c97099a`, tree `5c26a8706c81120bf298c02f2bb26ad8ec51b4c0`, Phase 0A `35244060983`, Phase 0B `35244060475`, and Playwright `35244060724` with 60/60 Chromium tests in 25.2s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities all passed before merge.**

Actions:
- centralise school-day, eligible-pupil-day, register-completion, and attendance-counting logic;
- make Monthly, Dashboard, Term, and YTD reporting reuse shared calculation logic;
- preserve missing-register safeguards;
- preserve transfer-aware denominators;
- prove both reporting populations against frozen fixtures before and after SQL consolidation.

**Exit:** one calculation model drives all reporting surfaces, and the requested reporting population is explicit and mathematically consistent across all outputs.

### Phase 5 — Security and access cleanup
**Status:** Phase 5A-5F are complete in repository and production, subject only to this documentation-closure checkpoint. The database hardening sequence remains live through v18 `20260919044948 attendance_v18_structural_write_boundary`; the hosted Auth minimum-password policy was separately changed from 6 to 8 on 20 September 2026 after explicit approval. Leaked-password protection remains an accepted Free-plan limitation rather than an unfinished Attendance change.
**Goal:** Make the trust boundary explicit and minimal.

Fresh Phase 5 impact-map findings:
- Phase 5C production reconciliation is merged via PR #70 as signed cleanup `main` `153f2d4830cba7750956ba020e3a99709d0f2722`; its approved exact head `26211e9fac2a6bf8917c8410f93f6bd557cd5fb0` preserved the exact v15 SQL blob and passed all five hard gates before merge. Phase 5D started only after a fresh teacher-management impact map and explicit repository/local approval;
- cleanup frontend uses Attendance RPCs only and has no direct `.from(...)` table calls; its Supabase client uses the publishable key, not a service-role/secret key;
- live Supabase has 21 public Attendance RPCs: 18 `SECURITY DEFINER` and 3 invoker-mode functions after Phase 5C; no Attendance function is executable by `PUBLIC`, and only `attendance_signup_options()` is executable by `anon`;
- all 16 Attendance tables have RLS enabled with 50 policies. After Phase 5C, `authenticated` is SELECT-only on `daily_registers`, `attendance_records`, `students`, `enrolments`, and `student_movements`; direct authenticated writes now remain on six residual Attendance structural/configuration tables (`academic_years`, `calendar_dates`, `classes`, `schools`, `settings`, `terms`) for later Phase 5F review;
- `attendance_save_register` is the controlled `SECURITY DEFINER` write boundary for register data. Phase 5C now makes `attendance_admin_transfer_in`, `attendance_admin_transfer_out`, and `attendance_admin_move_class` controlled `SECURITY DEFINER` movement boundaries and removes authenticated direct INSERT/UPDATE/DELETE on `students`, `enrolments`, and `student_movements` while retaining SELECT/RLS/service-role access;
- live class-assignment rows still store only generic `teacher`, while signup/review semantics distinguish `class_teacher` / `assistant_teacher`;
- private audit tables remain outside client schema/table privileges; `attendance_record_audit` intentionally has RLS disabled but no `anon`/`authenticated`/service-role schema usage or table privileges, while `teacher_access_audit` has RLS enabled with no client policies/grants;
- Supabase Security Advisor still reports leaked-password protection disabled. The current Supabase organization is on the Free plan, where leaked-password protection is not available. After explicit production approval, the user confirmed changing only the hosted Auth minimum password length from 6 to 8; password character requirements, secure-password-change/current-password settings, SMTP, CAPTCHA, Science student PIN auth, SQL, and Netlify were left unchanged. The connected Supabase tools do not expose hosted Auth-config reads, so the minimum=8 value is recorded as user-confirmed Dashboard evidence rather than independently MCP-read evidence;
- Security Advisor also flags the expected exposed `SECURITY DEFINER` RPCs. Source review found the privileged Attendance bodies either check `auth.uid()` plus class/admin authorization, delegate through checked v2 reporting functions, or are the intentionally anonymous signup-options RPC; these warnings are not by themselves proof of an authorization defect;
- current public-schema default privileges still auto-grant future public tables/functions/sequences to Data API roles. Because the Supabase project is shared with Science, do not change project-wide `public` defaults in an Attendance-only checkpoint; keep explicit per-object Attendance grants/revokes and verify them instead.

- Phase 5D discovery confirms `assets/js/teacher-admin.js` owns the in-app admin teacher workflow and calls only `attendance_admin_teacher_requests`, `attendance_admin_teachers`, `attendance_admin_review_teacher_request`, and `attendance_admin_set_teacher_active`; no frontend table DML exists and no DOM/frontend change is required for this checkpoint;
- live authenticated INSERT/UPDATE/DELETE remains on `teacher_school_memberships` and `teacher_class_assignments`, even though the two mutation RPCs already coordinate membership/assignment state and private `teacher_access_audit` semantics. Those two RPCs are the only public Attendance functions that write the teacher membership/assignment tables;
- both teacher mutation RPCs are already authenticated-only `SECURITY DEFINER` functions with fixed empty `search_path`, explicit `auth.uid()` plus school-admin authorization, and live PL/pgSQL bodies matching the repository baseline exactly. Therefore Phase 5D can remain grant-only rather than rewriting functions;
- authenticated SELECT must remain on both teacher tables because invoker-mode `attendance_bootstrap()` reads `teacher_school_memberships` directly, while teacher status/bootstrap access also depends on the existing RLS-scoped read model;
- live teacher baseline before v16 remains 2 active school memberships (1 admin, 1 teacher), 2 active class assignments, 1 approved signup request, and 2 teacher-access audit rows (`request_submitted`, `request_approved`). Both active class assignments store generic `teacher`;
- Phase 5D must preserve the generic `teacher` membership/assignment role even when an approved signup request records `class_teacher` or `assistant_teacher`; authoritative role persistence remains the separate Phase 5E checkpoint;
- current browser coverage already exercises admin approval, rejection, Attendance-only disable/enable, hostile teacher text escaping, teacher assigned-class scope, and admin all-class scope. The new Phase 5D backend verifier adds the missing proof that these coordinated RPC/audit paths still work after direct table DML is revoked.

Proposed checkpoint split:
- **5A — Security contract safety net:** repository/local-test only. Add a synthetic role-based security verifier and isolated local-Supabase gate covering RPC EXECUTE boundaries, anonymous/unauthorized/assigned-teacher/admin access, current direct-DML baseline exposure, controlled `attendance_save_register` and student-movement RPC dependencies, private-audit inaccessibility, `SECURITY DEFINER` search-path/auth markers, and the intentional anonymous signup-options exception. **Complete and merged via PR #59 as signed cleanup `main` commit `16425a337a000c9db4db8a63c7c2ca21902a7ead`; approved exact final head `68174078a86d0f2098e7d6f1dda9bea00d0d4b32`, tree `93860907833a5f277ecd39cda788193aa7378375`. Exact-final-head gates passed: Phase 0A `35315054747`, Phase 0B `35315054722`, Playwright `35315054721` with 60/60 Chromium tests in 26.4s, Phase 4B1V Local DB Validation `35315054737`, and Phase 5A Security Access Validation `35315054732`. No live migration/Auth setting change was made.**
- **5B — Attendance register write boundary:** **complete and merged via PR #62 as signed cleanup `main` `d74ee1d7d213e47d6b85a83dfe77ae0b96d7d2ea`; approved exact head `b99574daa86137573b1d633c84dada2e08599d79`, tree `452e8a0efaf4e022396181bb05c6e2fd937cc42d`.** Repository v14 `20260918094529_attendance_v14_register_write_boundary.sql` changes the existing `attendance_save_register(uuid,date,jsonb,text)` to `SECURITY DEFINER` without rewriting its body/signature/return shape and revokes authenticated INSERT/UPDATE/DELETE on only `daily_registers` / `attendance_records`, retaining authenticated SELECT, existing RLS policies, triggers, service-role privileges, frontend behavior, and Science isolation. Exact-head gates passed: Phase 0A `35326473184`, Phase 0B `35326473139`, Playwright `35326473157` with 60/60 Chromium tests in 24.2s, Phase 4B1V Local DB Validation `35326473156`, and Phase 5A Security Access Validation `35326473148`. **Production v14 is applied and verified as live migration `20260918094529 attendance_v14_register_write_boundary`; this source-only checkpoint reconciles the repository migration version to the same live ledger version without changing SQL bytes or reapplying production state.**
- **5C — Student movement write boundary:** preserve Transfer In/Out/Move Class signatures and history semantics while removing authenticated direct writes to `students`, `enrolments`, and `student_movements`.
- **5D — Teacher-management write boundary:** **complete in repository and production.** PR #71 merged approved exact head `37040973da36a88f83cfb27551547758c46c84d1` as signed cleanup `main` `44aa4e9ecb46273dc30df5a0aac0d94e9c303448`. After separate production approval, the exact reviewed grant-only SQL was applied once and recorded live as `20260919005727 attendance_v16_teacher_management_write_boundary`; repository reconciliation renames the original `20260919090000...` candidate to the live ledger version without changing SQL bytes. Authenticated INSERT/UPDATE/DELETE is revoked on teacher memberships/assignments, authenticated SELECT remains, and existing teacher-admin RPC bodies, RLS, policies, triggers, service-role privileges, and audit semantics are unchanged.
- **5E — Teacher assignment role semantics:** **complete in repository and production.** PR #75 merged the reviewed v17 candidate; after separate explicit approval, exact SQL blob `9f127e3615fae3e8a508b636435e6035d9fea560` was applied once and Supabase recorded live migration `20260919015041 attendance_v17_teacher_role_persistence`. PR #77 approved exact reconciliation head `4faff2a846b374d0ab5bc04582d3e6644ea6b393` and merged as signed `main` `27162895073795f3af5b1f9d7be53ee3a66a4d12`, aligning the repository migration filename and reconstruction references to the live ledger with zero SQL-byte changes. Production persists authoritative `class_teacher` / `assistant_teacher` assignment roles while preserving generic school membership and unchanged class-access semantics.
- **5F — Structural-admin and Auth hardening decisions:** **complete in repository and production, pending only this documentation closure.** PR #79 merged the reviewed grant-only v18 candidate; after separate explicit production approval, exact SQL blob `5182f1dff7a993151e81a331188aa692dfc86fb3` was applied once and Supabase recorded live migration `20260919044948 attendance_v18_structural_write_boundary`. PR #81 reconciled the repository source to that live version, PR #82 closed 5F1, PR #83/#84 recorded and closed the 5F2 impact map, PR #85/#86 aligned the browser to an 8-character policy, and PR #87/#88 corrected and repository-closed the pinned Supabase 2.114.0 successful weak-password session behavior. After a separate explicit production approval on 20 September 2026, the user confirmed changing only the hosted Auth minimum password length from 6 to 8 in the Supabase Dashboard. Post-change read-only verification shows the project still `ACTIVE_HEALTHY`, live ledger still ending at v18, 15 active classes, 319 active pupils / 319 active enrolments, 3A at 137 registers / 3,425 attendance records, 2 confirmed Auth users, 3 Auth sessions, 1 active admin + 1 active teacher Attendance membership, and one active Science-teacher/Auth/Attendance overlap. Security Advisor findings remain unchanged. The hosted minimum=8 value itself is user-confirmed Dashboard evidence because the connected Supabase tools do not expose Auth-config reads; no real teacher credential was used for live sign-in testing.

Actions:
- review every public Attendance RPC and `SECURITY DEFINER` function;
- keep privileged functions only where needed and verify internal authorization;
- clarify/reduce direct authenticated table-write privileges if RPC-only writes are intended;
- review direct authenticated school-admin writes to reporting-structural tables (`academic_years`, `terms`, `calendar_dates`, `classes`) because custom admin clients can alter reporting periods/calendar structure outside controlled app workflows;
- review private audit-table RLS and document intentional exceptions;
- persist `class_teacher` / `assistant_teacher` authoritatively instead of collapsing to generic `teacher`;
- review password policy and leaked-password protection;
- document intentionally anonymous signup-options access.

**Exit:** application access model is explicit and verified.

### Phase 6 — Performance and data-model cleanup
**Status:** Impact mapping, **Phase 6A, and Phase 6B are complete**. Phase 6B repository/local implementation, production v20 application, and repository/live migration-ledger reconciliation are all merged/verified. PR #99 reconciled v20 and merged as signed `main` `9abb0e406b911a4a4d09408b6c32f83d0b9837be`. Repository and production both use `20260920100523 attendance_v20_enrolment_lifecycle_contract` and preserve reviewed SQL blob `e11760161e7d2a36c1d6eff23d01a92168bf68c5`. **Phase 6C read-only impact/preflight is now complete; the production data operation remains blocked pending an official gender source plus an approved privacy-safe update method.**
**Goal:** Remove smaller backend debt before wider rollout without changing established Attendance behavior, historical eligibility, reporting semantics, Science objects, or production data unintentionally.

Fresh Phase 6 impact-map findings:
- Before production v19, Supabase Performance Advisor reported exactly **6 unindexed Attendance/Attendance-private foreign keys**: `teacher_signup_requests.approved_class_id`, `teacher_signup_requests.requested_class_id`, `teacher_signup_requests.reviewed_by`, `attendance_private.teacher_access_audit.actor_user_id`, `attendance_private.teacher_access_audit.class_id`, and `attendance_private.teacher_access_audit.request_id`. Production v19 now covers all six; post-application catalog verification returns zero uncovered Attendance/Attendance-private foreign keys and the `unindexed_foreign_keys` advisor finding is absent. The affected tables had only 1 signup request and 2 teacher-access audit rows at application time, so this remains preventive scale/referential-integrity hardening rather than a response to a demonstrated latency incident.
- Performance Advisor also reports unused indexes, including both Attendance and Science indexes. **Do not remove any index in Phase 6A merely because pilot traffic has not used it.** Science indexes are out of scope and Attendance index-removal is not part of this cleanup phase.
- Live enrolments are currently internally consistent at the current-roster level: 319/319 are `active=true`, `end_date is null`, and `enrolment_status='ENROLLED'`. There are currently 0 live student-movement rows.
- The lifecycle RPCs intentionally use richer historical semantics: Transfer In creates a current `TRANSFERRED IN` enrolment; Transfer Out sets `end_date` plus `TRANSFERRED OUT`; Move Class closes the old enrolment with `MOVED CLASS` and creates a new current `ENROLLED` enrolment. The closing operations intentionally do **not** set `active=false`.
- Historical reporting and roster functions already filter on both `e.active` and date eligibility. Therefore redefining ended enrolments as `active=false` would remove their historical contribution from reports and is **not** an acceptable cleanup. Phase 6B must treat `active` as a row-validity/availability flag, while `end_date` + `enrolment_status` carry lifecycle state, unless a separately reviewed reporting redesign proves otherwise.
- `enrolments` currently has only the date-order check `end_date is null or end_date >= start_date`; there is no database CHECK constraining `enrolment_status` values or tying status shape to `end_date`. Existing v15 movement tests already prove the intended statuses `ENROLLED`, `TRANSFERRED IN`, `TRANSFERRED OUT`, and `MOVED CLASS`.
- Gender completeness is the largest live data-quality gap: 319 active pupils total; 25 have gender recorded (`3A` only: 14 Male, 11 Female) and **294 pupils across the other 14 active classes are unset**. Reporting intentionally treats anything other than exact `Male`/`Female` as unknown and keeps total attendance mathematically complete while warning that gender splits are partial.
- Transfer In accepts `Male`, `Female`, `Other`, or blank. No live `Other` values are present. Completing gender data is therefore a data backfill, not a reporting-formula change.
- No official gender source is currently established in the repository/live database evidence reviewed for this checkpoint. Phase 6C remains blocked until an official source is provided and a privacy-safe import/update method is agreed. Real pupil identifiers or source datasets must never be committed to the public repository or migration history.

Proposed checkpoint split:
- **6A — FK index coverage:** **complete.** The reconciled repository source is `20260920060510_attendance_v19_fk_index_coverage.sql`, matching the live Supabase migration version while preserving exact reviewed SQL blob `998e10512a87f85c32b71b0a59d8fa0b2bdb23e4`. PR #93 final head `46716810fcf260186b48886cd212a78306c6d73f` passed Phase 0A `35493805437`, Phase 0B `35493805446`, Playwright `35493805433` (65/65 Chromium in 25.6s, 0 npm vulnerabilities), Phase 4B1V `35493805432`, and Phase 5A `35493805478`; both isolated local stacks applied reconciled version `20260920060510` and the FK verifier returned zero rows. PR #93 then merged as signed `main` `18d551698c40519a96e4511b5813bc3e618f1d03`. Production remains verified with all six exact indexes, zero uncovered Attendance FKs, no `unindexed_foreign_keys` advisor finding, unchanged security-advisor baseline, 15 active classes / 319 active pupils / 319 active enrolments, 3A 137 registers / 3,425 attendance records, February 398 / 0.9365 / 93.65%, and Term 1 1,051 / 0.9342 / 93.42%. No row, frontend/DOM, RPC/RLS/grant/function/constraint, Auth, Science, or Netlify change occurred.
- **6B — Enrolment lifecycle consistency:** **complete.** Repository/local implementation is merged/validated, production v20 is applied/verified, and repository/live migration-ledger reconciliation is merged. PR #96 approved exact head `6d46bcd1b00a8107841295cdd9535f7041cc2fe4` and merged as signed `main` `eb1bb3915acd423113ec92691ebcfbd6101b06d6`. Candidate migration `20260920082000_attendance_v20_enrolment_lifecycle_contract.sql` adds exactly one lifecycle CHECK to `attendance.enrolments`, first `NOT VALID` then fully `VALIDATE`d: current rows require `end_date is null` with `ENROLLED`/`TRANSFERRED IN`; closed rows require non-null `end_date` with `TRANSFERRED OUT`/`MOVED CLASS`. The migration deliberately does not constrain `active`, so Transfer Out and old Move Class rows remain `active=true` for historical reporting. Dedicated rollback-only verifier `attendance_enrolment_lifecycle_v20_contract.sql` rejects malformed shapes, invokes the real Transfer Out / Move Class RPCs, asserts closed rows remain active, and proves `attendance.reporting_class_period_facts` JSON is unchanged for affected historical periods before/after closure. Final PR #96 head passed Phase 0A `35499700093`, Phase 0B `35499699983`, Playwright `35499700031` (65/65 Chromium in 27.4s, 0 npm vulnerabilities), Phase 5A `35499700008`, and Phase 4B1V `35499699962`; both isolated local stacks applied candidate version `20260920082000`, the lifecycle verifier passed, and Phase 4B1V retained all reporting-population/equivalence/public-API gates. Fresh production preflight after merge verified project `ACTIVE_HEALTHY`, live ledger still ending at v19 `20260920060510`, lifecycle CHECK absent, 319 enrolments with 0 inactive / 0 ended / 0 unknown-status / 0 status-date-shape / 0 invalid-date-order rows, 0 movement rows, unchanged security-advisor baseline, 15 active classes / 319 active pupils / 319 active enrolments, 3A 137 registers / 3,425 attendance records, February 398 / 0.9365 / 93.65%, and Term 1 1,051 / 0.9342 / 93.42%. Merged v20 Git blob is `e11760161e7d2a36c1d6eff23d01a92168bf68c5`. Production application used the exact merged two-statement SQL; Supabase recorded `20260920100523 attendance_v20_enrolment_lifecycle_contract`. Post-application verification found `attendance_enrolments_lifecycle_status_check` present and validated, 319 enrolments with zero lifecycle/date-shape violations, movement rows still 0, unchanged grants/RLS/policies and movement RPC metadata, unchanged security-advisor baseline, unchanged 15/319/319 and 3A 137/3,425 counts, and unchanged February 398 / 0.9365 / 93.65% plus Term 1 1,051 / 0.9342 / 93.42%. The applied SQL touched only `attendance.enrolments`; it contained no Science, Auth, RPC, RLS, grant, trigger, frontend, or data mutation. Repository/live reconciliation branch renames the migration to `20260920100523_attendance_v20_enrolment_lifecycle_contract.sql`, updates Phase 0B plus both local-Supabase reconstruction/ledger checks to `20260920100523`, appends sanitized live v20 metadata to production history (Attendance migration count 39), and updates README/roadmap. The renamed migration retains exact Git blob `e11760161e7d2a36c1d6eff23d01a92168bf68c5`; no SQL body, verifier, RPC/function/grant/RLS/policy/trigger, frontend/DOM, Auth, Science, data, Netlify, or production change is included.
- **6C — Gender completeness:** **read-only impact/preflight complete; production update blocked.** Current live state is 319 active pupils, with gender recorded for 25 pupils in 3A only (14 Male / 11 Female) and 294 unknown across the other 14 classes; no live `Other` values are present. `attendance.students.gender` is nullable text. Authenticated clients retain SELECT-only table privilege, so there is no existing direct browser write path. The only current stored function that inserts/updates `attendance.students` is `public.attendance_admin_transfer_in`, which accepts `Male`, `Female`, `Other`, or blank and only updates an existing student's gender when that pupil returns through Transfer In. No current UI/RPC exists for editing gender on an already-enrolled pupil. Reporting reads the current `students.gender` dynamically through `attendance.reporting_class_period_facts`: exact `Male`/`Female` values feed the split; null and every other value (including `Other`) remain unknown. Therefore a backfill will retrospectively reclassify historical male/female attendance while total attendance, eligible-pupil denominators, averages, percentages, missing-register semantics, and attendance history must remain unchanged. Recommended implementation remains a one-time private transactional production update after exact dry-run reconciliation of all 319 current pupils, with the real per-pupil mapping kept out of GitHub and migration history. Primary source match key should be official `Student ID / student_ref`; class and full name may be used only as cross-checks, not as the primary match key. Preflight must reject duplicate IDs, missing/extra pupils, unsupported values, or disagreement with the existing 3A 14/11 baseline before any update. Post-update checks must prove only `students.gender` changed, unknown counts fall exactly according to the official source, 3A remains 14/11 unless the official source explicitly proves otherwise, and all protected attendance/reporting fixtures remain unchanged.

Repository/UI ownership for Phase 6:
- frontend ownership remains `assets/js/student-management.js` for Transfer In/Out/Move Class and existing reporting modules for gender display/warnings; no Phase 6A/6B frontend edit is planned;
- stable relevant DOM remains `#adminStudentList`, `#tiGender`, `#tiClass`, `#tiDate`, `#tiGroup`, `#tiStats`, `#toDate`, `#moveClassDialog`, and `#mcDate`;
- relevant backend contracts remain `attendance_admin_student_roster`, `attendance_admin_transfer_in`, `attendance_admin_transfer_out`, `attendance_admin_move_class`, and the existing v2 reporting RPCs; no signature or returned-field change is planned;
- authenticated table access for `students` / `enrolments` remains SELECT-only after v15; movement writes stay behind authenticated-only `SECURITY DEFINER` RPCs with fixed empty `search_path`;
- no browser API/global wrapper change is planned. Existing dialog APIs, request serials, Auth/session ownership, and Netlify behavior remain untouched.


Phase 6C impact/preflight details:
- **Exact signed baseline:** repository `main` `2d1aff6d6acc5580437f93f513e07dc1ef3fd42e`; Phase 6B is closed and no Phase 6C production mutation has occurred.
- **Live completeness:** 319 active pupils across 15 active classes; 3A is complete at 14 Male / 11 Female; the other 14 classes contain 294 pupils with unset/unknown gender; no live `Other` value is present.
- **Table/access contract:** `attendance.students.gender` is nullable `text`; authenticated table privilege is SELECT-only while service role retains full access. Existing students RLS policies remain present as defense-in-depth. `attendance_students_touch` is the only non-internal students trigger and only refreshes `updated_at` on update.
- **Writer ownership:** live function-source inspection finds exactly one `attendance.students` writer, `public.attendance_admin_transfer_in` (authenticated-only `SECURITY DEFINER`, fixed empty `search_path`). It validates `Male` / `Female` / `Other` / blank and may fill gender for a returning pupil, but it is not a general gender-maintenance API. No permanent gender-edit RPC or UI should be added solely for this one-time backfill unless separately approved.
- **Frontend/DOM ownership:** `assets/js/student-management.js` sends Transfer In gender from stable selector `#tiGender`. Reporting surfaces remain `#statsMale`, `#statsFemale`, `#genderStatsBanner`, `#reportMale`, `#reportFemale`, `#reportGenderBanner`, and `#dashboardGenderIncomplete`. No Phase 6C frontend change is currently planned.
- **Reporting semantics:** `attendance.reporting_class_period_facts` joins the current `students.gender` value at report time. Exact `Male` and `Female` values feed gender splits; null and any other value, including `Other`, feed unknown-gender counts. Updating gender therefore changes historical gender breakdowns retrospectively while total attendance mathematics must remain invariant.
- **Existing coverage:** Playwright already covers Transfer In gender payloads, the complete 3A 14/11 reporting case, incomplete-gender warnings, CSV gender fields, missing-register safeguards, and the February/Term-1 protected fixtures. There is no browser coverage for editing an existing pupil's gender because no such product feature exists.
- **Recommended private update contract:** use an official source keyed by `student_ref` with gender; class and full name may be cross-check fields. Dry-run reconciliation must match all 319 current pupils exactly and must report duplicate IDs, missing source rows, source-only rows, unsupported values, class/name mismatches, 3A disagreement, and resulting per-class gender totals. Real pupil rows/mappings must never be committed to the public repository, migration history, CI fixtures, logs, screenshots, or PR comments.
- **Production transaction/rollback:** after explicit approval, update only `attendance.students.gender` inside one transaction using a private ephemeral mapping; verify affected-row count and all invariants before commit. If verification fails, rollback the transaction. After commit, rollback would require the captured private pre-update gender mapping, which must remain outside the repository.

Phase 6B impact-map details:
- **Exact signed baseline:** repository `main` `84c4555a75002926548b20534d08de8fb8e59255`; live Supabase latest migration `20260920060510 attendance_v19_fk_index_coverage`; project remains `ACTIVE_HEALTHY` on Postgres 17.
- **Table contract:** `attendance.enrolments` has `start_date NOT NULL`, nullable `end_date`, `active boolean NOT NULL default true`, and `enrolment_status text NOT NULL default 'ENROLLED'`. Existing lifecycle-adjacent CHECK only enforces `end_date is null or end_date >= start_date`; there is no status vocabulary/shape CHECK.
- **Indexes/uniqueness:** current-enrolment uniqueness is already enforced by `attendance_one_current_enrolment_per_student_idx` on `student_id WHERE end_date IS NULL`; class/student and roster indexes remain unchanged. The proposed 6B CHECK aligns with that end-date definition rather than introducing a second current-row rule.
- **Runtime writers:** live function-source inspection finds exactly three enrolment writers: Transfer In, Transfer Out, and Move Class. `attendance_admin_student_roster`, `attendance_load_register`, `attendance_save_register`, and `attendance.reporting_class_period_facts` read enrolments but do not write them.
- **Access/security:** authenticated table access remains SELECT-only; service role retains full table privileges. Existing enrolment RLS policies remain present as defense-in-depth, but authenticated direct INSERT/UPDATE/DELETE grants were revoked in v15. The three movement RPCs remain authenticated-only `SECURITY DEFINER` with fixed empty `search_path`; roster remains SECURITY INVOKER. No Phase 6B authorization redesign is proposed.
- **Trigger:** `attendance_enrolments_touch` remains the only non-internal enrolment trigger and only updates `updated_at`; no trigger change is proposed.
- **Frontend/DOM:** ownership remains `assets/js/student-management.js`; stable contracts remain `#adminStudentList`, `#studentClassFilter`, `#transferInDialog`, `#tiGender`, `#tiClass`, `#tiDate`, `#transferOutDialog`, `#toDate`, `#moveClassDialog`, `#mcClass`, and `#mcDate`. The browser calls only the four roster/movement RPCs; no direct table API or browser-global wrapper change is proposed.
- **Existing coverage:** Playwright already proves exact Transfer In/Out/Move Class payloads, roster refresh, history-preservation messages, Brunei date defaults and hostile-text escaping, but it mocks the backend and cannot prove database CHECK behavior. The v15 database verifier proves movement authorization, table-write boundaries, result shapes, row status/end-date outcomes and physical attendance-record preservation, but it does **not** currently assert that old rows stay `active=true`, compare historical report results before/after closure, or deliberately reject malformed lifecycle shapes.
- **Implementation verification:** implemented exactly as mapped: one focused v20 lifecycle-CHECK migration, a dedicated rollback-only 6B verifier, a static Phase 0B scope guard, and Phase 4B1V/Phase 5A reconstruction. Final reconciliation head `e2a451ce297f580bd3071ccdeb5a9def1f72af8b` passed Phase 0A `35507270700`, Phase 0B `35507270766`, Playwright `35507270711` (65/65 Chromium in 26.2s, 0 npm vulnerabilities), Phase 5A `35507270715`, and Phase 4B1V `35507270736`; both isolated local stacks applied and recorded `20260920100523`, and all retained reporting/security/lifecycle verifiers passed. PR #99 then merged as signed `main` `9abb0e406b911a4a4d09408b6c32f83d0b9837be`. Existing 3A February 398 / 0.9365 / 93.65% and Term 1 1,051 / 0.9342 / 93.42% fixtures remain exact.
- **Deployment/rollback risk:** production v20 is now applied and validated. No frontend deployment occurred. Rollback is limited to dropping `attendance_enrolments_lifecycle_status_check`; no row rollback is required. The main forward-compatibility cost is intentional: rollover or any future lifecycle status must first extend the allowed vocabulary/shape explicitly.

Verification/build implications:
- current browser suite is 65 tests and already covers Transfer In, Transfer Out, Move Class, 3A gender-complete reporting, incomplete-gender warnings, missing-register safeguards, February/Term-1 fixtures, and reporting populations;
- `.github/workflows/phase0b-backend-contract.yml`, `phase4b1v-local-db-validation.yml`, and `phase5a-security-validation.yml` now include the reconciled v19 state plus the repository/local v20 lifecycle candidate and dedicated lifecycle verifier; exact-head CI must prove the v20 reconstruction and all retained v13-v19 contracts before merge;
- Phase 6A/6B must run Phase 0A, Phase 0B, full Playwright, Phase 4B1V Local DB Validation, and Phase 5A Security Access Validation. No assertion may be weakened to accommodate the new migration;
- Netlify production remains unaffected by repository merges because the site is not Git-linked; any production database/data operation remains a separately approved step.

**Exit:** all agreed Attendance foreign-key coverage is explicit; enrolment lifecycle semantics are constrained/documented without historical-report regression; gender completeness is either completed from an official source or explicitly remains a blocked data-quality item; no Science or unrelated index cleanup is mixed into the phase.

**Next action:** Phase 6C impact/preflight is complete, but the production data operation remains blocked. Obtain an official pupil source keyed by `Student ID / student_ref` with gender (class and full name may be included as cross-checks). Once provided, perform a **dry-run reconciliation only** against all 319 current pupils and STOP before any production update: report duplicates, missing/extra IDs, unsupported values, class/name mismatches, 3A agreement with 14 Male / 11 Female, and expected per-class gender totals. Keep the real mapping private and out of GitHub/migrations/CI/logs. Do not update production gender data until that dry-run is reviewed and explicitly approved.

### Phase 7 — Full equivalence QA
**Status:** Phase 7 is complete. Phase 7D final equivalence review passed and PR #108 merged as signed cleanup `main` `b9dd4ab11b06cc62877640b51dc5d18dfc081c30`. The final review found no unexplained cleanup regression or release-blocking equivalence difference; evidence is recorded in `docs/PHASE7D_FINAL_EQUIVALENCE_REVIEW.md`. Runtime/backend bytes did not drift after the fully hard-gated Phase 7B candidate, known exceptions remain explicitly tracked, and Phase 8 remains unstarted pending a separate pilot checkpoint and explicit approval.

**Goal:** Prove cleanup changed structure, not established outcomes.

Actions:
- run targeted tests after each checkpoint;
- run full browser/regression and hard-gate suites;
- compare known-good report fixtures;
- verify roster, teacher access, corrections, student movements, historical counts, and reporting equivalence;
- review full diff before promotion.

Phase 7 checkpoint split:
- **7A — Equivalence Contract Freeze:** freeze the public-safe acceptance matrix in `docs/PHASE7_EQUIVALENCE_CONTRACT.md`, including exact v1.0 source baseline, live v0.7/v1.0 separation, protected counts/reporting fixtures, browser/DB/security acceptance layers, Phase 6C known exception, and Phase 8 deployment boundary. Documentation/QA contract only; no runtime or production mutation.
- **7B — Full Automated QA:** run Phase 0A, Phase 0B, Phase 1 Playwright, Phase 4B1V, Phase 5A, and all included database verifiers on one exact candidate SHA. Do not weaken assertions.
- **7C — Live Read-only Equivalence Seal:** verify the production Attendance migration/API/security/aggregate/reporting contract without writes and record only sanitized aggregate evidence.
- **7D — Final Equivalence Review:** review the complete release-candidate diff, known exceptions, CI, live seal, deployment boundary, rollback plan, and Phase 8 handoff. If a real defect is found, stop and correct it in a separate focused branch/PR.

Phase 7A frozen baseline:
- cleanup candidate baseline: `7c13336afd3338d7a439406405322579dfe86e45`;
- protected live site remains v0.7 at `https://srlumapas.netlify.app/`;
- the password-recovery race reproduced on live v0.7 does not represent the cleaned v1.0 state: Phase 2A already fixes recovery priority and current browser coverage proves the corrected v1.0 flow;
- live Attendance migration remains `20260920100523 attendance_v20_enrolment_lifecycle_contract`;
- 15 active classes / 319 active pupils / 319 active enrolments / 0 ended enrolments / 0 movement rows;
- 3A remains 137 registers / 3,425 attendance records;
- February 2026 remains 398 / 0.9365 / 93.65% with 17 completed and 0 missing registers;
- Term 1 remains 1,051 / 0.9342 / 93.42% with 45 completed and 0 missing registers;
- Phase 6C remains a known data-quality exception at 294 active pupils with unknown gender and must not be silently treated as a regression or inferred from names;
- Security Advisor baseline remains 2 `rls_enabled_no_policy` INFO, 4 `anon_security_definer_function_executable` WARN, 18 `authenticated_security_definer_function_executable` WARN, and 1 `auth_leaked_password_protection` WARN.

**Exit:** all agreed equivalence checks pass with no unexplained differences. Phase 8 must not begin automatically.

### Phase 8 — Pilot cleaned build
**Status:** repository/pilot setup prepared; deployment has not started. Exact pilot branch `pilot/phase-8-cleaned-v1-preview` is pinned to signed source `dc8e29bbf727c1d4dacf9ad7986e28f450b75718` with zero branch drift. Deployment, Netlify changes, and hosted Auth redirect changes remain separately approval-gated. Detailed execution/rollback checklist: `docs/PHASE8_PILOT_DEPLOYMENT_CHECKLIST.md`.

Deploy a safe preview/staging build and test with admin plus teachers from multiple year levels. Collect real feedback on login, mobile attendance, corrections, reports, and performance. The preview must remain separate from production v0.7. Because signup/recovery redirect to `window.location.origin + '/'`, the exact pilot origin must be explicitly allow-listed in hosted Supabase Auth before real pilot signup/recovery tests; do not replace the production Site URL or remove the production redirect. Pilot writes must be genuine school operations only—do not fabricate transfers, corrections, approvals, or attendance records merely for testing.

**Exit:** representative pilot is stable, the exact deployed SHA is recorded, recovery works on the hosted pilot, known async-risk surfaces have been exercised, rollback is clear, and no unexplained production/reporting drift exists.

### Phase 9 — Promote cleaned version
Verify exact release candidate, backend compatibility, rollback path, tests, CI/hard gates, and diff. Promote only after explicit approval.

**Exit:** cleaned build replaces v0.7 and becomes the production baseline.

### Phase 10 — Resume feature development
Recommended order:
1. CCA annual setup and roster foundation.
2. CCA Saturday attendance.
3. Separate CCA statistics/dashboard.
4. Year-end promotion/rollover.
5. Remaining official attendance statistic once confirmed.
6. Further reporting/export refinement.

## Risk register

### Immediate
- **Fresh Phase 5B:** changing `attendance_save_register` to `SECURITY DEFINER` means its explicit `auth.uid()` / `attendance.can_access_class()` checks become the primary class authorization inside that RPC because the `postgres` owner can bypass table RLS. The current body already validates authentication, class access, school day, exact eligible roster size, duplicate enrolments, class/date eligibility, active status/reason codes, required OTHER note, no-change behavior, correction reason length/presence, correction counter, and audit GUCs. Phase 5B must prove those checks under the privileged execution mode before production application.
- **Fresh Phase 5B:** direct register/record DML can be closed without changing the frontend or RPC contract by revoking authenticated INSERT/UPDATE/DELETE while retaining SELECT. However, this does **not** make attendance history fully immutable: authenticated school-admin DELETE remains possible on parent `academic_years`, `classes`, `students`, and `enrolments`, and existing `ON DELETE CASCADE` paths can indirectly remove registers/records. Treat that as explicit residual risk for Phase 5C/5F rather than silently expanding 5B.
- **Fresh Phase 5B:** live production currently has 137 registers and 3,425 attendance-audit rows, all audit actions are INSERT, and no register has `correction_count > 0`. Therefore production data cannot itself prove correction-update semantics; Phase 5B must rely on the synthetic/local security fixture plus browser correction regressions to validate correction audit behavior before any live change.
- **Fresh Phase 5 post-v14:** authenticated direct write privileges remain on 11 Attendance admin tables: `academic_years`, `calendar_dates`, `classes`, `enrolments`, `schools` (UPDATE), `settings`, `student_movements` (INSERT), `students`, `teacher_class_assignments`, `teacher_school_memberships`, and `terms`. Phase 5B has removed authenticated INSERT/UPDATE/DELETE from `daily_registers` and `attendance_records`. RLS scopes the remaining admin writes, but authorized users can still bypass controlled RPC validation/audit semantics on those remaining tables, so Phase 5C-5F hardening must stay staged rather than blanket revocation.
- **Fresh Phase 5:** Supabase Security Advisor reports leaked-password protection disabled. This is a live Auth hardening gap; the organization is currently on the Free plan, so that specific protection is unavailable without a plan change. Do not guess or silently alter minimum password/character policy—verify the live Auth settings first.
- **Fresh Phase 5:** current live function/grant review found no Attendance function executable by `PUBLIC`; only `attendance_signup_options()` is intentionally anonymous. The 14 authenticated `SECURITY DEFINER` warnings require continued source/authorization testing, but no missing class/admin/auth check was identified in this impact-map review.
- Fresh Phase 0B evidence confirms authenticated direct DML is granted on `attendance.daily_registers` and `attendance.attendance_records` within RLS-accessible classes. This preserves class scoping but can bypass `attendance_save_register` completeness, correction-reason, and register correction-count safeguards. Preserve the live state in Phase 0B; redesign/revoke only in the later security phase with regression coverage.
- Fresh Phase 3B6 read-only live verification confirms authenticated direct table privileges also exist on `attendance.students` and `attendance.enrolments` (SELECT/INSERT/UPDATE/DELETE) and on `attendance.student_movements` (SELECT/INSERT). RLS restricts student/enrolment writes to school administrators, but an already-authorized admin using a custom client can bypass the controlled movement RPCs' date validation and movement-history semantics. Preserve this live state during structural Phase 3B6; review/reduce it only in Phase 5 with dedicated security/regression coverage.
- Fresh Phase 3B7A read-only live verification confirms authenticated direct SELECT/INSERT/UPDATE/DELETE privileges on `attendance.teacher_school_memberships` and `attendance.teacher_class_assignments`. RLS limits writes to school administrators, but an already-authorized admin using a custom client can bypass the coordinated teacher-management RPCs and their `attendance_private.teacher_access_audit` semantics. Preserve the live state during structural Phase 3B7A; review/reduce it only in Phase 5 with dedicated security/regression coverage.
- Fresh Phase 3B7E read-only live verification confirms authenticated direct table-write privileges also exist on reporting-structural tables including `attendance.academic_years`, `attendance.terms`, `attendance.calendar_dates`, and `attendance.classes`; RLS limits those writes to school administrators, but an already-authorized admin using a custom client could alter reporting periods/calendar/class structure outside controlled application workflows. This is not a 3B7E structural-extraction bug; preserve live state and review/reduce it only in Phase 5 with dedicated security/regression coverage.
- `attendance_admin_student_roster` currently has no frontend request-serial guard. A slower older roster refresh can theoretically overwrite a newer post-movement roster in the browser. This is a pre-existing stale-UI correctness gap, not a Phase 3B6 regression; do not mix its behavioural fix into the extraction checkpoint. Record it for a dedicated later correctness follow-up with a deterministic race regression.
- `loadAdminTeachers()` currently has no frontend request-serial guard. A slower older teacher/request refresh can theoretically overwrite a newer post-approval or post-enable/disable UI snapshot. This is a pre-existing stale-UI correctness gap, not a Phase 3B7A regression; do not mix its behavioural fix into the structural extraction. Record it for a dedicated later correctness follow-up with deterministic deferred-RPC coverage.
- `Check Approval` currently calls `enterApp()` without a request-serial or in-flight guard. Two overlapping `attendance_teacher_status` checks can therefore allow an older unauthorized result to render the teacher gate after a newer authorized result has already entered the app. This is a pre-existing UI-routing correctness gap surfaced during Phase 3B7B mapping, not a Phase 3B7B regression; do not mix its behavioural fix into the structural extraction. Record it for a dedicated later correctness follow-up with deterministic deferred-RPC coverage.
- Production migration history includes Attendance pupil-roster/pilot seed migrations. Their historical SQL must **not** be copied into the public repository because it may contain real pupil/attendance data. Record only sanitized version/name metadata and recreate the current structure from the live structural snapshot.
- Phase 1 regression coverage is complete on cleanup `main`; the merged Phase 3B7F3 baseline has 53 Chromium tests including composition-root lifecycle coverage. Exact PR #47 head Phase 0A `35053697452`, Phase 0B `35053697405`, and 53/53 Playwright `35053697404` verification passed before merge.
- Phase 3B5 remains a protected correction-semantics boundary: untouched loaded `PP`/`TM` codes must remain unchanged when another pupil is corrected, absence reason `OTHER` must still require a note, non-school days must never become saveable, and enrolment eligibility/class authorization remain backend-owned invariants. Later refactors must not weaken `attendance_save_register` or introduce direct browser table access.
- v0.7 live and v1.0 cleanup `main` have diverged by design.
- The password-recovery routing race remains a known behaviour of the protected v0.7 production baseline; the Phase 2A fix is merged into cleanup `main` but must not be treated as production until an explicitly approved promotion.
- The shared-device pending-signup bug remains a known behaviour of the protected v0.7 production baseline; the Phase 2B fix is merged into cleanup `main` but must not be treated as production until an explicitly approved promotion.
- Live Netlify Build & deploy settings evidence supplied on 13 September 2026 shows `Current repository: Not linked` for the SR Lumapas site. The site is therefore not currently connected to this GitHub repository for Netlify continuous deployment; merging GitHub `main` does not by itself trigger a Netlify Git deploy. No Netlify setting was changed, and explicit release/promotion discipline remains mandatory.
- Phase 3A changes the frontend loader from inline JavaScript to a same-origin external module. Repository/CI proves source equivalence, and live Netlify evidence clears the Git-linked auto-deploy blocker because the site reports `Current repository: Not linked`. GitHub `main` itself is still unprotected and has no repository rulesets, so explicit PR review/approval discipline remains a required guardrail.
- Stale async-response overwrites are guarded on cleanup `main` by request-serial/selection-key checks with deterministic browser coverage for the protected surfaces implemented in Phase 2C. The student-admin roster, teacher-admin list, and teacher access-check routing surfaces are separately recorded pre-existing gaps and are not silently included in those guarantees. This does not constitute production promotion; v0.7 remains the protected live baseline until an explicitly approved release.
- Fresh Phase 2D live Dashboard evidence confirms the production Site URL/Redirect URL pair still matches the frontend. The wildcard Redirect URL is broader than the exact root path the current frontend requests; leave it unchanged in Phase 2D because no correctness mismatch exists. Review any narrowing or preview-host additions separately with release/pilot scope.
- Phase 3B4 is startup-order sensitive: recovery-link detection and `PASSWORD_RECOVERY` must continue to outrank normal `getSession()` routing. The merged checkpoint keeps one Supabase client and proves the source transformation plus real-browser recovery behavior; it remains a protected invariant for later refactors.
- Phase 3B7F1 is application-startup sensitive: `enterApp()` remains downstream of `ensureTeacherAccess()` and must continue to use the same single Supabase client, exact `attendance_bootstrap` contract, class-selector initialization, saved-class fallback, zero-class guard, and initial `loadRegister()` ordering. The structural extraction must not absorb the deferred Check Approval stale-response fix or alter Auth/recovery startup.
- Phase 3B7F2 is application-navigation sensitive: all six panels must preserve exactly-one-visible and exactly-one-active-tab behavior; Dashboard/Statistics/Reports/Students/Teachers must retain their current lazy-load calls; Statistics and Reports must retain attendance-class fallback; Dashboard → Statistics must preserve class/month routing; returning to Attendance must not generate a new RPC. Keep listener and unload ownership in `main.js` for this checkpoint.
- Phase 3B7F3 closes modularisation without another runtime move: `main.js` intentionally remains the composition root for module initialization, static event registration, generic dialog-close wiring, the unsaved-attendance `beforeunload` guard, and Auth startup. Later work should not extract those responsibilities merely to shrink the file; require a concrete ownership/test benefit.
- Phase 4 reporting population is an explicit business rule: some departments require Whole-Class statistics including SEN pupils, while others require Non-SEN-only statistics. Cleanup v1.0 now exposes that choice explicitly across Monthly Statistics, Dashboard, Term/YTD, Dashboard → Statistics routing, and CSV while preserving Whole Class as the default; `include_in_class_stats` is used only for the explicit Non-SEN population.
- Phase 4B1/v13 is production-applied after explicit approval: live Supabase records `20260917090547 attendance_v13_shared_reporting_model`, while the protected v0.7 frontend continues to call the legacy Whole-Class wrappers. Post-application verification proved exact legacy/v2 Whole-Class shape compatibility, all four protected 3A population fixtures, intended function/grant boundaries, and all 15 Dashboard classes. Repository migration metadata now matches the live ledger; broader direct-DML/security debt remains Phase 5 scope.
- A fresh read-only live comparison across all saved 2026 class-register days found zero differences between the Dashboard's existing direct Whole-Class attendance count and the shared eligible-pupil attendance basis, supporting latest-day consolidation without an observed Whole-Class result change on current data.

### Medium-term
- Frontend module ownership is now split; remaining frontend reproducibility/CSP debt is concentrated in the runtime CDN and inline styling rather than a single-file JavaScript bottleneck.
- The shared v13 reporting engine is live and verified, and cleanup frontend v1.0 now exposes explicit Whole-Class/Non-SEN population selection across Monthly Statistics, Dashboard, Term/YTD, routing, and CSV. The protected live v0.7 frontend is still unchanged and continues to use the legacy Whole-Class path until a separately approved promotion.
- Teacher active assignment role semantics are weaker than signup role semantics: the approval RPC records `class_teacher` / `assistant_teacher` on the signup request/audit but persists the active class-assignment role as generic `teacher`. Redesign only in Phase 5 with authorization/regression coverage.
- Public RPC / `SECURITY DEFINER` surface needs maintained verification.
- Runtime CDN/inline JS/CSS reduce reproducibility and CSP strength.
- Attendance and Science share one Supabase project, increasing operational coupling.

### Lower priority / data debt
- Several foreign keys lack covering indexes.
- Some indexes appear unused but production traffic is still too small for removal decisions.
- 294 of 319 pupils currently lack gender values.
- Enrolment `active`, `end_date`, and `enrolment_status` need formal consistency rules.

## Reference QA fixtures

### 3A February 2026 — Whole class (including SEN; current/default)
- Pupils: 25
- School days: 17
- Registers: 17/17
- Male cumulative: 214
- Female cumulative: 184
- Total cumulative: 398
- Possible attendance: 425
- Average attendance ratio: 0.9365
- Attendance percentage: 93.65%

### 3A February 2026 — Non-SEN pupils only
- Pupils: 24
- School days: 17
- Registers: 17/17
- Male cumulative: 208
- Female cumulative: 184
- Total cumulative: 392
- Possible attendance: 408
- Average attendance ratio: 0.9608
- Attendance percentage: 96.08%

### 3A Term 1 2026 — Whole class (including SEN; current/default)
- Period: 2026-01-03 to 2026-03-12
- Pupils: 25
- Registers: 45/45
- Male cumulative: 567
- Female cumulative: 484
- Cumulative attendance: 1,051
- Possible attendance: 1,125
- Average attendance ratio: 0.9342
- Attendance percentage: 93.42%

### 3A Term 1 2026 — Non-SEN pupils only
- Period: 2026-01-03 to 2026-03-12
- Pupils: 24
- Registers: 45/45
- Male cumulative: 552
- Female cumulative: 484
- Cumulative attendance: 1,036
- Possible attendance: 1,080
- Average attendance ratio: 0.9593
- Attendance percentage: 95.93%

Core rule:

`average attendance ratio = cumulative attendance / possible attendance`

`attendance % = average attendance ratio × 100`

For transfers, use eligible pupil-days. Missing registers must never be treated as zero attendance. Population filtering must be applied to both attendance numerator and possible-attendance denominator.

## Current status

**Current checkpoint:** Phase 8 package manifest is prepared but no external deployment/configuration change has started. Current `main` before this docs checkpoint is `5b335114807756e2979fade2b4e8c3a4aaac6746`; runtime pilot branch remains pinned to `dc8e29bbf727c1d4dacf9ad7986e28f450b75718`. Exact-tree verification corrected an earlier documentation count: the minimal runtime payload is 17 reviewed source files total = `index.html` + **16** JavaScript modules under `assets/js/`, totalling 99,231 bytes by GitHub blob-size metadata, plus deployment-only `_headers`. `docs/PHASE8_PILOT_PACKAGE_MANIFEST.md` records every exact Git blob SHA and the runtime dependency closure. No local CSS/image/font/build artifact is required; repository docs/tests/Supabase/CI files must not be uploaded. Netlify/Auth remain unchanged and production v0.7 remains protected.
- Post-v17 authenticated access to `attendance.teacher_school_memberships` and `attendance.teacher_class_assignments` remains SELECT-only; INSERT/UPDATE/DELETE remain revoked. Service-role full CRUD, RLS, all 8 policies, and both update-touch triggers remain present.
- `attendance_admin_review_teacher_request(...)` retains its signature, owner `postgres`, `SECURITY DEFINER` mode, fixed empty `search_path`, authenticated/service-role EXECUTE, and no `anon`/`PUBLIC` EXECUTE. Its intended post-v17 body MD5 is `8b6832c6b1fc21edcb282e0bc0790b09`. `attendance_admin_set_teacher_active(...)` remains unchanged at MD5 `888384aead500bc5e67e5a7e1beb0f90`.
- V17 expanded the class-assignment role constraint to `teacher`, `viewer`, `class_teacher`, and `assistant_teacher`, while leaving `attendance.can_access_class()` unchanged at MD5 `9a8a3f4714689d31658782522c950530` and `attendance_teacher_status()` unchanged at MD5 `0c4cf334a1446ce35f0d83d1750fc610`.
- The deterministic production backfill changed exactly one assignment from generic `teacher` to `class_teacher`; final assignment roles are 1 `teacher` + 1 `class_teacher`, with zero ambiguous or remaining eligible backfill candidates. School membership roles remain 1 admin + 1 teacher.
- Teacher signup/audit aggregates remain unchanged: 1 approved signup request, 1 `request_submitted`, and 1 `request_approved`.
- Protected attendance history remains exactly 137 registers / 3,425 attendance records / 3,425 audit rows, with zero corrected registers, zero correction-count sum, zero audit UPDATEs, and zero audit DELETEs.
- Post-v17 protected reporting-population and shared-engine equivalence verifiers returned zero mismatches. Rollback-scoped production-safe smoke checks confirmed the backfilled teacher retains class access, `attendance_teacher_status()` reports `class_teacher`, and bootstrap access remains intact.
- The repository `attendance_teacher_role_v17_contract.sql` is synthetic/local-only and is not run verbatim against production; production verification uses equivalent read-only and rollback-isolated checks derived from live authorization context.
- Post-DDL Security Advisor shows no new v17-specific anonymous Attendance exposure. Existing known findings remain: two RLS-with-no-policy informational findings, anonymous `attendance_signup_options()` plus three Science RPC warnings, authenticated `SECURITY DEFINER` advisories, and leaked-password protection disabled.
- Phase 5F1 impact mapping confirmed the cleanup frontend has zero `.from(...)` table calls and no structural-admin screen; all current runtime data access is RPC/Auth based.
- Post-v18 authenticated access to `academic_years`, `calendar_dates`, `classes`, `schools`, `settings`, and `terms` is SELECT-only; INSERT/UPDATE/DELETE are revoked on all six. A schema-wide check confirms zero authenticated direct-write tables remain in Attendance. Service-role CRUD, RLS, all structural policies/triggers/FKs/constraints, and policy/trigger fingerprints are unchanged.
- The structural cascade/reporting risk that motivated 5F1 is now closed at the direct authenticated DML boundary: custom signed-in admin clients can no longer directly delete the 3A class or alter calendar/term structure through table DML. No replacement structural writer RPC was introduced; rollover/structural administration remains a later controlled-design checkpoint.
- Post-v18 protected attendance history remains exactly 137 registers / 3,425 attendance records / 3,425 audit rows, with zero corrected registers, zero correction-count sum, zero audit UPDATEs, and zero audit DELETEs. Protected reporting remains exact: 3A February `398/425 = 0.9365 = 93.65%` with 17 completed registers; 3A Term 1 `1051/1125 = 0.9342 = 93.42%` with 45 completed registers. Reporting population and shared-engine equivalence verifiers returned zero mismatches.
- Post-v18 Security Advisor shows no new v18-specific exposure. Existing baseline findings remain unchanged, including two RLS-with-no-policy informational findings, anonymous `attendance_signup_options()` plus three Science RPC warnings, authenticated `SECURITY DEFINER` advisories, and leaked-password protection disabled. Science object counts remained unchanged and v18 referenced no Science object.
- Phase 5F1 explicitly excludes Auth-policy changes, replacement structural-write RPCs, FK redesign, Science, frontend/DOM, and Netlify changes. Phase 5F2 remains a separate decision after authoritative project-level Auth settings are available.
- Phase 5F2 repository ownership map: `assets/js/auth-session.js` owns `signInWithPassword`, `resetPasswordForEmail`, recovery `updateUser({password})`, recovery-session sign-out, `PASSWORD_RECOVERY`, and startup session priority; `assets/js/teacher-access.js` owns teacher `signUp`; `assets/js/supabase-client.js` pins `@supabase/supabase-js@2.114.0` with persisted sessions, auto-refresh, and URL-session detection. Stable DOM contracts are login `#loginView/#loginForm/#email/#password/#forgotBtn/#openSignupBtn`, signup `#signupView/#signupForm/#signupName/#signupEmail/#signupPassword/#signupClass/#signupRole/#signupBtn`, and recovery `#recoveryView/#recoveryForm/#newPassword/#confirmPassword/#recoveryBtn`.
- Current client-side password validation is limited to HTML `minlength="8"` on signup/recovery plus explicit recovery confirmation equality. Login intentionally has no local minimum check. There is no current client handling/test contract for required character classes, leaked-password rejection, structured weak-password errors, reauthentication nonce/current-password requirements, or server/client policy equivalence.
- `tests/e2e/auth-access.spec.mjs` currently contains 17 Auth/access browser tests, including email-verification return, failed signup, shared-device signup isolation, forgot-password redirect, recovery priority, confirmation mismatch, successful old/new password transition with preserved teacher scope, recovery sign-out failure, and external sign-out behavior. The synthetic harness mocks `signInWithPassword`, `signUp`, `resetPasswordForEmail`, `updateUser`, `signOut`, and Auth state events; any 5F2 implementation must extend—not weaken—these contracts.
- Shared-project impact is concrete: Science student PIN/token login is separate from Supabase password Auth, but one `science_teacher_profiles` row is linked to an Auth user who is also an active Attendance member. Therefore project-level password/reauth policy changes affect Attendance and at least Science teacher access, but not the Science student PIN policy.
- Security Advisor still reports leaked-password protection disabled. Supabase documentation states leaked-password protection is a Pro-plan-and-above feature, while the verified organization plan is Free; treat this as a documented residual risk/future upgrade option rather than an immediately implementable 5F2 control.
- The connected Supabase actions still do not expose `GET /v1/projects/{ref}/config/auth`, the repository has no `supabase/config.toml`, and direct project-network probing is unavailable; however, the Phase 5F2 hosted baseline required for this decision is now resolved by direct Supabase Dashboard screenshots. Future hosted Auth changes must still be verified from the Dashboard or an authoritative Management-API read rather than inferred from database state.
- The hosted Auth baseline was originally resolved by direct Dashboard screenshots: Email provider ON; allow new signup ON; confirm email ON; secure email change ON; secure password change OFF; require current password OFF; leaked-password protection OFF/unavailable on Free; minimum password length 6; password character requirements unset; email OTP expiry 3600 seconds and length 8; Site URL `https://srlumapas.netlify.app/`; redirect allow-list `https://srlumapas.netlify.app/**`; custom SMTP OFF; email rate limit 2/hour; sign-up/sign-in 30 requests/5 min/IP; CAPTCHA OFF. On 20 September 2026, after explicit approval to change only the minimum, the user confirmed the Dashboard minimum was changed from 6 to 8 with all other instructed settings left unchanged. The connected Supabase tools cannot independently read hosted Auth config, so the current minimum=8 remains user-confirmed live-system evidence.
- Phase 5F2A repository/local implementation keeps the existing HTML `minlength="8"` contract, adds structured `weak_password` handling using Supabase Auth `error.code` for login/signup/recovery, and adds browser regressions proving 7-character signup/recovery never reach Auth plus safe handling of server weak-password responses. Existing users rejected at sign-in are directed to Forgot password; recovery remains on Set New Password after server rejection. No DOM IDs, Supabase RPCs/tables/grants/RLS/SECURITY DEFINER functions, Science objects, hosted Auth settings, SMTP/CAPTCHA settings, or Netlify behavior change in 5F2A.

- Initial exact head `24a74a1253d1078a2e1cf76a2678e5823464fb1e` passed all five required gates: Phase 0A `35419249575`, Phase 0B `35419249610`, Playwright `35419249677` with 60/60 Chromium tests in 25.1s, Phase 4B1V Local DB Validation `35419249618`, and Phase 5A Security Access Validation `35419249599`. Both isolated stacks applied local migration `20260919050000`; Phase 5A passed the core contract, updated security contract, v15 movement, v17 teacher-role, and v18 structural-write verifiers; Phase 4B1V additionally passed protected reporting population, shared-engine equivalence, and public-API verifiers.



- PR #66 reconciliation base was signed `main` `74d1858f84a5ba88504a42dd90320baabee26b53`; approved exact head `49ea709896dfea3b53521cd3f6cdfe367568d7d0` changed source/metadata only and merged as `a344514a7a7f99175a74e8f718fb64c898a93bb1`.
- Reconciled repository v14 source is `supabase/migrations/20260918094529_attendance_v14_register_write_boundary.sql`; its SQL content remains the exact reviewed blob `7659c0f3595237bbc8e7d6522cf83a28d6e19e49`.
- After explicit approval, that exact reviewed SQL was applied once through the Supabase migration mechanism. Production recorded live migration **`20260918094529 attendance_v14_register_write_boundary`**.
- Post-apply `attendance_save_register(uuid,date,jsonb,text)` is `SECURITY DEFINER`, owner `postgres`, `VOLATILE`, fixed empty `search_path`, with the same ACL and the same body MD5 `db200d65b686af25c306aba8429ef471` / 7,944 body characters. `PUBLIC` / `anon` still cannot execute; `authenticated` / `service_role` still can.
- Authenticated access to `attendance.daily_registers` and `attendance.attendance_records` is now SELECT-only: INSERT/UPDATE/DELETE are revoked. Service-role full CRUD remains unchanged. Both tables remain RLS-enabled/not-forced; all eight existing RLS policies and all three actor/audit triggers retain their pre-v14 fingerprints.
- Attendance RPC inventory is now exactly 21 total = 15 `SECURITY DEFINER` + 6 invoker; anon-executable count remains 1 and authenticated-executable count remains 21.
- Security Advisor added exactly the expected authenticated-`SECURITY DEFINER` warning for `attendance_save_register`. The existing anonymous warnings, including the three Science auth RPCs and Attendance signup-options, are unchanged; no new anonymous Attendance exposure was introduced.
- Live behavior verification passed without data mutation: a synthetic unauthorized UID was rejected before save; an actually assigned teacher replaying an existing saved-register payload returned `ok=true`, `no_changes=true`, `changed_records=0`, `correction_count=0`.
- Final live data/audit fingerprint remains exactly 137 registers, 3,425 attendance records, 3,425 audit rows, zero corrected registers, zero correction-count sum, zero audit UPDATEs, and zero audit DELETEs.
- All protected reporting fixtures remain exact after production v14: February Whole Class `398/425 = 0.9365 = 93.65%`; February Non-SEN `392/408 = 0.9608 = 96.08%`; Term 1 Whole Class `1051/1125 = 0.9342 = 93.42%`; Term 1 Non-SEN `1036/1080 = 0.9593 = 95.93%`.
- No rollback was required. No frontend runtime/DOM, Auth setting, Science object, production Attendance row, Netlify/deployment, public default privilege, or Phase 5C change was made.
- **Repository/live ledger reconciliation:** merged. Migration filename, reconstruction workflows, backend README, sanitized migration history, and this roadmap are aligned to live version `20260918094529` while preserving the exact v14 SQL bytes. The reconciliation did not apply/reapply v14.
- Phase 5C impact mapping quantified the movement-history risk: production currently has 319 students / 319 current enrolments / 0 student-movement rows; 25 enrolments already carry attendance history and have no movement-FK reference. Direct authenticated admin DML on students/enrolments can therefore bypass movement validation and reach attendance history through existing parent cascades.
- The repository/local v15 candidate moves Transfer In / Transfer Out / Move Class behind controlled `SECURITY DEFINER` boundaries and revokes direct authenticated INSERT/UPDATE/DELETE on `students`, `enrolments`, and `student_movements` while retaining SELECT, existing RLS, and service-role privileges. Transfer Out / Move Class add an explicit active-admin guard before privileged enrolment access and constrain the enrolment to a school administered by the caller.
- The dedicated rollback-only `attendance_student_movement_v15_contract.sql` verifies blocked direct DML, teacher/outsider rejection on all three movement RPCs, authorized Transfer In, Transfer Out attendance-history preservation, and Move Class old-history/new-eligibility semantics.
- Structural-parent cascade risk through classes/academic years remains outside Phase 5C and is still assigned to later Phase 5F work.

**Rollback remains available if a later production regression is discovered:** in one transaction, restore authenticated INSERT/UPDATE/DELETE on the two register tables and return `attendance_save_register(uuid,date,jsonb,text)` to `SECURITY INVOKER`, then rerun the frozen pre-v14 fingerprint checks. No data rollback is required by v14 itself because the migration transformed no data.

**v15 rollback remains available if a later production regression is discovered:** restore the exact pre-v15 definitions of `attendance_admin_transfer_in`, `attendance_admin_transfer_out`, and `attendance_admin_move_class` from the captured baseline, return them to `SECURITY INVOKER`, and restore the pre-v15 authenticated DML grants on `students`, `enrolments`, and `student_movements`. Then rerun the movement/security contracts plus the protected reporting fixtures. v15 transformed no existing rows, so rollback is definition/grant restoration rather than data reversal.

**v16 rollback remains available if a production regression is discovered:** restore authenticated INSERT/UPDATE/DELETE on `attendance.teacher_school_memberships` and `attendance.teacher_class_assignments`, then rerun the teacher-management verifier, security/access contract, bootstrap/status checks, protected reporting fixtures, and full regression suite. V16 changed no rows and no function bodies, so rollback is grant restoration only.

**v17 rollback remains available if a production regression is discovered:** restore the exact pre-v17 `attendance_admin_review_teacher_request(...)` body so new class assignments write generic `teacher`; convert only `class_teacher` / `assistant_teacher` values in `attendance.teacher_class_assignments` back to generic `teacher`; restore the pre-v17 role constraint allowing only `teacher` / `viewer`; then rerun teacher-role/security checks, bootstrap/status/class-access checks, protected reporting fixtures, and the full browser suite. Approved assignment type remains preserved in `teacher_signup_requests` and private audit history, so rollback does not delete approval or attendance history.

**v18 rollback remains available if a production regression is discovered:** restore the exact pre-v18 authenticated structural DML grants—INSERT/UPDATE/DELETE on `academic_years`, `calendar_dates`, `classes`, `settings`, and `terms`, and UPDATE on `schools`—while preserving SELECT/RLS/service-role state; then rerun the structural/security contracts, protected reporting fixtures/equivalence/public API, and the full browser suite. V18 changed no rows, functions, policies, triggers, FKs, or constraints, so rollback is grant restoration only.

**Next action:** STOP after this Phase 5 production/Auth documentation-closure checkpoint. Do not automatically start Phase 6. The next possible work is a fresh **Phase 6 performance/data-model impact map** from the exact signed `main`, covering Attendance-only foreign-key indexing, enrolment `active` / `end_date` / `enrolment_status` consistency, and official-source gender completeness while preserving all reporting/history invariants and Science isolation. Phase 6 requires separate approval before implementation. Hosted Auth rollback remains minimum password length 6 if a password-policy regression is later confirmed; do not change SMTP, CAPTCHA, leaked-password protection, secure-password/current-password settings, Science, SQL, or Netlify as part of this closure.

**Recommended thinking effort:** High.

## Change log

- **25 Sep 2026:** Phase 8 package manifest prepared without deployment. Exact pilot-tree inspection found 16 JavaScript modules under `assets/js/`, correcting the earlier checklist wording of 15. The minimal runtime package is therefore 17 reviewed source files (`index.html` + 16 JS modules), totalling 99,231 bytes by GitHub blob-size metadata, plus one generated deployment-only `_headers` file. `docs/PHASE8_PILOT_PACKAGE_MANIFEST.md` records each exact Git blob SHA, file size, dependency closure, expected package tree, excluded repository-only paths, and pre-upload verification steps. `index.html` references only `./assets/js/main.js`; `main.js` closes over the remaining local modules; the only external module is pinned Supabase JS 2.114.0 from jsDelivr. No Netlify deployment, Supabase Auth change, SQL migration, Science change, or production-data mutation occurred.

- **25 Sep 2026:** Phase 8 deployment preflight tightened without external changes. No Netlify connector is available through the current ChatGPT integrations, so production account linkage cannot be live-read from here. Current Netlify documentation confirms manual drag-and-drop deployment can create/update a project without continuous Git deployment and that `_headers` in the publish directory is a supported custom-header mechanism. Repository-root inspection shows the signed pilot source also contains `docs/`, `tests/`, `supabase/`, CI files and the cleanup roadmap; therefore the pilot must not upload the whole repository. The approved deployment shape is a minimal pre-built folder containing exact pilot-SHA `index.html`, exact pilot-SHA `assets/js/`, plus deployment-only `_headers` mirroring the existing security-header policy. Supabase redirect guidance confirms redirect targets must be allow-listed; because cleanup v1.0 requests `window.location.origin + '/'`, the narrow pilot entry after hostname assignment is `https://<pilot-site>.netlify.app/`, while the production Site URL/redirect remain unchanged. No Netlify deploy, Auth config change, SQL migration, Science change, or production-data mutation occurred.

- **25 Sep 2026:** Phase 8 repository/pilot setup prepared without deployment. Created exact pilot branch `pilot/phase-8-cleaned-v1-preview` at signed `main` `dc8e29bbf727c1d4dacf9ad7986e28f450b75718`; compare result is identical (0 ahead / 0 behind / zero changed files). Fresh read-only preflight confirms live Attendance still ends at v20, API/security boundary and protected roster/history/reporting fixtures remain unchanged, and the private audit table remains RLS-off but isolated from anon/authenticated/PUBLIC by schema/table privileges. Added `docs/PHASE8_PILOT_DEPLOYMENT_CHECKLIST.md` defining exact-source deployment, hosted Auth redirect prerequisite, real-operation-only write rules, staged pilot tests, async-risk checks, stop conditions, evidence hygiene, and rollback. No Netlify deployment, Auth config change, SQL migration, Science change, or production-data mutation occurred.

- **24 Sep 2026:** Phase 7 formally closed after PR #108 merged. Approved exact head `bd580e8d2011ed8fb54dbedece4ee18f9a5c7a59` passed Phase 0A `36009786256`, Phase 0B `36009786156`, and Playwright `36009786831` (65/65 in 26.9s; 0 npm vulnerabilities), then merged with expected-head protection as `b9dd4ab11b06cc62877640b51dc5d18dfc081c30`, now verified as `main`. The PR was documentation-only and introduced no runtime, frontend/DOM/API/RPC/SQL migration/grant/RLS/policy/trigger/Auth/Science/Netlify/production-data change. Phase 7 exit criteria are satisfied; Phase 8 remains unstarted pending a separate pilot impact map and explicit approval.

- **24 Sep 2026:** Phase 7D final equivalence review completed from signed `main` `e85a20e5f797dd9b4d0064455752e2aa383cacbd`. Full cleanup diff from frozen original v1.0 baseline `c578b4240a2bd9899db602fa818bda99bd6ff3cd` is 377 commits ahead / 0 behind across 63 files; the staged diff is fully accounted for by frontend cleanup/modularisation, repository backend reconstruction and v13-v20 hardening, reporting changes, tests/verifiers, CI, and documentation. No runtime/backend drift occurred after the fully hard-gated Phase 7B head `03413456970ffb00f46ef6194ed25236cce4a3bc`; subsequent changes are docs only. Current source recheck confirms 171 unique DOM IDs / zero duplicates, zero direct table `.from(...)` access, all 21 public Attendance RPCs used through RPC/Auth boundaries, and no Attendance-specific wrapper chain. Known exceptions are retained rather than hidden: Phase 6C gender incompleteness, private audit RLS-off with client privilege isolation, leaked-password warning, three inherited async UI races, and repository-governance/deployment controls requiring explicit process discipline. Phase 8 handoff requires an isolated exact-SHA preview with v0.7 untouched and no new DB/Auth/Science changes. Detailed evidence is in `docs/PHASE7D_FINAL_EQUIVALENCE_REVIEW.md`. Phase 8 remains unstarted.

- **24 Sep 2026:** Phase 7C live read-only equivalence verification completed against signed cleanup `main` `71ba744fc23598e11bf98b4efc028c52cc185162`. Latest Attendance migration remains `20260920100523 attendance_v20_enrolment_lifecycle_contract`. Protected roster/history/reporting aggregates and all four 3A Whole-Class/Non-SEN fixtures remain exact; repository `attendance_contract.sql` and read-only FK-index verifier returned zero mismatches; public RPC signatures/security modes/search paths/EXECUTE grants, table grants, RLS/policies/triggers, and validated enrolment lifecycle CHECK match the frozen contract; Security Advisor counts remain unchanged. The private attendance audit table still has RLS disabled but no anon/authenticated/PUBLIC private-schema usage or client table privileges, matching the documented privilege-isolation design while remaining a residual defense-in-depth risk. No runtime, SQL migration, grant/RLS/policy/trigger, Auth, Science, Netlify, or production-data change occurred. Sanitized evidence is in `docs/PHASE7C_LIVE_READ_ONLY_SEAL.md`. Phase 7D remains unstarted.

- **24 Sep 2026:** Phase 7B formally closed after merge verification of PR #105. Final approved exact head `03413456970ffb00f46ef6194ed25236cce4a3bc` passed Phase 0A `35978518239`, Phase 0B `35978518257`, Playwright `35978518244` (65/65 in 26.5s, 0 npm vulnerabilities), Phase 4B1V `35978518195`, Phase 5A `35978518253`, and historical-v16 workflow `35978518209` with both jobs PASS and the unchanged v16 teacher-management verifier explicitly passing at its v16 boundary. PR #105 merged as `47fc66b4528e42a6a84ba2a5327eec80e66ddfc5`, now verified as `main`. No runtime, DOM, RPC, SQL migration, grant/RLS/policy/trigger, Auth, Science, Netlify, or production-data change occurred. Phase 7C is next and remains unstarted pending a separate checkpoint.

- **24 Sep 2026:** Phase 7B replacement implementation validation passed on PR #105 head `50bfb7b14259a27e37099d708f25134e743ff76b`. Exact-head current gates: Phase 0A `35978090919` PASS; Phase 0B `35978090799` PASS; Playwright `35978090834` PASS with 65/65 Chromium tests in 26.6s and 0 npm vulnerabilities; Phase 4B1V `35978090883` PASS through current v20 with all current-compatible DB verifiers; Phase 5A `35978090832` PASS through current v20 with all current-compatible security/lifecycle verifiers. Historical workflow `35978090759` PASS in both reporting and security jobs; each stopped exactly at v16 `20260919005727`, confirmed the current v16 teacher-management verifier is byte-identical to signed v16 checkpoint `3816f87f32663eb3f4ef9edd8155038d70bae1ce`, and logged the v16 verifier PASS unchanged. The security historical job uses the signed-v16 security-access companion because the current security-access verifier intentionally evolved after v16. No runtime or production mutation occurred. Final exact-head rerun is required after this evidence update before merge approval.

- **24 Sep 2026:** Phase 7B historical-boundary correction approved after PR #104 exposed an invalid verifier-placement assumption. PR #104 exact head `cb7f49d7ac5b36d6dc45b9888452419dbf4c71ac` passed Phase 0A, Phase 0B, and Playwright (65/65, 26.5s, 0 vulnerabilities), but both Phase 4B1V and Phase 5A failed at the unchanged v16 assertion `Generic teacher class-assignment contract changed`. Repository history confirms v16 intentionally required generic class-assignment `teacher`, while Phase 5E/v17 deliberately introduced authoritative `class_teacher` / `assistant_teacher` persistence. PR #104 was closed unmerged. Replacement design: preserve the v16 verifier unchanged, reconstruct isolated historical stacks only through live-aligned v16 `20260919005727`, run the original reporting/security v16 boundary checks there, and keep current v20 Phase 4B1V/5A semantics unchanged. The new historical workflow is also added to the Phase 4B1V/5A path triggers so all five current hard gates and the historical boundary run on the same PR head. No runtime, SQL body/migration, Auth, Science, Netlify, or production-data change is included.

- **24 Sep 2026:** Phase 7A formally closed after post-merge verification of PR #102. Exact approved head `f897eff8a1d5eae25b7f7cbf505a1167e8db5331` remained based on `7c13336afd3338d7a439406405322579dfe86e45` with 2 commits ahead / 0 behind and exactly 2 QA/docs files; exact-head Phase 0A `35962936706`, Phase 0B `35962936636`, and Playwright `35962936646` were green with 65/65 Chromium tests in 27.5s and 0 npm vulnerabilities, and no reviews/comments/threads were present. PR #102 merged as `2a6a70650e8191313bd7d825035dc277fa77bce3`, now verified as `main`. Fresh read-only production checks still match the frozen v20 aggregate/security baseline. No runtime, DOM, RPC, SQL migration, grant/RLS/policy/trigger, Auth, Science, Netlify, or production-data change occurred. Phase 7B is next but has not started.

- **24 Sep 2026:** Phase 7A equivalence-contract freeze implemented from exact signed `main` `7c13336afd3338d7a439406405322579dfe86e45` on `qa/phase-7a-equivalence-contract`. Added public-safe `docs/PHASE7_EQUIVALENCE_CONTRACT.md` and updated the canonical roadmap only; no application runtime, DOM, RPC, SQL migration, grant/RLS/policy/trigger, Auth setting, Science object, Netlify deployment, or production data changed. Fresh live read-only evidence still shows `20260920100523 attendance_v20_enrolment_lifecycle_contract`, 15 active classes / 319 active pupils / 319 active enrolments, 3A 137 registers / 3,425 attendance records, February 398 / 0.9365 / 93.65%, Term 1 1,051 / 0.9342 / 93.42%, 294 unknown-gender pupils, and the unchanged Security Advisor baseline. The password-recovery race reproduced on live v0.7 is explicitly distinguished from cleaned v1.0, where Phase 2A already contains the corrected recovery flow. Exact-head CI remains mandatory before 7A can be considered ready; Phase 7B/7C/7D have not started.

- **20 Sep 2026:** Phase 6C read-only impact/preflight completed from signed `main` `2d1aff6d6acc5580437f93f513e07dc1ef3fd42e`. Live gender completeness remains 25 known in 3A (14 Male / 11 Female) and 294 unknown across the other 14 classes, with no live `Other` values. `attendance.students.gender` is nullable text; authenticated table access is SELECT-only; the only current students writer is `attendance_admin_transfer_in`; no existing UI/RPC edits gender for an already-enrolled pupil. Reporting reads the current gender dynamically, so a future backfill will retrospectively reclassify historical male/female splits while protected total attendance mathematics must remain invariant. Recommended path is a private transactional production-only update after an exact 319/319 dry-run keyed by official `student_ref`; real mappings must never enter the public repository or migration history. No production/repository runtime, SQL, RPC, grant/RLS/policy/trigger, frontend/DOM, Auth, Science, Netlify, or pupil-data mutation occurred. Production update remains blocked pending the official source and explicit approval.

- **20 Sep 2026:** Phase 6B fully closed after PR #99 repository/live v20 reconciliation. Final reconciliation head `e2a451ce297f580bd3071ccdeb5a9def1f72af8b` passed Phase 0A `35507270700`, Phase 0B `35507270766`, Playwright `35507270711` (65/65 in 26.2s; 0 vulnerabilities), Phase 5A `35507270715`, and Phase 4B1V `35507270736`; both isolated local stacks applied/recorded live version `20260920100523` and all reporting/security/lifecycle verifiers passed. PR #99 merged as signed `main` `9abb0e406b911a4a4d09408b6c32f83d0b9837be`. Fresh post-merge live verification confirms the same validated lifecycle CHECK, 319 enrolments with zero lifecycle/date-shape violations, 0 movement rows, unchanged security-advisor baseline, 15 active classes / 319 active pupils / 319 active enrolments, 3A 137 registers / 3,425 attendance records, February 398 / 0.9365 / 93.65%, and Term 1 1,051 / 0.9342 / 93.42%. Phase 6C remains blocked pending an official gender source and privacy-safe update method.

- **20 Sep 2026:** Phase 6B repository/live migration-ledger reconciliation implemented from signed `main` `b044fea6821e1eb802bb957c06c1b2066d6ffad1` on `cleanup/phase-6b-production-reconciliation`. Repository v20 is renamed from candidate version `20260920082000` to live version `20260920100523`; the SQL bytes remain unchanged with Git blob `e11760161e7d2a36c1d6eff23d01a92168bf68c5`. Phase 0B, Phase 4B1V, and Phase 5A now reconstruct/check live version `20260920100523`; sanitized production history now records v20 and 39 Attendance migrations; README and roadmap reflect production-applied/reconciled state. No database/production mutation, SQL-body/verifier change, RPC/grant/RLS/policy/trigger, frontend/DOM, Auth, Science, data, or Netlify change is included. Exact-head five-gate validation is pending.

- **20 Sep 2026:** Production v20 applied from signed `main` `4c51f461cede7db0367d0126cece808d96e45bab` using the exact merged SQL from blob `e11760161e7d2a36c1d6eff23d01a92168bf68c5`. Supabase recorded live migration `20260920100523 attendance_v20_enrolment_lifecycle_contract`. Post-application verification confirmed the lifecycle CHECK is present and validated, all 319 enrolments satisfy the contract, movement rows remain 0, grants/RLS/policies and movement RPC metadata are unchanged, security-advisor baseline is unchanged, protected counts remain 15 active classes / 319 active pupils / 319 active enrolments and 3A 137 registers / 3,425 attendance records, and protected reporting remains February 398 / 0.9365 / 93.65% and Term 1 1,051 / 0.9342 / 93.42%. The applied SQL touched only `attendance.enrolments` and contained no Science, Auth, RPC, RLS, grant, trigger, frontend, or row mutation. Repository/live timestamp reconciliation is now required because the repository candidate is still named `20260920082000_attendance_v20_enrolment_lifecycle_contract.sql`.

- **20 Sep 2026:** Phase 6B repository/local implementation merged via PR #96. Final exact head `6d46bcd1b00a8107841295cdd9535f7041cc2fe4` passed Phase 0A `35499700093`, Phase 0B `35499699983`, Playwright `35499700031` (65/65 in 27.4s; 0 vulnerabilities), Phase 5A `35499700008`, and Phase 4B1V `35499699962`, then merged as signed `main` `eb1bb3915acd423113ec92691ebcfbd6101b06d6`. Candidate v20 migration blob is `e11760161e7d2a36c1d6eff23d01a92168bf68c5`. Fresh production-v20 preflight confirmed production still ends at v19 `20260920060510`, the lifecycle CHECK is absent, 319/319 live enrolments remain current and valid with zero status/date-shape violations, movement rows remain 0, security-advisor baseline is unchanged, protected 15/319/319 and 3A 137/3,425 counts remain unchanged, and February 398 / 0.9365 / 93.65% plus Term 1 1,051 / 0.9342 / 93.42% remain exact. No production mutation occurred. Production v20 now awaits separate explicit approval; Phase 6C has not started.

- **20 Sep 2026:** Phase 6B repository/local implementation completed from signed `main` `e33467074ae689d15a28e8f74d99ff534af51fa1` on `cleanup/phase-6b-enrolment-lifecycle`. Candidate v20 `20260920082000_attendance_v20_enrolment_lifecycle_contract.sql` adds one validated enrolment lifecycle CHECK only and deliberately leaves `active` independent. New rollback-only verifier proves malformed shapes fail, closed Transfer Out / old Move Class rows remain `active=true`, and historical `reporting_class_period_facts` JSON is unchanged through each closure boundary. Phase 0B freezes exact DDL scope; Phase 4B1V and Phase 5A reconstruct v20 after v19. First validation head `2470474162a2178ad42120ad71c45042e128e845` passed Phase 0A `35499481059`, Phase 0B `35499481053`, Playwright `35499481066` (65/65 in 26.5s; 0 vulnerabilities), Phase 5A `35499481048`, and Phase 4B1V `35499481051`; local logs confirm `20260920082000` applied and `attendance_enrolment_lifecycle_v20_contract.sql` passed in both database workflows while reporting equivalence/public API remained green. No production mutation occurred; production remains v19. Final exact-head CI and PR review remain pending.

- **20 Sep 2026:** Phase 6B enrolment-lifecycle impact mapping completed read-only from signed `main` `84c4555a75002926548b20534d08de8fb8e59255` and live production v19 `20260920060510 attendance_v19_fk_index_coverage`. Live data has 319 enrolments, all `active=true`, `end_date=null`, `enrolment_status='ENROLLED'`; there are 0 ended enrolments and 0 movement rows, and zero rows violate the proposed current lifecycle shape. Runtime writers are exactly Transfer In / Transfer Out / Move Class; reporting, register load/save and admin roster are readers. Historical/reporting eligibility depends on `active` plus start/end dates, so closed movement rows must remain `active=true`. Preferred future repository/local change is one CHECK tying null end dates to `ENROLLED`/`TRANSFERRED IN` and non-null end dates to `TRANSFERRED OUT`/`MOVED CLASS`, with no RPC/frontend/security redesign. Current browser tests mock the backend; the v15 verifier preserves rows but does not yet prove closed rows remain active, historical report equivalence under closed enrolments, or invalid status/date rejection. No migration, schema/data, RPC/RLS/grant/function/trigger, Science/Auth/frontend/Netlify or production mutation occurred during mapping.

- **20 Sep 2026:** Phase 6A closed after repository/live migration-ledger reconciliation. PR #93 final exact head `46716810fcf260186b48886cd212a78306c6d73f` passed Phase 0A `35493805437`, Phase 0B `35493805446`, Playwright `35493805433` (65/65 in 25.6s; 0 vulnerabilities), Phase 4B1V `35493805432`, and Phase 5A `35493805478`, then merged as signed `main` `18d551698c40519a96e4511b5813bc3e618f1d03`. Repository and production are aligned on `20260920060510 attendance_v19_fk_index_coverage` with unchanged reviewed SQL blob `998e10512a87f85c32b71b0a59d8fa0b2bdb23e4`. Production remains verified with zero uncovered Attendance foreign keys, unchanged security baseline and protected counts/reporting. Phase 6B has not started; next step is impact mapping only before any schema constraint proposal.

- **20 Sep 2026:** Production v19 applied after explicit approval and verified, then Phase 6A repository/live migration-ledger reconciliation started from signed `main` `f0c6a59239d0f494f62350d8949497c07d8f039e` on `cleanup/phase-6a-production-reconciliation`. Supabase recorded live `20260920060510 attendance_v19_fk_index_coverage`; all six exact indexes exist, zero Attendance/Attendance-private foreign keys remain uncovered, and the `unindexed_foreign_keys` advisor finding is absent. Security-advisor baseline, 15 active classes / 319 active pupils / 319 active enrolments, 3A 137 registers / 3,425 attendance records, February 398 / 0.9365 / 93.65%, and Term 1 1,051 / 0.9342 / 93.42% remain unchanged. The repository migration was renamed from candidate version `20260920032000` to live version `20260920060510` while preserving identical Git blob `998e10512a87f85c32b71b0a59d8fa0b2bdb23e4`; CI reconstruction references and sanitized production metadata were aligned. First validation head `a0597df4d422caab04e3b5c8815a97fd783de15b` passed Phase 0A `35493604711`, Phase 0B `35493604704`, Playwright `35493604700` (65/65 in 28.1s; 0 vulnerabilities), Phase 4B1V `35493604717`, and Phase 5A `35493604713`; local logs confirm reconciled v19 version `20260920060510` applied and `attendance_fk_index_v19_contract.sql` passed in both isolated database workflows. No new production mutation, data/RPC/RLS/grant/function/constraint, Science/Auth/frontend/Netlify change occurred during reconciliation. Final exact-head CI and PR review remain pending; Phase 6B has not started.

- **20 Sep 2026:** Phase 6A repository checkpoint merged and production-v19 preflight completed. PR #91 final exact head `d07a8eb8368c02cbcaed8ee11924d220935e8635` passed Phase 0A `35486880085`, Phase 0B `35486880087`, Playwright `35486880118` (65/65 in 25.9s; 0 vulnerabilities), Phase 5A `35486880089`, and Phase 4B1V `35486880091`, then merged as signed `main` `24cae653b2627182d2c2c3fc11cd0311687d005c`. The merged v19 candidate remains exactly six additive B-tree FK-covering indexes; no index removal or data/security/frontend/Science change is present. Fresh production preflight verified project `ACTIVE_HEALTHY`, live migration ledger still ends at `20260919044948 attendance_v18_structural_write_boundary`, all six target index names are absent, the same six unindexed-FK advisor findings remain, security-advisor baseline is unchanged, 15 active classes / 319 active pupils / 319 active enrolments remain, 3A remains 137 registers / 3,425 attendance records, and protected reporting remains February 398 / 0.9365 / 93.65% and Term 1 1,051 / 0.9342 / 93.42%. No production mutation occurred. Next possible step is a separately approved production application of v19 only.

- **20 Sep 2026:** Phase 6A repository/local implementation started from signed `main` `ef9aec8c0ca16d70c3a21527f50f2582a722b371` on `cleanup/phase-6a-fk-index-coverage`. Added repository candidate `20260920032000_attendance_v19_fk_index_coverage.sql` with exactly six additive B-tree FK-covering indexes, plus read-only `attendance_fk_index_v19_contract.sql`. Phase 0B now rejects any v19 scope beyond those six exact indexes; Phase 4B1V and Phase 5A reconstruct v19 and run the new verifier. `supabase/README.md` records candidate/rollback semantics. First validation head `9c61cc9cf4a138d48b269458e2cca6e6b94bb91e` passed Phase 0A `35486538597`, Phase 0B `35486538542`, Playwright `35486538536` (65/65 in 20.0s; 0 vulnerabilities), Phase 5A `35486538546`, and Phase 4B1V `35486538684`; local logs confirm v19 applied and `attendance_fk_index_v19_contract.sql` passed in both isolated database workflows. No production migration, row/data mutation, RPC/RLS/grant/function change, Science/Auth/frontend/Netlify change, or `production_migration_history.md` update occurred. Final exact-head CI and PR review remain pending.

- **20 Sep 2026:** Phase 6 performance/data-model impact map completed read-only from signed `main` `6949f0218a4806517bafc7299cc17a046d7a0932`. Performance Advisor reports exactly six unindexed Attendance/Attendance-private foreign keys and 25 unused indexes across the shared project; no index removal is proposed. Live enrolments are 319 current `ENROLLED` rows with no movement history yet, while v15 movement contracts show ended enrolments deliberately remain `active=true` so historical reporting is preserved. Gender is complete only for 3A (14 Male / 11 Female); 294 pupils in the other 14 active classes remain unset. Phase 6 is split into 6A FK index coverage, 6B enrolment lifecycle consistency, and 6C official-source gender completion. No migration, function, grant/RLS, frontend, Science, Netlify, Auth, or production-data mutation occurred during mapping.

- **20 Sep 2026:** Production Auth minimum-password alignment completed after separate explicit approval. The user confirmed applying exactly one Supabase Dashboard change: minimum password length **6→8**. Because the connected Supabase tools do not expose hosted Auth-config reads, the new minimum=8 is recorded as user-confirmed Dashboard evidence rather than independently MCP-read evidence. Fresh post-change read-only checks verified project `ACTIVE_HEALTHY`; live migration ledger still ends at `20260919044948 attendance_v18_structural_write_boundary`; 15 active classes; 319 active pupils / 319 active enrolments; 3A 137 registers / 3,425 attendance records; 2 confirmed Auth users; 3 Auth sessions; 1 active admin + 1 active teacher Attendance membership; and one active Science-teacher profile overlapping Attendance/Auth. Security Advisor findings are unchanged, including leaked-password protection disabled/unavailable on the Free plan. No SQL/migration/RPC/RLS/grant, Science data/object, SMTP/CAPTCHA, Netlify, or production Attendance-row mutation occurred. No real teacher credential was used for live sign-in verification; repository browser coverage remains the acceptance safety net for ordinary login, weak-password reset guidance, signup/recovery 8-character enforcement, recovery sign-out, and teacher-scope preservation.

- **20 Sep 2026:** Phase 5F2A1 corrective repository/browser patch merged. PR #87 approved exact head `6bd4ede1dfa7b8dbd4b8447f6adaf445d11625f4` merged as signed cleanup `main` `b82a952e96cdf0c731497d84d1bc98c915b17348`. Final exact-head gates passed: Phase 0A `35482593762`, Phase 0B `35482593841`, and Playwright `35482593766` with 65/65 Chromium tests in 25.5s and 0 npm vulnerabilities. The correction handles the pinned Supabase 2.114.0 successful weak-password shape, clears only the newly created local session, prevents Attendance app entry/RPCs, remains signed out after refresh, and preserves ordinary login. Fresh post-merge read-only production checks remain 15 active classes, 319 active pupils / enrolments, 3A 137 / 3,425, 2 confirmed Auth users, 3 active sessions, 1 admin + 1 teacher membership, and latest migration `20260919044948 attendance_v18_structural_write_boundary`. No hosted Auth, SQL/migration/RPC/RLS/grant, Science, SMTP/CAPTCHA, Netlify, or production-data mutation occurred from the merge.

- **20 Sep 2026:** Phase 5F2A1 initial corrective head `a892d00ef7eb13dc0f19cb1bcc614af23a5be85d` passed all applicable gates: Phase 0A `35482527596`, Phase 0B `35482527545`, and Playwright `35482527543` with 65/65 Chromium tests in 25.9s. CI logs explicitly show the ordinary teacher sign-in/refresh/sign-out path and corrected successful weak-password sign-in path both passed. The corrected path models exact Supabase 2.114.0 semantics (`data.session` + `data.weakPassword`, `error:null`), performs `signOut({scope:'local'})`, enters no Attendance RPC, remains on login with Forgot-password guidance, and stays signed out after refresh. The exact branch diff remains four files only; production Auth is still minimum 6 with no hosted Auth/SQL/Science/SMTP/CAPTCHA/Netlify mutation. A roadmap-only validation seal follows and requires the same exact-head gates before review readiness.

- **20 Sep 2026:** Phase 5F2A1 corrective repository/local patch started after production preflight found a pinned-client contract mismatch in the merged 5F2A login test. Exact `@supabase/supabase-js@2.114.0` source shows `signInWithPassword()` can successfully save the session, emit `SIGNED_IN`, and return `data.weakPassword` with `error:null` for an existing weak password. Attendance itself does not enter the app from `SIGNED_IN`, so there is no Auth-state race; the real gap is that the submit handler inspected only `error` and would have entered the app despite `data.weakPassword`. 5F2A1 changes login to clear only the new local session with `signOut({scope:'local'})`, preserve other shared-project sessions, keep the teacher on login with reset guidance, update the harness to emit real `SIGNED_IN` semantics, and replace the inaccurate error/no-session test. Production Auth remains minimum 6; no hosted Auth, SQL/migration/RPC/RLS/grant, Science, SMTP/CAPTCHA, or Netlify change has occurred.

- **20 Sep 2026:** Phase 5F2A repository/browser implementation merged. PR #85 approved exact head `51e146864b61f174e4c79802387757106bc3ab46` merged as signed `main` `3b8b5dbe817b3f4e29b7a895477b95b35f6598e7`. Final exact-head gates passed: Phase 0A `35481224295`, Phase 0B `35481224391`, and Playwright `35481224371` with 65/65 Chromium tests in 25.9s; CI logs showed all five new password-policy regressions passing individually. The merged runtime changes are limited to stable `weak_password` handling in `auth-session.js` / `teacher-access.js`; existing `minlength="8"` DOM contracts remain unchanged. No hosted Supabase Auth setting, SQL/migration/RPC/RLS/grant, Science object/data, SMTP/CAPTCHA setting, or Netlify production behavior changed. The last Dashboard-verified backend minimum remains 6; a 6→8 hosted change requires separate explicit production approval.

- **20 Sep 2026:** Phase 5F2A initial implementation head `bed9b95ff325645579354b6ab5e84e6e2382a5e5` passed all applicable first-pass gates: Phase 0A `35481160067`, Phase 0B `35481160066`, and Playwright `35481160065` with 65/65 Chromium tests in 19.8s. CI logs show all five new regressions passed individually: legacy weak-password sign-in recovery guidance, 7-character signup native blocking, signup server `weak_password`, 7-character recovery native blocking, and recovery server `weak_password` retention. Phase 5A Security Access Validation is not path-triggered because no Supabase baseline/migration/verifier file changed. Production Auth, migration ledger, Science, SMTP/CAPTCHA, and Netlify remain unchanged. A roadmap-only validation seal follows and must rerun the applicable exact-head gates before review readiness.

- **20 Sep 2026:** Phase 5F2A repository/local implementation started after explicit approval from signed `main` `eb7fd48b2826cdee36e9ffe74d1c5f51e62b948f` on branch `cleanup/phase-5f2a-password-policy-alignment`. User-provided Supabase Dashboard evidence resolved the hosted Auth baseline: email signup and confirmation enabled; secure password change/current-password requirement disabled; backend minimum password length 6 with no character-class requirement; custom SMTP disabled with 2 emails/hour; CAPTCHA disabled; production Site URL/redirects point to `srlumapas.netlify.app`; leaked-password protection remains unavailable on the Free plan. Repository scope keeps existing `minlength="8"`, adds stable `weak_password` handling for login/signup/recovery, and adds browser regressions for native 8-character enforcement and server rejection behavior. Production Auth remains unchanged at minimum 6; no SQL/migration/RPC/RLS/grant/Science/SMTP/CAPTCHA/Netlify mutation has occurred.

- **19 Sep 2026:** Phase 5F2 Auth impact-map checkpoint closed in the repository. PR #83 approved exact head `c8761dbd8f684a6eb9e697a45244285bd452294c` merged as signed `main` `ea1ea61698f679441d73be946d68d3933d240148`. Exact-head docs-only gates passed: Phase 0A `35429588825`, Phase 0B `35429588829`, and Playwright `35429588828` with 60/60 Chromium tests in 25.4s. The merged roadmap records actual Attendance Auth ownership/selectors/browser contracts, the shared Attendance/Science-teacher Auth impact, the Free-plan limitation for leaked-password protection, and the authoritative hosted Auth-config evidence gap. No Auth setting, frontend behavior, Science object/data, database schema, Netlify configuration, or production runtime changed. Phase 5F2 implementation remains blocked pending a read-only capture of hosted Auth/password/SMTP/reauth configuration.

- **19 Sep 2026:** Phase 5F2 Auth impact mapping completed read-only from signed `main` `2e086602b2662a0674c956a418a4b66d7f12ea2b`; no Auth/frontend/Science/database/runtime mutation occurred. Live project `SR Lumapas Science Dev` is on the Free plan with 2 confirmed email Auth users, 3 active sessions, 0 anonymous/SSO users, and 0 MFA factors; both Auth users are active Attendance members. Attendance Auth ownership is isolated to `supabase-client.js`, `auth-session.js`, and `teacher-access.js`; the 17-test Auth/access browser suite freezes signup, recovery priority, recovery sign-out, class-scope preservation, and shared-device behavior. Science students use separate PIN/token RPC auth, while one Science teacher Auth profile overlaps an Attendance member, making hosted Auth policy a shared Attendance/Science-teacher concern. Security Advisor continues to report leaked-password protection disabled; current Supabase docs state that feature requires Pro or above, so the Free project cannot treat it as an immediate 5F2 switch. The authoritative hosted password/SMTP/reauth configuration remains unavailable through the connected Supabase actions and is not represented by any repository `supabase/config.toml`; implementation is therefore blocked pending a read-only Dashboard/Management-API capture of `/v1/projects/{ref}/config/auth`.

- **19 Sep 2026:** Phase 5F1 repository/live reconciliation closed. PR #81 approved exact head `a05dc16b41a1439a3cf55bdd9cd6b34d1470f882` merged as signed `main` `8fd4e13a2a4b325033a1f66a139d2154310a7fdc`. Exact-head gates passed: Phase 0A `35425596677`, Phase 0B `35425596782`, Playwright `35425596628` with 60/60 Chromium tests in 18.4s, Phase 4B1V Local DB Validation `35425596619`, and Phase 5A Security Access Validation `35425596630`. The migration was a pure filename reconciliation from `20260919050000...` to live `20260919044948...` with zero SQL-byte changes; Git blob remained `5182f1dff7a993151e81a331188aa692dfc86fb3`. Both isolated DB workflows explicitly applied `20260919044948` and passed the v18 structural-write contract; Phase 4B1V also passed protected reporting population, shared-engine equivalence, public-API, movement, and teacher-role verifiers. Production v18 was not reapplied. No frontend/DOM/Auth/Science/Netlify or production-data change occurred in PR #81.

- **19 Sep 2026:** Phase 5F1 v18 was explicitly approved for production and applied once through the Supabase migration mechanism. Live ledger recorded **`20260919044948 attendance_v18_structural_write_boundary`** from reviewed SQL blob `5182f1dff7a993151e81a331188aa692dfc86fb3`. Post-application verification confirmed authenticated SELECT remains on all six structural tables while authenticated INSERT/UPDATE/DELETE is removed everywhere; a schema-wide check found zero authenticated direct-write Attendance tables. Service-role CRUD, RLS, structural policy/trigger fingerprints, FKs/constraints, RPC inventory, and frontend ownership remained unchanged. A rollback-scoped authenticated DML probe was rejected. Protected history remained 137 / 3,425 / 3,425 with zero correction/audit-update/delete drift; 3A February remained `398/425 = 0.9365 = 93.65%` with 17 completed registers and Term 1 remained `1051/1125 = 0.9342 = 93.42%` with 45 completed registers. Reporting population/shared-engine verifiers returned zero mismatches. Security Advisor and Science scope remained unchanged. Repository/live version reconciliation is required because the reviewed source was `20260919050000...` while Supabase recorded live `20260919044948...`; exact SQL bytes remain unchanged.

- **19 Sep 2026:** Phase 5F1 repository/local checkpoint closed. PR #79 approved exact head `20d5c62095d104882162b75428ae2981ae708592` merged as signed `main` `9d362c9c5a0cae0f3c4b534fd8a3372af838c52d`. Final exact-head gates passed: Phase 0A `35419452432`, Phase 0B `35419452412`, Playwright `35419452692` with 60/60 Chromium tests in 24.4s, Phase 4B1V Local DB Validation `35419452454`, and Phase 5A Security Access Validation `35419452437`. Both isolated Supabase workflows applied candidate v18 `20260919050000`; Phase 5A passed the core contract, updated security contract, v15 movement, v17 teacher-role, and v18 structural-write verifier, while Phase 4B1V additionally passed protected reporting population, shared-engine equivalence, and public-API verifiers. Candidate v18 remains grant-only with Git blob `5182f1dff7a993151e81a331188aa692dfc86fb3`. Production remains unchanged on v17 with no live v18 entry; no Auth/Science/frontend/DOM/Netlify/FK/function/data mutation occurred from the merge itself.

- **19 Sep 2026:** Phase 5F1 initial repository/local head `24a74a1253d1078a2e1cf76a2678e5823464fb1e` passed all five required gates: Phase 0A `35419249575`, Phase 0B `35419249610`, Playwright `35419249677` with 60/60 Chromium tests in 25.1s, Phase 4B1V `35419249618`, and Phase 5A `35419249599`. Both isolated Supabase stacks applied candidate v18 `20260919050000`; Phase 5A passed `attendance_contract.sql`, the updated `attendance_security_access_contract.sql`, v15 movement, v17 teacher-role, and new v18 structural-write contracts. Phase 4B1V additionally passed protected population fixtures, v13 shared-engine equivalence, and public reporting API checks. Candidate v18 remained grant-only; production remained on v17 with no live v18 mutation. This roadmap-only validation seal creates a new exact PR head requiring the same five gates before merge review.

- **19 Sep 2026:** Phase 5F1 repository/local implementation started after explicit approval from exact signed `main` `21fc5c4325db2f11002121f057b2f94ed8ddffee`. Impact mapping confirmed the cleanup frontend has zero direct table writers and no structural-admin screen, while live authenticated school-admin grants still permit direct DML on the six structural tables. The highest concrete risk is the 3A class cascade: direct deletion can reach 25 enrolments, 137 registers, and all 3,425 attendance records; calendar/term writes can alter protected reporting denominators/periods. Branch `cleanup/phase-5f1-structural-write-boundary` adds grant-only candidate v18 `20260919050000_attendance_v18_structural_write_boundary.sql`, updates the shared security contract to expect zero authenticated direct Attendance writes, adds rollback-only `attendance_structural_write_v18_contract.sql`, and extends Phase 0B/4B1V/5A reconstruction gates. No production Supabase/Auth/Science/frontend/Netlify/data mutation has occurred; production remains on v17. Exact-head validation remains required before merge review.

- **19 Sep 2026:** Phase 5E repository/live reconciliation closed. PR #77 approved exact head `4faff2a846b374d0ab5bc04582d3e6644ea6b393` merged as signed `main` `27162895073795f3af5b1f9d7be53ee3a66a4d12`. Exact-head gates passed: Phase 0A `35414214134`, Phase 0B `35414214095`, Playwright `35414214094` with 60/60 Chromium tests in 24.0s, Phase 4B1V Local DB Validation `35414214111`, and Phase 5A Security Access Validation `35414214112`. The migration was a pure filename reconciliation from `20260919013000...` to live `20260919015041...` with zero SQL-byte changes; Git blob remained `9f127e3615fae3e8a508b636435e6035d9fea560`. Both isolated DB workflows explicitly applied `20260919015041` and passed the v17 role contract; Phase 4B1V also passed the protected reporting population, shared-engine equivalence, and public-API verifiers. Production v17 was not reapplied. No frontend/DOM/Auth/Science/Netlify or production-data change occurred in PR #77.

- **19 Sep 2026:** Phase 5E v17 was explicitly approved for production and applied once through the Supabase migration mechanism. Live ledger recorded **`20260919015041 attendance_v17_teacher_role_persistence`** from reviewed SQL blob `9f127e3615fae3e8a508b636435e6035d9fea560`. Post-application verification confirmed the role constraint now accepts `teacher`, `viewer`, `class_teacher`, and `assistant_teacher`; exactly one deterministic historical assignment changed from generic `teacher` to `class_teacher`, leaving final roles 1 `teacher` + 1 `class_teacher` and zero remaining/ambiguous backfill candidates. School memberships, authenticated SELECT-only teacher-table access, service-role CRUD, RLS/policy/trigger counts, teacher signup/audit aggregates, and the 137 / 3,425 / 3,425 attendance-history baseline remained unchanged. Protected reporting population/shared-engine verifiers returned zero mismatches, and rollback-scoped production-safe checks confirmed class access, teacher-status `class_teacher`, and bootstrap behavior. The synthetic/local v17 verifier is not valid verbatim on production because it uses synthetic fixture IDs; its attempted live transaction failed safely on a missing synthetic FK before any committed write, after which equivalent production-safe verification was used. Security Advisor showed no new v17-specific anonymous Attendance exposure. Repository/live migration-version reconciliation is required because the reviewed repository candidate was `20260919013000...` while Supabase recorded live `20260919015041...`; exact SQL bytes remain unchanged.

- **19 Sep 2026:** Phase 5E repository/local checkpoint closed. PR #75 approved exact head `9fdde375fa4418677c349eeebd5d27946c6f56dd` merged as signed `main` `87c6d3737c4c64ac957219965f8aac27c87100c8`. Final exact-head gates passed: Phase 0A `35412909543`, Phase 0B `35412909556`, Playwright `35412909548` with 60/60 Chromium tests in 24.3s, Phase 4B1V Local DB Validation `35412909642`, and Phase 5A Security Access Validation `35412909636`. Both isolated Supabase workflows applied candidate v17 locally and passed the new teacher-role contract; Phase 4B1V also passed protected reporting population/equivalence/public-API checks. Production remains on v16 `20260919005727 attendance_v16_teacher_management_write_boundary`; v17 has not been applied. No frontend/DOM/Auth/Science/Netlify or production-data change occurred from the repository merge itself.

- **19 Sep 2026:** Phase 5E initial implementation head `96a1546c84cb0b22f52f1e73a22d903c0c56d692` passed all five required repository/local gates: Phase 0A `35412667600`, Phase 0B `35412667641`, Playwright `35412667658` with 60/60 Chromium tests in 24.6s, Phase 4B1V Local DB Validation `35412667833`, and Phase 5A Security Access Validation `35412667624`. Both isolated Supabase workflows applied local migration `20260919013000`; Phase 5A passed `attendance_contract.sql`, `attendance_security_access_contract.sql`, the v15 movement contract, and `attendance_teacher_role_v17_contract.sql`; Phase 4B1V additionally passed protected reporting population/equivalence/public-API verifiers. Production remained on v16 with 2 generic-teacher assignments and no live v17 entry. This roadmap-only seal now requires a fresh exact-head gate run before merge review.

- **19 Sep 2026:** Phase 5E repository/local implementation started after explicit approval from exact signed `main` `18c6cbedb26fc3394596c301dc8f25da9eeda562`. Fresh impact mapping confirmed signup/gate/admin UI already preserves the requested/approved `class_teacher` / `assistant_teacher` value; the current approval RPC alone collapses class assignments to generic `teacher`. Live production remains on v16 and has 2 active generic-teacher assignments: one unmatched admin assignment and one non-admin assignment with exactly one approved `class_teacher` request. Candidate v17 `20260919013000_attendance_v17_teacher_role_persistence.sql` expands the class-assignment role domain, persists validated assignment type in the existing approval RPC, and includes a conservative exact user/class backfill only for unambiguous approved types. New rollback-only `attendance_teacher_role_v17_contract.sql` proves both teacher types, legacy teacher/viewer compatibility, unchanged class access, retained v16 DML boundary, status/bootstrap reads, audit, and enable/disable role preservation. No production Supabase/Auth/Science/frontend/Netlify mutation has occurred.

- **19 Sep 2026:** Phase 5D repository/live reconciliation closed. PR #73 approved exact head `81f9043f584a6c199f9e2fdf34e2d7a373b28241` merged as signed `main` `3816f87f32663eb3f4ef9edd8155038d70bae1ce`. Exact-head gates passed: Phase 0A `35411258071`, Phase 0B `35411258005`, Playwright `35411257964` with 60/60 Chromium tests in 21.1s, Phase 4B1V Local DB Validation `35411258059`, and Phase 5A Security Access Validation `35411258024`. The migration was a pure filename reconciliation from `20260919090000...` to live version `20260919005727...` with zero SQL-byte changes; Git blob remained `c9437b07c07b0c91d5e1470c63b78dadced4d4ba`. V16 was not reapplied. No frontend/DOM/Auth/Science/Netlify or production-data change occurred in PR #73.

- **19 Sep 2026:** Phase 5D v16 was explicitly approved for production and applied once through the Supabase migration mechanism. Live migration ledger recorded **`20260919005727 attendance_v16_teacher_management_write_boundary`**. Post-application verification confirmed authenticated access to `teacher_school_memberships` and `teacher_class_assignments` is SELECT-only while service-role CRUD, RLS, all 8 policies, both triggers, coordinated teacher-admin RPC bodies/ACLs, teacher/access aggregates, attendance history, and all protected reporting fixtures remain unchanged. Security Advisor showed no new anonymous Attendance exposure. No rollback, frontend/Auth/Science/Netlify change, or production-data mutation was required. Repository/live migration-version reconciliation is required because the reviewed source was originally named `20260919090000...`; exact SQL bytes remain unchanged.

- **19 Sep 2026:** Phase 5D repository/local implementation closed. PR #71 final approved head `37040973da36a88f83cfb27551547758c46c84d1` merged as signed `main` `44aa4e9ecb46273dc30df5a0aac0d94e9c303448`. Exact-head gates passed: Phase 0A `35401645755`, Phase 0B `35401645844`, Playwright `35401645750` with 60/60 Chromium tests in 23.8s, Phase 4B1V Local DB Validation `35401645871`, and Phase 5A Security Access Validation `35401645765`. The merged v16 candidate remains grant-only and repository/local: no function body, frontend, DOM, Auth, Science, Netlify, or production-data change. Fresh post-merge live verification confirms production still ends at v15 `20260918144321 attendance_v15_student_movement_write_boundary`; there is no v16 migration entry, and authenticated direct INSERT/UPDATE/DELETE on both teacher-management tables remains present until a separately approved production application.


- **19 Sep 2026:** Phase 5D initial validated implementation head `365beae2e69960f8880e5e8a5b77a6905843d779` passed all five required repository/local gates: Phase 0A `35401407800`, Phase 0B `35401407786`, Playwright `35401407777` with 60/60 Chromium tests in 26.6s, Phase 4B1V Local DB Validation `35401407798`, and Phase 5A Security Access Validation `35401407791`. Both isolated DB workflows applied local migration `20260919090000`; `attendance_contract.sql`, protected reporting population/equivalence/public-API checks, `attendance_security_access_contract.sql`, the v15 movement verifier, and the new `attendance_teacher_management_v16_contract.sql` all passed. Earlier CI exposed two verifier-only fixture/privilege assumptions (direct authenticated signup-request inspection and dependence on Phase 5A identities in Phase 4B1V); both were corrected without changing v16 runtime SQL or weakening assertions. This roadmap-only seal creates a new PR head requiring the same five exact-head gates before merge approval. Production remains on v15 and no live mutation occurred.


- **19 Sep 2026:** Phase 5D repository/local implementation started from exact signed `main` `153f2d4830cba7750956ba020e3a99709d0f2722` after explicit approval. Fresh impact mapping confirmed `teacher_school_memberships` and `teacher_class_assignments` still expose authenticated INSERT/UPDATE/DELETE while the cleanup frontend remains RPC-only. The only public writers are existing authenticated-only `SECURITY DEFINER` RPCs `attendance_admin_review_teacher_request` and `attendance_admin_set_teacher_active`; their live bodies match repository source, so candidate v16 `20260919090000_attendance_v16_teacher_management_write_boundary.sql` is grant-only and does not rewrite functions. The rollback-only `attendance_teacher_management_v16_contract.sql` proves direct admin DML blocking, non-admin rejection, authorized approval, coordinated disable/enable, private audit semantics, generic-`teacher` Phase 5E compatibility, teacher status, and invoker bootstrap reads. No production Supabase/Auth/Science/data/frontend/Netlify mutation has occurred; production remains on v15.

- **18 Sep 2026:** Phase 5C repository/live reconciliation PR #70 merged approved head `26211e9fac2a6bf8917c8410f93f6bd557cd5fb0` as signed `main` `153f2d4830cba7750956ba020e3a99709d0f2722`. The reconciliation preserved exact v15 SQL blob `b3c1c2b904558d1d0fb867f5e71feb2506ceec76`, aligned the repository migration version to live `20260918144321`, and passed Phase 0A `35359186781`, Phase 0B `35359186820`, Playwright `35359186772` (60/60 in 27.0s), Phase 4B1V `35359186793`, and Phase 5A `35359186767`. Post-merge production verification showed exactly one v15 ledger entry; v15 was not reapplied.


- **18 Sep 2026:** Phase 5C v15 was explicitly approved for production and applied once through the Supabase migration mechanism. Live migration ledger recorded **`20260918144321 attendance_v15_student_movement_write_boundary`**. Post-application verification confirmed all three movement RPCs are controlled `SECURITY DEFINER` functions with fixed empty `search_path`, authenticated/service-role EXECUTE retained, and no `anon`/`PUBLIC` EXECUTE; authenticated access to `students`, `enrolments`, and `student_movements` is now SELECT-only while service-role privileges, RLS, policies, and triggers remain unchanged. Rollback-only live checks passed for blocked direct admin DML, teacher/outsider rejection, and authorized Transfer In / Transfer Out / Move Class. Production data remained exactly 319 active pupils, 319 current enrolments, 137 registers, 3,425 attendance rows, 3,425 audit rows, zero movement rows, zero corrections, and zero audit UPDATE/DELETE rows. All four protected fixtures remained exact: Feb Whole Class 398/425 = 0.9365 = 93.65%; Feb Non-SEN 392/408 = 0.9608 = 96.08%; Term 1 Whole Class 1,051/1,125 = 0.9342 = 93.42%; Term 1 Non-SEN 1,036/1,080 = 0.9593 = 95.93%. Security Advisor changed only by the expected three authenticated-`SECURITY DEFINER` warnings for the movement RPCs, with no new anonymous Attendance exposure. No rollback, Auth/Science/frontend/Netlify deployment, or production data mutation was required. Repository/live migration-version reconciliation is required because the reviewed repository source was originally named `20260918131500...` while Supabase recorded live version `20260918144321`; the SQL blob remains exactly `b3c1c2b904558d1d0fb867f5e71feb2506ceec76`.


- **18 Sep 2026:** Phase 5C repository/local implementation merged via PR #68. Final exact head `a2b8da0f4e7eaf49cbc74194e1bbff2d85a851b0` retained all five gates green — Phase 0A `35353960786`, Phase 0B `35353960794`, Playwright `35353960781` (60/60 in 26.2s), Phase 4B1V `35353960789`, and Phase 5A `35353960785` — and merged as signed `main` `73ea8383ebb95872eafa92a4932b176987ba1ab0`. Immediate post-merge compare showed `main` identical to the merge commit. Read-only live verification still showed latest migration `20260918094529 attendance_v14_register_write_boundary`, all three movement RPCs invoker-mode, and the pre-v15 authenticated movement-table write grants still present. No Supabase/Auth/Science/data/frontend/Netlify/deployment mutation occurred; v15 production application remains a separate approval gate.


- **18 Sep 2026:** Phase 5C initial implementation head `e9b3a46482977a578d6fadee8adabc76dceebc6d` passed all five required repository/local gates: Phase 0A `35353485677`, Phase 0B `35353485968`, Playwright `35353485606` with 60/60 Chromium tests in 24.5s, Phase 4B1V Local DB Validation `35353485905`, and Phase 5A Security Access Validation `35353485701`. Both isolated DB workflows applied local migration `20260918131500`; `attendance_contract.sql`, all protected reporting population/equivalence/public-API checks, `attendance_security_access_contract.sql`, and `attendance_student_movement_v15_contract.sql` all passed. This roadmap-only seal creates a new PR head requiring the same five exact-head gates before merge approval. Production remains on v14 and no live mutation occurred.


- **18 Sep 2026:** Phase 5C repository/local implementation started from exact signed `main` `a5aa82fc51703ef352caa5b694bab42700c1ba5c` after explicit approval. Fresh read-only live impact mapping confirmed the three movement RPCs were still invoker-mode, authenticated direct DML remained on `students` / `enrolments` / `student_movements`, production held 319 students / 319 current enrolments / 0 movement rows, and 25 attendance-bearing enrolments lacked movement-FK protection. Candidate v15 `20260918131500_attendance_v15_student_movement_write_boundary.sql` and rollback-only movement verification are repository/local only; no Supabase/Auth/Science/data/frontend/Netlify/deployment mutation has occurred and production remains on v14.


- **18 Sep 2026:** Phase 5B repository/live migration-ledger reconciliation completed. PR #66 approved exact head `49ea709896dfea3b53521cd3f6cdfe367568d7d0` retained all five gates green — Phase 0A `35343322044`, Phase 0B `35343322031`, Playwright `35343322023` (60/60 Chromium tests in 22.5s), Phase 4B1V Local DB Validation `35343322098`, and Phase 5A Security Access Validation `35343322026` — and was merged with the exact-head guard as `a344514a7a7f99175a74e8f718fb64c898a93bb1`. Post-merge compare confirmed `main` is identical to that merge commit and contains exactly the approved seven-file reconciliation diff, including the zero-content-change v14 migration rename. No Supabase/Auth/Science/data/frontend/Netlify/deployment mutation occurred during reconciliation; Phase 5C remains unstarted. Closure read-only catalog recheck confirms 21 Attendance RPCs = 15 `SECURITY DEFINER` + 6 invoker, `attendance_save_register` is definer, and exactly 11 Attendance tables retain direct authenticated write grants; these figures replace the pre-v14 impact-map counts.

- **18 Sep 2026:** Phase 5B production-reconciliation PR #66 initial exact head `ed094fc1230d9fe4a5a469f048f2abb0a7867ebf` passed all five required gates: Phase 0A `35336713850`, Phase 0B `35336713833`, Playwright `35336713824` (60/60 Chromium tests in 25.9s), Phase 4B1V Local DB Validation `35336713871`, and Phase 5A Security Access Validation `35336713874`. Full seven-file diff review found only the intended workflow/reference metadata changes plus a zero-content-change migration rename; there were no PR comments, reviews, or threads. This roadmap-only seal update creates a new PR head, so the same five gates remain mandatory on that current head before merge approval. No Supabase/Auth/Science/data/frontend/Netlify/deployment mutation was made.

- **18 Sep 2026:** Phase 5B production-reconciliation source checkpoint implemented from signed `main` `74d1858f84a5ba88504a42dd90320baabee26b53`. Repository v14 is renamed from `20260918082500_attendance_v14_register_write_boundary.sql` to live ledger version `20260918094529_attendance_v14_register_write_boundary.sql` with the exact same SQL blob `7659c0f3595237bbc8e7d6522cf83a28d6e19e49`; Phase 0B/Phase 4B1V/Phase 5A workflow references, backend README, sanitized production migration history, and current roadmap state are reconciled. This checkpoint is source-only: it does not apply/reapply v14, mutate Supabase/Auth/Science/data, change frontend runtime/DOM, or deploy v1.0. Exact-head five-gate verification and explicit merge approval remain required; Phase 5C is not started.

- **18 Sep 2026:** Phase 5B v14 Attendance Register Write Boundary applied to production after explicit approval and fully verified. Exact repository source blob `7659c0f3595237bbc8e7d6522cf83a28d6e19e49` was applied once; Supabase recorded `20260918094529 attendance_v14_register_write_boundary`. Post-apply checks confirmed identical save-register body MD5 `db200d65b686af25c306aba8429ef471`, intended definer/EXECUTE boundary, authenticated SELECT-only on the two register tables, unchanged service-role CRUD/RLS policies/triggers, expected RPC inventory 15 definer / 6 invoker, expected new Security Advisor warning only, synthetic unauthorized rejection, authorized no-change save success, unchanged 137 / 3,425 / 3,425 data-audit fingerprint with zero corrections, and all four protected reporting fixtures exact. No rollback was required. The live ledger version differs from repository filename `20260918082500`, so a source-only reconciliation checkpoint is required before Phase 5C; v14 must not be reapplied.

- **18 Sep 2026:** Phase 5B production-application verification planning completed from signed `main` `e9f2006d0076629911bc2f9ff8ba8fce883ee57a` with no production mutation. Repository v14 blob `7659c0f3595237bbc8e7d6522cf83a28d6e19e49` was compared against fresh live pre-v14 state: migration ledger still ends at v13; save-register remains invoker-mode with body MD5 `db200d65b686af25c306aba8429ef471`; authenticated/service-role still have full CRUD on both register tables; 137 registers / 3,425 records / 3,425 audit rows and zero corrections remain unchanged; all four Whole-Class/Non-SEN reporting fixtures match exactly. The production plan now freezes pre/post structural, privilege, advisor, non-mutating authorized/unauthorized RPC, reporting, migration-ledger, rollback, and failure-stop checks. Production v14 application still requires separate explicit approval; Phase 5C remains unstarted.

- **18 Sep 2026:** Phase 5B Attendance Register Write Boundary repository/local checkpoint completed and merged via PR #62. Approved exact head `b99574daa86137573b1d633c84dada2e08599d79` passed Phase 0A `35326473184`, Phase 0B `35326473139`, Playwright `35326473157` (60/60 in 24.2s), Phase 4B1V Local DB Validation `35326473156`, and Phase 5A Security Access Validation `35326473148`. It merged as signed cleanup `main` `d74ee1d7d213e47d6b85a83dfe77ae0b96d7d2ea` with tree `452e8a0efaf4e022396181bb05c6e2fd937cc42d`. Repository v14 now defines the controlled register-write boundary, but no production Supabase/Auth, frontend runtime, Science, production data, Netlify, or deployment mutation occurred. Production v14 application remains a separate explicit approval checkpoint; Phase 5C has not started.

- **18 Sep 2026:** Phase 5B pre-closure candidate `250ab363ef1b987fb7eae89e9c8c508093707fa6` passed Phase 0A `35325022916`, Phase 0B `35325022917`, Playwright `35325022914` (60/60 in 18.1s), Phase 4B1V Local DB Validation `35325022940`, and Phase 5A Security Access Validation `35325022938`. The current PR head must repeat the five-gate seal before merge; the eventual merged SHA will be recorded in a separate closure checkpoint. No production application has occurred.

- **18 Sep 2026:** Phase 5B one-commit normalized candidate `6728b0b3819db92e22348f98ad627215d7ee0f66` passed Phase 0A `35324571464`, Phase 0B `35324571404`, Playwright `35324571532` (60/60 in 24.2s), Phase 4B1V Local DB Validation `35324571381`, and Phase 5A Security Access Validation `35324571460`. Fresh baseline+v13+v14 reconstructions kept all reporting fixtures/public APIs green and re-proved the hardened direct-DML/RPC/correction/audit boundary. Final documented-head normalization and exact-head CI remain required before merge approval.

- **18 Sep 2026:** Phase 5B pre-normalization repository/local implementation completed on PR #62. Exact head `7fc9acd41f3e54936197b2cde2b8ccf79701fe0d` passed Phase 0A `35324147199`, Phase 0B `35324147147`, Playwright `35324147163` (60/60 in 25.3s), Phase 4B1V Local DB Validation `35324147158`, and Phase 5A Security Access Validation `35324147240`. Both fresh local stacks applied baseline + v13 + proposed v14; reporting equivalence remained green and the hardened security verifier proved direct register/record DML blocked while the controlled save/correction/audit path remained intact. Final one-commit normalization and exact normalized-head CI remain mandatory before merge approval.

- **18 Sep 2026:** Phase 5B repository/local implementation started from signed `main` `2bc514be9bdc5cd3d0b0c52466e35cec40768d1a` on branch `cleanup/phase-5b-register-write-boundary`. Proposed v14 repository migration changes only `attendance_save_register` to `SECURITY DEFINER` and revokes authenticated INSERT/UPDATE/DELETE on `daily_registers` / `attendance_records`. Backend/security verifiers and isolated local workflows are being evolved to prove direct DML is blocked while first-save/no-change/correction, audit metadata, reporting equivalence, and live-v0.7 RPC compatibility remain intact. No production application, frontend runtime, Science, data, Netlify, or deployment change has occurred.

- **18 Sep 2026:** Phase 5B Attendance Register Write Boundary impact mapping completed from signed `main` `9955c96ab07b22e6939f61f4a14eb5de73c45eef`. Fresh repository/live evidence confirms `attendance_save_register` is the sole Attendance writer of `daily_registers` / `attendance_records`, remains invoker-mode, and already contains explicit authentication/class/date/roster/record/correction safeguards. Both tables retain authenticated full CRUD under class-scoped RLS. The smallest proposed hardening is an unchanged-body `ALTER FUNCTION ... SECURITY DEFINER` plus revoking authenticated INSERT/UPDATE/DELETE on only those two tables while retaining SELECT, RLS policies, triggers, service-role privileges, and the public RPC contract. A new residual risk was mapped: parent admin DELETE privileges can still cascade into attendance history through academic-year/class/student/enrolment relationships, so 5B closes direct register/record DML but not full history immutability. Live production has no correction events (137 registers; 3,425 audit INSERTs; zero corrected registers), so correction-update semantics must be proved synthetically/local before any production application. No migration or live mutation was made.

- **18 Sep 2026:** Phase 5A Security Contract Safety Net completed and merged via PR #59. Approved exact final head `68174078a86d0f2098e7d6f1dda9bea00d0d4b32` passed Phase 0A `35315054747`, Phase 0B `35315054722`, Playwright `35315054721` (60/60 in 26.4s), Phase 4B1V Local DB Validation `35315054737`, and Phase 5A Security Access Validation `35315054732`. It merged as signed cleanup `main` `16425a337a000c9db4db8a63c7c2ca21902a7ead` with tree `93860907833a5f277ecd39cda788193aa7378375`. The checkpoint added repository/local security evidence only; no production Supabase/Auth, frontend runtime, Science, data, Netlify, or deployment mutation occurred. Phase 5B remains unstarted and requires an impact map before any migration.

- **18 Sep 2026:** Phase 5A one-commit normalized candidate `b3093353c55e86e3f8641f2de15f73ade0bc01d3` passed Phase 0A `35314716791`, Phase 0B `35314716814`, Playwright `35314716843` (60/60 in 19.5s), Phase 4B1V Local DB Validation `35314716784`, and Phase 5A Security Access Validation `35314716803`. The existing reporting reconstruction remained green and the new role-level security verifier passed. Final documented-head normalization and exact-head CI remain required before merge approval.

- **18 Sep 2026:** Phase 5A pre-normalization implementation completed on PR #59. Exact head `c862a8615fb0a4ef7e8d5b4bff367350a34f47a5` passed Phase 0A `35314340711`, Phase 0B `35314340857`, Playwright `35314340982` (60/60 in 27.4s), Phase 4B1V Local DB Validation `35314340988`, and Phase 5A Security Access Validation `35314340873`. The new local security gate passed synthetic anon/outsider/teacher/admin authorization, EXECUTE boundaries, direct-DML baseline exposure, controlled register/Transfer In dependencies, private-audit isolation, and SECURITY DEFINER source/config checks. Final one-commit normalization and exact normalized-head CI remain required before merge approval.

- **18 Sep 2026:** Phase 5A Security Contract Safety Net started from signed `main` `289fe1c05fe2ce11e70000da50ba575bcdd8aff5` on branch `cleanup/phase-5a-security-contract-safety-net`. Scope is repository/local validation only: a synthetic role fixture, a security/access verifier, a dedicated isolated Supabase workflow, Phase 0B source-contract inclusion, and roadmap bookkeeping. The planned verifier covers anonymous/unauthorized/assigned-teacher/admin role behavior, RPC EXECUTE boundaries, `SECURITY DEFINER` search-path/auth markers, current direct-DML exposure, controlled register/movement write-path dependencies, and private-audit isolation. No live migration, grant/RLS/Auth, frontend runtime, Science, data, Netlify, or deployment mutation is included.

- **18 Sep 2026:** Phase 5 security/access impact mapping completed from signed `main` `8a43724dd115ec4322bfb9457ad16a3032577c25`. Repository review confirmed the cleanup frontend remains RPC-only with a publishable key and no direct table calls. Fresh production catalog review found 21 public Attendance RPCs (14 `SECURITY DEFINER`, 7 invoker), 16/16 Attendance tables with RLS and 50 policies, and authenticated direct writes on 13 Attendance tables. The four main controlled write RPCs for register saving/student movement remain invoker-mode, so direct-DML revocation must be staged behind equivalent controlled privileged write boundaries. Private audit tables remain outside client access. Live role data confirms active class assignments still collapse to generic `teacher`. Security Advisor reports leaked-password protection disabled; the organization is on the Free plan, so that feature is unavailable under the current plan. No live/database/Auth/Science/frontend/deployment mutation was made. Phase 5A is proposed as a repository/local security safety-net checkpoint only and remains unstarted pending explicit approval.

- **18 Sep 2026:** Phase 4 reporting consolidation closed through Phase 4B2. PR #55 repository/live migration-ledger reconciliation merged approved head `52d1a4afaba830eb04ef40de99a0a2ba7c6791fb` as signed `main` `61e7f9db8c3250147f589f700bd6d47e7131568a` (tree `dc0479a4429e1f39c891753fa3486156608d19fc`) after Phase 0A `35206715686`, Phase 0B `35206715458`, Playwright `35206715593` (53/53), and Phase 4B1V Local DB Validation `35206715525` passed. PR #56 then merged approved head `4877ee7d08be7a1b7c2266eeb76224e74c97099a` as signed `main` `209f5d2945988f014120ce8a81e327ab3a732140` (tree `5c26a8706c81120bf298c02f2bb26ad8ec51b4c0`) after Phase 0A `35244060983`, Phase 0B `35244060475`, and Playwright `35244060724` with 60/60 Chromium tests in 25.2s. Cleanup v1.0 now exposes explicit Whole-Class/Non-SEN population selection while preserving all protected fixtures and missing-register semantics. The live v0.7 frontend remains unchanged; Phase 5 has not started.

- **17 Sep 2026:** Phase 4B1P production-reconciliation checkpoint opened as draft PR #55 on `cleanup/phase-4b1p-production-reconciliation` from exact signed `main` `40521ab9cb4383d6454f288c54b890c5edc1528d`. The repository v13 migration is renamed from `20260917070000` to live ledger version `20260917090547` with the identical SQL blob `a6c1e658753849c17e9f85baf6cb8640e15799c3`; Phase 0B/Phase 4B1V workflow references, sanitized migration history, backend README, and canonical roadmap are reconciled. This PR is repository-only and does not apply/reapply v13, mutate production data/schema, change Science/Auth/Netlify, change frontend runtime/DOM, or deploy cleanup v1.0. Exact-final-head gates and explicit merge approval remain mandatory.
- **17 Sep 2026:** After separate explicit user approval, the exact reviewed v13 shared-reporting migration was applied to production Supabase. Supabase recorded `20260917090547 attendance_v13_shared_reporting_model`. Post-application verification confirmed all seven expected reporting functions/grants, intended invoker/definer security boundaries, exact legacy Whole-Class response compatibility, all four protected 3A Whole-Class/Non-SEN fixtures, and all 15 Dashboard classes. Science objects and the protected live v0.7 frontend were not changed. Security-advisor changes were limited to the expected three authenticated `SECURITY DEFINER` warnings for the new v2 APIs; broader security/performance findings remain deferred to Phase 5/6.
- **17 Sep 2026:** Phase 4B1V PR #53 merged from approved exact head `f49ef79f9411f0b04167ff66c702d1a9c8748463` as signed cleanup `main` `40521ab9cb4383d6454f288c54b890c5edc1528d` (tree `5b90cd85f311afe1717d3b83e25a5c961c3bfac6`). Exact-head gates were Phase 4B1V Local DB Validation `35179695632`, Phase 0A `35179695598`, Phase 0B `35179695592`, and Playwright `35179695604` with 53/53 Chromium tests in 23.2s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The isolated stack applied the sanitized reconstructed baseline plus v13 and passed the catalog, population-fixture, shared-engine, and public legacy/v2 API verifiers. The PR merge itself did not mutate production.
- **17 Sep 2026:** Phase 4B1 PR #50 merged at exact approved head `c5483dbc366978846d6532f9bb273c041bb0b811` as signed cleanup `main` `ecea2fc99a79c4af07d3b70bca3dcb240c57aacc` (tree `577182114b230758a6693f1c3c3ff092e6b08119`). Exact-head gates were Phase 0A `35161452577`, Phase 0B `35161452452`, and Playwright `35161452255` with 53/53 Chromium tests in 22.7s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The merge records repository v13/shared-reporting source only; live v0.7 and live Supabase remain unchanged on `20260906125521 attendance_v12_term_ytd_reporting`. Fresh branch inventory found no hosted Supabase development branches, and repository inspection found no `supabase/config.toml` or database-execution CI workflow, so Phase 4B1V is defined as a separate isolated local-Supabase execution-validation checkpoint before any production application or Phase 4B2.
- **17 Sep 2026:** Phase 4A PR #49 merged as signed cleanup `main` `c205249763ca5a1d81affe32ff90ec8ed8d865f2`, preserving live v0.7 and live Supabase unchanged. Phase 4B impact mapping from that exact base confirmed Monthly/Term-YTD SQL duplication plus the Dashboard latest-day duplicate count path. Current Supabase Data API documentation was rechecked and the initial overload idea was rejected in favor of unique `*_v2` public names. User approved Phase 4B1 only. Branch `cleanup/phase-4b1-shared-reporting-engine` and draft PR #50 were created with repository-only v13 migration source: one internal invoker-mode shared calculation engine, three population-aware v2 RPCs, and the three legacy RPCs retained as Whole-Class compatibility wrappers. The Phase 0B gate, backend catalog verifier, reconstruction README, and a new read-only shared-engine fixture verifier were evolved without frontend/Science/Netlify/live-data changes. A fresh live read-only comparison found zero Dashboard Whole-Class attendance-count mismatches across saved 2026 register days. Pre-roadmap head `6762b575958e23dabcc27fc4140c6ca54f178002` passed all three CI workflows; exact-final-head CI remains mandatory after roadmap bookkeeping. The proposed v13 migration remains unapplied to live Supabase.
- **17 Sep 2026:** Phase 4 impact mapping established the department-dependent reporting-population requirement: Whole Class (including SEN) must coexist with Non-SEN-only statistics rather than forcing one interpretation globally. Draft PR #49 (`cleanup/phase-4a-reporting-population-fixtures`) implements Phase 4A safety-net work only: aggregate production-derived fixtures, a read-only verifier, browser fixture fidelity corrections, and backend-contract CI inclusion. Live verification produced Whole-Class February `398/425 = 93.65%`, Non-SEN February `392/408 = 96.08%`, Whole-Class Term 1 `1051/1125 = 93.42%`, and Non-SEN Term 1 `1036/1080 = 95.93%`, with zero verifier mismatches. No runtime reporting code, public RPC signature, grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 4B remains not started and PR #49 remains unmerged pending final exact-head gates and explicit approval.
- **16 Sep 2026:** Phase 3 completed through Phase 3B7F3. Fresh remaining-ownership mapping found `assets/js/main.js` had become a small composition root, so no further listener extraction was justified. Branch `cleanup/phase-3b7f3-composition-closure` / PR #47 added only `tests/e2e/composition-root.spec.mjs`, with two browser regressions for the unsaved-attendance `beforeunload` guard and generic `[data-close]` student-movement dialog wiring. Exact head `a76229c3f720b6e6351a1b73f1cdef83da703fbf` passed Phase 0A `35053697452`, Phase 0B `35053697405`, and Playwright `35053697404` with 53/53 Chromium tests in 18.8s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. PR #47 then merged with the expected-head SHA guard as signed `main` commit `07505b7de962ecc46bc06ce4c51cfa47d94dcdd0`, with parents `db00744b43602a057fd9a034fa863f602e632c07` and `a76229c3f720b6e6351a1b73f1cdef83da703fbf`, and tree `7497d486a82a58db82e604668065f62ebe6f3db9`; GitHub signature verification is valid. No runtime/backend/Auth/Science/Netlify/production-data/reporting/deployment change was made. Phase 4 has not started.
- **16 Sep 2026:** Phase 3B7F2 completed. PR #45 approved normalized head `cb131514a7b90b81d52882455281a25de6b3609f` was exactly one commit ahead / zero behind signed/docs-closure base `cf939c3582d4db2c2be7a503b4355cfd9066af55` with exactly five changed files. Exact-head gates were fully green: Phase 0A Integrity `34987342629`, Phase 0B Backend Contract `34987342602`, and Phase 1 Playwright `34987342606` with 51/51 Chromium tests in 21.2s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The application-navigation regression passed alongside all protected recovery, Attendance/correction, student/teacher administration, official 3A February and Term 1/CSV, missing-register, gender-incomplete statistics, Dashboard → Statistics, YTD, stale-response, bootstrap, and UI-helper safeguards. PR #45 then merged as signed runtime commit `03207a339cf5b4c0836bcdc5fccd95f451adbfd4`, with parents `cf939c3582d4db2c2be7a503b4355cfd9066af55` and `cb131514a7b90b81d52882455281a25de6b3609f`, and tree `63297bbfca93db74bc4f4236e513aff322a2e7cc`; GitHub signature verification is valid. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B7F3+ has not started.
- **15 Sep 2026:** Phase 3B7F2 application-navigation checkpoint was implemented on branch `cleanup/phase-3b7f2-app-navigation` / draft PR #45 from exact signed/docs-closure `main` `cf939c3582d4db2c2be7a503b4355cfd9066af55`. Only `switchPanel(name)` and `openStatisticsForClass(classId, month)` move to new `assets/js/app-navigation.js`; all top-level event listeners, generic `[data-close]` wiring, `beforeunload`, backend contracts, Auth/recovery, Science, Netlify, production data, and deployment remain unchanged. A new browser navigation regression covers six-panel visibility/active-tab behavior, target lazy loaders, Statistics/Reports class fallback, Dashboard → Statistics routing, Students/Teachers loading, and no extra RPC on Attendance return. The evolved Phase 0A gate preserves historical exact-extraction evidence through Phase 3B7F1 and caught/rejected two source-format mismatches during construction without any assertion weakening. Corrected pre-normalization head `f47b1304bb3f89c065171262e76cd14b48e30f49` passed Phase 0A `34985669382`, Phase 0B `34985669379`, and Playwright `34985669413` with 51/51 Chromium tests in 16.5s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. Final one-commit normalization, exact normalized-head CI, diff/review verification, and explicit merge approval remained mandatory at that pre-merge checkpoint.
- **15 Sep 2026:** Phase 3B7F1 completed. PR #43 exact approved normalized head `0a83eb4218f2dea14711d8293227b8bccbb0579a` was exactly one commit ahead / zero behind signed base `09f86472da499811af416dee8176c58ca5e5e06c` with exactly five changed files. Exact-head gates were fully green: Phase 0A Integrity `34965425313`, Phase 0B Backend Contract `34965425341`, and Phase 1 Playwright `34965425320` with 50/50 Chromium tests in 19.6s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The saved-class and zero-assigned-class bootstrap regressions passed alongside all protected recovery, Attendance/correction, student/teacher administration, official 3A February and Term 1/CSV, missing-register, gender-incomplete statistics, Dashboard → Statistics, YTD, stale-response, and UI-helper safeguards. PR #43 then merged with the expected-head SHA guard as signed runtime commit `3c72aca9e043b734640b1d728d1910cf02632af3`, with parents `09f86472da499811af416dee8176c58ca5e5e06c` and `0a83eb4218f2dea14711d8293227b8bccbb0579a`, and tree `4c842562129abcc773e83cd31c80658121640f25`. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B7F2+ had not started at that merge checkpoint.
- **15 Sep 2026:** Phase 3B7F+ remaining-ownership impact mapping was completed from exact signed `main` `09f86472da499811af416dee8176c58ca5e5e06c` and user explicitly approved Phase 3B7F1 only. The map found `main.js` reduced to 8,035 bytes with remaining ownership concentrated in bootstrap/scope, panel routing, listener wiring, and the unload guard. Fresh read-only live Supabase verification confirmed `attendance_bootstrap()` remains no-argument `jsonb`, `STABLE` SECURITY INVOKER with authenticated/service-role execute only, while `attendance.can_access_class(uuid)` remains the internal SECURITY DEFINER authorization helper. Branch `cleanup/phase-3b7f1-app-bootstrap` and draft PR #43 were created. The approved split moves only `enterApp()`, `schoolId()`, `adminClasses()`, and `showAdminMsg()` into new `assets/js/app-bootstrap.js`; all panel/event/unload wiring remains in `main.js`. Initial structural head `88c3c2b2f2eb36b97cdd9e0850eb15f1be11547b` passed Phase 0A `34964378413`, Phase 0B `34964378421`, and Playwright `34964378410` with 50/50 Chromium tests in 19.5s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. Final normalized documented-head CI and explicit merge approval remained mandatory; no backend/Auth/Science/Netlify/production-data/deployment mutation was made.
- **15 Sep 2026:** Documentation-only PR #42 closed the canonical Phase 3B7E roadmap checkpoint and merged into signed cleanup `main` as `09f86472da499811af416dee8176c58ca5e5e06c`, with parents Phase 3B7E runtime merge `e9a4338c0723becc5dcd3f30fd207ad28efdd47b` and docs head `d9082cf490501e49cb76eaae74c25bada4af4f19`; merge tree `b8ad6ddcd5f73558982ea2d6a83674ecd85d8713`, GitHub signature valid. Exact docs-head gates were Phase 0A `34939873644`, Phase 0B `34939873657`, and Playwright `34939873655` with 48/48 Chromium tests in 19.1s. No runtime/backend/Auth/Science/Netlify/production-data/deployment change was included.
- **15 Sep 2026:** Phase 3B7E completed. PR #41 exact approved normalized head `f45cc8b585a2bbcae09699f1cd8b4c2f019e00b4` was exactly one commit ahead / zero behind signed base `c59dd2965b6ab2c6aef0e6340df8ebac6e73ef78` with exactly five changed files. Exact-head gates were fully green: Phase 0A Integrity `34930538204`, Phase 0B Backend Contract `34930538202`, and Phase 1 Playwright `34930538178` with 48/48 Chromium tests in 20.2s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The new loaded-report invalidation/CSV-export regression passed alongside all protected recovery, ten Attendance, student/teacher administration, official 3A February and Term 1/CSV, missing-register, gender-incomplete statistics, Dashboard → Statistics, YTD, stale-report-response, and UI-helper safeguards. PR #41 then merged with the expected-head SHA guard as signed runtime commit `e9a4338c0723becc5dcd3f30fd207ad28efdd47b`, with parents `c59dd2965b6ab2c6aef0e6340df8ebac6e73ef78` and `f45cc8b585a2bbcae09699f1cd8b4c2f019e00b4`, and tree `6805cf738e93c22f047a5ea224590c0453c712b7`. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B7F+ had not started.
- **15 Sep 2026:** Phase 3B7E Term/YTD/CSV impact mapping was completed and explicitly approved from exact signed `main` `c59dd2965b6ab2c6aef0e6340df8ebac6e73ef78`, then branch `cleanup/phase-3b7e-period-reports` and draft PR #41 were created. The approved boundary moves only report selection invalidation, report-options loading, Term/YTD load/render ownership, CSV escaping and browser-download ownership into new `assets/js/period-reports.js`; Reports panel routing/listeners and shared report state/request serials remain in their existing owners. Fresh read-only live Supabase verification reconfirmed latest migration `20260906125521 attendance_v12_term_ytd_reporting`, authenticated/service-role `STABLE SECURITY DEFINER` report RPCs with explicit authentication plus `attendance.can_access_class` authorization, and backend ownership of school-day/eligible-pupil-day/missing-register/reporting formulas. It also surfaced authenticated school-admin direct writes to reporting-structural tables for Phase 5 review. Corrected normalized four-file structural head `2916a2bc4ae07e5ae6f952e22f9bde33f360d34a` passed Phase 0A `34929855127`, Phase 0B `34929855115`, and Playwright `34929855093` with 48/48 Chromium tests after the integrity gate caught and rejected an earlier final-newline serialization mismatch. Final five-file normalization/current-head CI and explicit merge approval remained mandatory; no Supabase/Auth/Science/Netlify/production-data/deployment mutation was made.
- **15 Sep 2026:** Documentation-only PR #40 closed the canonical Phase 3B7D roadmap checkpoint and merged into signed cleanup `main` as `c59dd2965b6ab2c6aef0e6340df8ebac6e73ef78`, with parents runtime merge `52d4dd245e06cd0a3e42a5c7da15eeff121a9055` and docs head `e603bc8bc019a0e6b6f0f2ad7c68b2de63f67d19`; merge tree `766e5a3699ddb0f59b4c55201c9b54c24bc79df7`, GitHub signature valid. Exact docs-head gates were Phase 0A `34922321745`, Phase 0B `34922321842`, and Playwright `34922321685` with 47/47 Chromium tests in 20.1s. No runtime/backend/Auth/Science/Netlify/production-data/deployment change was included.
- **15 Sep 2026:** Phase 3B7D completed. PR #39 exact approved normalized head `c7c161a44d3729b124494f137a9bc4e863bc46c8` was exactly one commit ahead / zero behind signed base `18ddf13a3292772646bb8885b2470f3e4a081f64` with exactly five changed files. Exact-head gates were fully green: Phase 0A Integrity `34920286845`, Phase 0B Backend Contract `34920286857`, and Phase 1 Playwright `34920286878` with 47/47 Chromium tests in 21.1s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The new Dashboard → Statistics View regression passed alongside all protected recovery, ten Attendance, student/teacher administration, official 3A February and Term 1, missing-register, gender-incomplete statistics, dashboard/report stale-response, YTD/CSV, and UI-helper safeguards. PR #39 then merged with the expected-head SHA guard as signed runtime commit `52d4dd245e06cd0a3e42a5c7da15eeff121a9055`, with parents `18ddf13a3292772646bb8885b2470f3e4a081f64` and `c7c161a44d3729b124494f137a9bc4e863bc46c8`, and tree `975fac2c13f0958aebafb4b304b33f7cd59aad05`. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B7E+ had not started.
- **15 Sep 2026:** Phase 3B7D Admin Dashboard impact mapping was completed and explicitly approved from exact signed `main` `18ddf13a3292772646bb8885b2470f3e4a081f64`, then branch `cleanup/phase-3b7d-admin-dashboard` and draft PR #39 were created. The approved boundary moves only Dashboard banner/load/render ownership into new `assets/js/admin-dashboard.js`, keeps shared state/request serials plus `schoolId()` and panel/listener orchestration in existing owners, and injects a narrow `openStatisticsForClass(classId, month)` callback so cross-panel routing remains in `main.js`. Fresh read-only live Supabase verification reconfirmed latest Attendance migration `20260906125521 attendance_v12_term_ytd_reporting`, authenticated/service-role `STABLE SECURITY DEFINER` dashboard access with explicit `attendance.is_school_admin(p_school_id)` authorization, and dashboard reuse of `attendance_monthly_class_stats()`. Four-file structural head `0bf7970b8f1af3f8ed930612d6646fee4c24191f` passed Phase 0A `34919575848`, Phase 0B `34919575745`, and Playwright `34919575863` with 47/47 Chromium tests in 18.4s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The new Dashboard → Statistics View regression passed alongside all protected recovery, Attendance, administration, reporting-fixture, missing-register, stale-response, CSV, and UI-helper safeguards. Final five-file normalization/current-head CI and explicit merge approval remained mandatory; no Supabase/Auth/Science/Netlify/production-data/deployment mutation was made.
- **15 Sep 2026:** Phase 3B7C completed. PR #37 exact approved normalized head `0ee26371a67860c98c45d8c48b79b9a8ec3f2776` was exactly one commit ahead / zero behind signed base `1912512eb344b17fa0f219268becfdac31bba00d` with exactly five changed files. Exact-head gates were fully green: Phase 0A Integrity `34913511240`, Phase 0B Backend Contract `34913511220`, and Phase 1 Playwright `34913511216` with 46/46 Chromium tests in 18.1s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The new gender-incomplete monthly-statistics regression passed alongside all signup/recovery, ten Attendance, student/teacher administration, missing-register, 3A February, admin-dashboard, Term 1/YTD, stale-report-response, CSV, and UI-helper safeguards. PR #37 then merged with the expected-head SHA guard as signed runtime commit `bf7379332a35f1ea9f859ea2ade6b6e67cdf3c73`, with parents `1912512eb344b17fa0f219268becfdac31bba00d` and `0ee26371a67860c98c45d8c48b79b9a8ec3f2776`, and tree `91337aa06661e772876ebf2a419935d00bce098e`. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/dashboard/Term-YTD/CSV/deployment mutation was made. Phase 3B7D+ had not started.
- **15 Sep 2026:** Phase 3B7C monthly-statistics impact mapping was completed and explicitly approved from exact signed `main` `1912512eb344b17fa0f219268becfdac31bba00d`, then branch `cleanup/phase-3b7c-statistics` and draft PR #37 were created. The approved boundary moves only `setStatsBanner()`, `loadMonthlyStats()`, and `renderMonthlyStats()` into new `assets/js/statistics.js`; `state.monthlyStats`, request serial ownership, statistics panel orchestration/listener wiring, Admin Dashboard, Term/YTD/CSV, Auth/recovery, backend contracts, Science, Netlify, production data, and deployment remain unchanged. Fresh read-only live Supabase verification reconfirmed latest Attendance migration `20260906125521 attendance_v12_term_ytd_reporting`, authenticated-only class-statistics access through `attendance.can_access_class`, and backend ownership of reporting denominators/missing-register/eligibility semantics. Four-file structural head `33db9b0429f0412731f79717473d61013660e508` passed Phase 0A `34912869909`, Phase 0B `34912869882`, and Playwright `34912869880` with 46/46 Chromium tests in 18.2s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The new gender-incomplete regression passed alongside all protected reporting, recovery, Attendance, administration, and UI-helper tests. Final roadmap/five-file normalization and exact-current-head CI remained mandatory before merge; no Supabase/Auth/Science/Netlify/production-data/deployment mutation was made.
- **15 Sep 2026:** Documentation-only PR #36 closed the canonical Phase 3B7B roadmap checkpoint and merged into signed cleanup `main` as `1912512eb344b17fa0f219268becfdac31bba00d`, with parents `326e54ea0c529f538e68433f2f30b4588ec2fe71` and `5360b2f20893f8274bb1b7c84ab7b2826e67ea58`. Exact docs-head gates were Phase 0A `34911018139`, Phase 0B `34911018241`, and Playwright `34911018210` with 45/45 Chromium tests in 13.4s. No runtime/backend/Auth/Science/Netlify/production-data/deployment change was included.
- **15 Sep 2026:** Phase 3B7B completed. PR #35 exact approved normalized head `69af4e3d8975f5a728223abd78bc3d5baca897fb` was exactly one commit ahead / zero behind signed base `3259ad542b9b1816ab98aafa2a502b833ee69a65` with exactly five changed files. Exact-head gates were fully green: Phase 0A Integrity `34906180110`, Phase 0B Backend Contract `34906180159`, and Phase 1 Playwright `34906180122` with 45/45 Chromium tests in 15.5s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The rejected-request correction/resubmission regression passed alongside all signup/recovery, ten Attendance, student/teacher administration, missing-register, 3A February, dashboard, Term 1/YTD, stale-report-response, CSV, and UI-helper safeguards. PR #35 then merged with the expected-head SHA guard as signed runtime commit `326e54ea0c529f538e68433f2f30b4588ec2fe71`, with parents `3259ad542b9b1816ab98aafa2a502b833ee69a65` and `69af4e3d8975f5a728223abd78bc3d5baca897fb`, and tree `1e5e8b5b053201c7b267057efa03f04eee591289`. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B7C+ had not started.
- **15 Sep 2026:** Phase 3B7B teacher signup/gate/access impact mapping was completed and explicitly approved from exact signed `main` `3259ad542b9b1816ab98aafa2a502b833ee69a65`, then branch `cleanup/phase-3b7b-teacher-access` and draft PR #35 were created. The approved boundary moves only signup options/request submission, versioned account-bound pending-signup storage, signup/gate rendering, and the `attendance_teacher_status` authorization check into new `assets/js/teacher-access.js`; `auth-session.js`, recovery priority, authorized `attendance_bootstrap` startup, event-listener ownership, DOM IDs/selectors, backend contracts, Science, Netlify, production data, and deployment remain unchanged. Fresh read-only live verification reconfirmed latest Attendance migration `20260906125521 attendance_v12_term_ytd_reporting`, the intended anonymous/authenticated signup-options grant, authenticated-only request/status RPCs, caller-bound request semantics, and the lack of direct anon/authenticated table access to signup requests/audit. Mapping also identified a pre-existing concurrent Check Approval stale-response/UI-routing race, deferred to a dedicated correctness checkpoint. Four-file structural head `6df42d8b5dc63ff8bf103aed6b0acf3f82c19dc4` passed Phase 0A `34905444884`, Phase 0B `34905444906`, and Playwright `34905444876` with 45/45 Chromium tests in 14.8s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. Final roadmap/five-file normalization and exact-current-head CI remained mandatory; no Supabase/Auth/Science/Netlify/production-data/deployment mutation was made.
- **14 Sep 2026:** Phase 3B7A completed. PR #33 exact approved normalized head `30228093fb1e013929bad99ccf76a12505fae0dc` was exactly one commit ahead / zero behind signed base `fbe65f87b5a6a106ce1953a9d22a26a6793c7635` with exactly five changed files. Exact-head gates were fully green: Phase 0A Integrity `34862335422`, Phase 0B Backend Contract `34862335187`, and Phase 1 Playwright `34862335217` with 44/44 Chromium tests in 17.4s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. The new teacher-admin hostile-text/action regression passed alongside all signup/recovery, ten Attendance, student-management, missing-register, 3A February, admin-dashboard, Term 1, and CSV safeguards. PR #33 then merged with the expected-head SHA guard as signed runtime commit `f9fb4f88a98f5e9f03a1d33b92ebe69ea19691ca`, with parents `fbe65f87b5a6a106ce1953a9d22a26a6793c7635` and `30228093fb1e013929bad99ccf76a12505fae0dc`. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B7B had not started.
- **14 Sep 2026:** Phase 3B7A teacher-admin impact map was completed and explicitly approved from exact signed `main` `fbe65f87b5a6a106ce1953a9d22a26a6793c7635`, then `cleanup/phase-3b7a-teacher-admin` and draft PR #33 were created. Fresh read-only live Supabase verification confirmed the four teacher-admin RPCs remain authenticated-only SECURITY DEFINER functions with internal admin authorization and audit semantics, and latest Attendance migration remains `20260906125521 attendance_v12_term_ytd_reporting`. It also confirmed authenticated direct CRUD on teacher school/class assignment tables, classified as Phase 5 security/audit debt, and identified the pre-existing `loadAdminTeachers()` stale-response race for a separate correctness follow-up. The approved extraction moves only in-app teacher-admin list/render/review/active-toggle ownership into `assets/js/teacher-admin.js`; signup/gate/pending-signup/`enterApp()` and recovery startup remain untouched. One hostile-text/action browser regression was added. Exact structural head `4c5eea1dea8f5e49b5635b95038021ba79d51f25` passed Phase 0A `34853746386`, Phase 0B `34853746557`, and Playwright `34853746392` with 44/44 Chromium tests in 19.1s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. Final five-file normalization/current-head CI and explicit merge approval remained pending at that implementation checkpoint. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made; Phase 3B7B had not started.
- **14 Sep 2026:** Phase 3B6 completed. PR #31 exact approved head `5c60d94f3b9dca395bce4f37f7c8066a031f3ce3` passed all required exact-head gates: Phase 0A Integrity `34825996158`, Phase 0B Backend Contract `34825996159`, and Phase 1 Playwright `34825996150` with 43/43 Chromium tests in 17.5s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. All five student-management browser scenarios passed, including the new hostile-text/filter/action integrity and Transfer-In-default regressions; all ten Attendance regressions and the 3A February, missing-register, admin-dashboard, and Term 1/CSV safeguards also passed. PR #31 then merged with the expected-head SHA guard as signed runtime commit `a915ebf897a29285594bd274725f00c3dfc1bea9`, with parents `b4fbee48cc91fb9e60f4163c0fd59d1363f5f9db` and `5c60d94f3b9dca395bce4f37f7c8066a031f3ce3`. Superseded draft PR #30 was closed without merge. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B7 had not started.
- **14 Sep 2026:** Phase 3B6 student-management impact map was completed and explicitly approved from exact signed `main` `b4fbee48cc91fb9e60f4163c0fd59d1363f5f9db`. Fresh read-only live Supabase verification confirmed the four student-management RPCs remain authenticated-only SECURITY INVOKER functions with school-admin checks and reconfirmed the latest Attendance migration `20260906125521`. It also surfaced broader authenticated direct table privileges on `students`/`enrolments` and `student_movements`, classified as Phase 5 security debt, plus a pre-existing frontend stale-roster response race classified for a separate correctness follow-up. The approved Phase 3B6 implementation extracts only student-management ownership into `assets/js/student-management.js`, keeps shared admin helpers/state/orchestration in `main.js`, and adds two narrow browser regressions. Because the originally named branch already contained prior unsigned work, a clean `cleanup/phase-3b6-student-management-v2` branch was created from the exact signed base and the old branch was left untouched. Verification and explicit merge approval remained pending at that implementation checkpoint; no SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made.
- **14 Sep 2026:** Documentation-only PR #29 closed the canonical Phase 3B5 checkpoint and merged as signed `main` commit `b4fbee48cc91fb9e60f4163c0fd59d1363f5f9db`, with parents `8a3b8173a1e576a8eee39df721bcfcc7a98ce971` and `58d1c7dfe19721883ec28ba8209c3bf112924ec1`. Exact docs-head gates were Phase 0A `34814909499`, Phase 0B `34814909340`, and Playwright `34814909377` with 41/41 Chromium tests in 15.4s. No runtime/backend/Auth/Science/Netlify/production-data/deployment change was included.
- **14 Sep 2026:** Phase 3B5 completed. PR #28 exact approved head `6cf10229ca8da5d8fb012dc483f7d36250a1010e` passed all required exact-head gates: Phase 0A Integrity `34808420449`, Phase 0B Backend Contract `34808420379`, and Phase 1 Playwright `34808420455` with 41/41 Chromium tests in 17.1s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. All ten Attendance tests passed, including new regressions for untouched `PP`/`TM` preservation, `OTHER` note validation, and non-school-day save blocking; the 3A February, missing-register, admin-dashboard, and Term 1/CSV reporting safeguards also passed. PR #28 then merged with the expected-head SHA guard as signed runtime commit `8a3b8173a1e576a8eee39df721bcfcc7a98ce971`, with parents `dc274130989e32d354ff4f28d8cc3c302010b7e5` and `6cf10229ca8da5d8fb012dc483f7d36250a1010e`. No Supabase/Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B6 had not started at that checkpoint.
- **14 Sep 2026:** Phase 3B5 attendance/corrections impact map was completed and explicitly approved from exact signed `main` `dc274130989e32d354ff4f28d8cc3c302010b7e5`, then branch `cleanup/phase-3b5-attendance-register` was created from that SHA. The approved implementation extracts only the existing daily-register/correction ownership into `assets/js/attendance-register.js`, reuses the single existing Supabase client, preserves shared state/request serials and DOM/event orchestration, and adds three narrow browser regressions for untouched `PP`/`TM` preservation, `OTHER` note validation, and non-school-day save blocking. Phase 0A is evolved to keep historical Phase 3B4 proof and verify the exact 3B5 transformation. Fresh read-only live Supabase evidence reconfirmed the two register RPCs are authenticated-only SECURITY INVOKER functions using the existing class-access authorization helper and that correction/audit safeguards remain intact; the known direct-DML risk remains Phase 5 debt. No SQL/RPC/grant/RLS/Auth/Science/Netlify/production-data/deployment mutation was made. Exact-head CI/final review and explicit merge approval remained pending at that implementation checkpoint.
- **14 Sep 2026:** Documentation-only PR #27 closed the canonical Phase 3B4 checkpoint and merged into signed cleanup `main` as `dc274130989e32d354ff4f28d8cc3c302010b7e5`, with parents `4cce7446a51671e35443247a755672d1f422d6c0` and `7ee5113ac3a323424e6511f235815d67e9173c08`. No runtime, backend, Auth, Science, Netlify, production-data, or deployment change was included.
- **14 Sep 2026:** Phase 3B4 completed. PR #26 exact approved head `1fa2e8191f4665a161f73f4be0e49c4b41b6f0b6` passed all required exact-head gates: Phase 0A Integrity `34795927230`, Phase 0B Backend Contract `34795927225`, and Phase 1 Playwright `34795927215` with 38/38 Chromium tests in 13.7s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. PR #26 then merged with the expected-head SHA guard as signed runtime commit `4cce7446a51671e35443247a755672d1f422d6c0`, with parents `cf753418b51e92ff91b223d448bbee2a783d3736` and `1fa2e8191f4665a161f73f4be0e49c4b41b6f0b6`. The merged checkpoint preserves recovery priority, uses one Supabase client, and adds browser regressions for recovery-session sign-out failure and external `SIGNED_OUT`. No SQL/RPC/grant/RLS/hosted-Auth/Science/Netlify/production-data/deployment mutation was made. Phase 3B5 had not started at that checkpoint.
- **14 Sep 2026:** Phase 3B4 impact map was reviewed and explicitly approved from exact signed `main` `cf753418b51e92ff91b223d448bbee2a783d3736`. Branch `cleanup/phase-3b4-auth-session` implements only the approved auth-session/password-recovery split into `assets/js/auth-session.js`, reusing the existing Supabase client and preserving recovery priority over normal session routing. Teacher signup/access ownership and pending-signup storage remain in `main.js`. Two narrow browser regressions were added for recovery sign-out failure and external `SIGNED_OUT`. Phase 0A was evolved to retain historical Phase 3B3B proof and verify the exact Phase 3B4 transformation. Fresh read-only live Supabase evidence remained 18 public RPCs, 11 SECURITY DEFINER RPCs, 18 Attendance/Private tables, 17 RLS-enabled tables, 50 Attendance RLS policies, latest migration `20260906125521`; no SQL/RPC/grant/RLS/Auth/Science/Netlify/data/deployment mutation was made. Verification was pending at that implementation checkpoint.
- **14 Sep 2026:** Phase 3B3B completed. PR #24 exact approved head `60b8be26675370e086fef620a01a9c798f4e26b3` passed targeted UI-helper coverage (2/2 Chromium tests in 1.9s) and all required exact-head gates: Phase 0A Integrity `34788503095`, Phase 0B Backend Contract `34788503087`, and Phase 1 Playwright `34788503082` with 36/36 Chromium tests in 12.8s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. PR #24 then merged with the expected-head SHA guard as signed runtime commit `53ed00e309f221a0f19e2ec446a41e01144177b0`, with parents `20f3088bb5a2f0161409ad04ce486c94c40a1801` and `60b8be26675370e086fef620a01a9c798f4e26b3`. No Supabase, Auth, Science, Netlify, production-data, or production-deployment mutation was made. Auth/recovery modularisation had not started at that checkpoint.
- **14 Sep 2026:** Phase 3B3B impact map was reviewed and explicitly approved. Exact signed Git `main` was re-verified at `20f3088bb5a2f0161409ad04ce486c94c40a1801`, branch `cleanup/phase-3b3b-ui-helpers` was created from that SHA, and draft PR #24 established the checkpoint. Fresh read-only live Supabase verification confirmed 18 public Attendance RPCs, 11 SECURITY DEFINER / 7 invoker RPCs, 18 Attendance/Private tables, 17 RLS-enabled tables, 50 Attendance RLS policies, latest Attendance migration `20260906125521 attendance_v12_term_ytd_reporting`, and the known direct authenticated DML risk on `attendance.daily_registers` / `attendance.attendance_records`; no live mutation was made. Approved runtime scope extracts only `$`, `esc()`, and `fillGroupedClasses()` into `assets/js/ui-helpers.js` and adds browser regressions for hostile-markup escaping and class grouping/sorting/compact/exclusion semantics. Auth/recovery, DOM IDs/selectors, RPC/SQL/grant/RLS contracts, Science, Netlify, production data, and deployment remain unchanged.
- **14 Sep 2026:** Phase 3B3A completed. PR #22 was re-verified at exact approved head `f52617c479e405fcbae0bef21b67dfbcc7485f4d`, with targeted staging run `34769713952` passing 22/22 Chromium tests and all three exact-head gates green (Phase 0A `34770151745`, Phase 0B `34770151759`, Playwright `34770151764`: 34/34 passed in 11.0s, Node `v22.23.2`, npm `10.9.8`, 0 vulnerabilities), then merged with an expected-head SHA guard. Verified signed runtime merge and new cleanup `main` `8427442405d6d5a20debe430163b21c243f167ae` with parents `363c581ec42dda337902dad147a8fbe7b18a155e` and `f52617c479e405fcbae0bef21b67dfbcc7485f4d`. No Supabase, Auth, Science, Netlify, production-data, or production-deployment mutation was made. Phase 3B3B had not started at that checkpoint.
- **14 Sep 2026:** Phase 3B3A implementation verification reached PR #22. Clean implementation head `06208a8de6100a1ab4e34af3071abfaf64acc266` is exactly one commit ahead / zero behind with only `.github/workflows/phase0a-integrity.yml`, `ATTENDANCE_TECH_DEBT_CLEANUP_ROADMAP.md`, `assets/js/date-helpers.js`, and `assets/js/main.js` changed. Targeted staging run `34769713952` passed 22/22 Chromium tests in 7.4s; initial clean PR-head gates also passed: Phase 0A `34769981106`, Phase 0B `34769981117`, and Playwright `34769981089` with 34/34 tests in 13.9s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. Final normalized-head CI remained mandatory before merge. Production v0.7 remained unchanged; Phase 3B3B had not started.
- **14 Sep 2026:** Phase 3B3A impact map was reviewed and explicitly approved. Exact signed Git `main` was re-verified at `363c581ec42dda337902dad147a8fbe7b18a155e`, then `cleanup/phase-3b3a-date-helpers` was created from that SHA. A fresh read-only live Supabase check succeeded before implementation and confirmed 18 public Attendance RPCs, 11 SECURITY DEFINER RPCs, 18 Attendance/Private tables, 17 RLS-enabled tables, 50 Attendance RLS policies, and latest Attendance migration `20260906125521`; no live mutation was made. The approved runtime scope extracts only the four existing date helpers into side-effect-free `assets/js/date-helpers.js`, preserving `Asia/Brunei` today semantics and UTC display formatting. DOM/UI helpers, selectors, RPC contracts, Auth/recovery, Science, Netlify, data, and deployment remain unchanged. Exact-current-head verification and explicit merge approval remained mandatory at that implementation checkpoint.
- **13 Sep 2026:** Phase 3B2 completed. PR #20 was re-verified at exact approved head `70abada98f17c9eb37209d315aa219005ce5f8ec`, with targeted browser run `34763678581` passing 16/16 tests and all three exact-head gates green (Phase 0A `34763765748`, Phase 0B `34763765633`, Playwright `34763765650`: 34/34 passed in 13.9s, Node `v22.23.2`, npm `10.9.8`, 0 vulnerabilities), then merged with an expected-head SHA guard. Verified signed merge commit and new cleanup `main` `ece37a0cfeaf996a20c10a3f0cc638d35cea3c56` with parents `0794ac1e63c9d516f4f69c3cde7185b4822b98fa` and `70abada98f17c9eb37209d315aa219005ce5f8ec`. No Supabase, Auth, Science, Netlify, production-data, or production-deployment mutation was made. Phase 3B3 had not started at that checkpoint.
- **13 Sep 2026:** Phase 3B2 impact map was reviewed and explicitly approved. From exact signed `main` `0794ac1e63c9d516f4f69c3cde7185b4822b98fa`, branch `cleanup/phase-3b2-shared-state` extracts only the existing shared mutable application state plus the five stale-response request serials and their two helper functions into side-effect-free `assets/js/app-state.js`. All 16 state fields and five serial keys are preserved exactly; `passwordRecoveryActive`, pending-signup ownership, DOM selectors, RPC contracts, `index.html`, and `supabase-client.js` remain unchanged. Fresh read-only live Supabase verification confirmed the same 18 public Attendance RPCs, 50 Attendance RLS policies, latest migration `20260906125521 attendance_v12_term_ytd_reporting`, and the known direct-DML risk deferred to Phase 5. No live mutation was made. Exact-current-head CI remained mandatory before merge at that checkpoint.
- **13 Sep 2026:** Phase 3B1 completed. PR #18 was re-verified at exact approved head `7c5e4e9c3e61b9394bb48850333d1cdacedeb648`, with all three gates green (Phase 0A `34760369839`, Phase 0B `34760369847`, Playwright `34760369842`: 34/34 passed in 14.1s, Node `v22.23.2`, npm `10.9.8`, 0 vulnerabilities), then merged with an expected-head SHA guard. Verified signed merge commit and new cleanup `main` `ad2026b1056e311e41b50746601398938b73e16a` with parents `a4eef41c5fa0dd6d807927b187b697ff03ae9afe` and `7c5e4e9c3e61b9394bb48850333d1cdacedeb648`. No Supabase, Netlify, Science, production-data, or production-deployment mutation was made. Phase 3B2 had not started at that checkpoint.
- **13 Sep 2026:** Phase 3B1 impact map was reviewed and explicitly approved. From exact signed `main` `a4eef41c5fa0dd6d807927b187b697ff03ae9afe`, branch `cleanup/phase-3b1-supabase-client` and draft PR #18 extract only the pinned Supabase dependency/client construction into `assets/js/supabase-client.js` via `createAttendanceClient()`, while `main.js` retains recovery-link detection and client-creation timing. Fresh read-only live verification confirmed the same 18 frontend Attendance RPC signatures and latest migration `20260906125521 attendance_v12_term_ytd_reporting`; no backend mutation was made. Initial runtime head `c7f0d35e2b657f4235a04af8970d20b4c5fdb083` passed Phase 0A `34760006063` (including historical Phase 3A and exact Phase 3B1 transformation proofs), Phase 0B `34760006039`, and Playwright `34760006095` with 34/34 Chromium tests in 14.2s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. Final current-head CI was mandatory before merge; exact results were recorded in PR #18 after verification.
- **13 Sep 2026:** PR #16 merged as signed `main` commit `0526944153c4176d47ea1a2b0891e3ed56fe479c`, completing Phase 3A. The former inline application module now loads from `assets/js/main.js` with byte-for-byte source equivalence verified at the approved PR head; Netlify remained unlinked and no production promotion occurred. Phase 3B remained unstarted at that checkpoint.
- **10 Sep 2026:** Initial cleanup roadmap created.
- **12 Sep 2026:** Rebased roadmap on audited v1.0 source and Attendance-specific engineering rules; split Phase 0 into frontend Git baseline and backend reconstruction; marked Maths-specific wrapper/loader checks as not applicable to Attendance; reclassified password-recovery routing race as confirmed production behaviour and added explicit acceptance criteria.
- **12 Sep 2026:** Created public canonical GitHub repository `nrnd-pixel/SR-Lumapas-Attendance`; recorded initial `main` SHA `5f4608613afefdba81c2bba4c7c8133b5fcf33f5`; froze exact v1.0 frontend on `cleanup/phase-0a-freeze-v1.0`; verified source integrity; opened draft PR #1 for review. No deployment or functional change.
- **12 Sep 2026:** Added minimal Phase 0A integrity CI at `.github/workflows/phase0a-integrity.yml`; PR #1 moved to head `c8963f78d6edf9dd770ac50a355fe6ce85525f1a` with four added files. GitHub Actions run `34676963624` completed successfully. Removed obsolete risk stating that no canonical Git/main baseline existed. User has approved merge, but PR #1 remains unmerged pending final exact-head verification.
- **12 Sep 2026:** Phase 0A completed. Re-verified PR #1 head `c8963f78d6edf9dd770ac50a355fe6ce85525f1a` and green integrity CI, marked the PR ready for review, and merged with an expected-head SHA guard. Verified current `main` at merge commit `c578b4240a2bd9899db602fa818bda99bd6ff3cd`. No deployment or runtime behaviour change. Phase 0B not started.
- **12 Sep 2026:** Phase 0B discovery started from verified `main` `c578b4240a2bd9899db602fa818bda99bd6ff3cd`; created `cleanup/phase-0b-backend-source` at the exact same SHA. Re-verified the 18-RPC frontend contract and confirmed the repository still has no Attendance SQL source. No backend SQL was written or applied; fresh live Supabase structural capture remains required before reconstruction.
- **13 Sep 2026:** Phase 0B fresh live structural snapshot captured from `SR Lumapas Science Dev` (`rojetehazryfpcxlwtbi`) and reconciled with frozen v1.0. Snapshot contains 423 metadata rows: 18 tables, 163 columns, 94 constraints, 57 indexes, 13 triggers, 50 RLS policies, and 25 functions. All 18 frontend RPC names match live public Attendance RPCs exactly. Documented confirmed direct-DML bypass risk and class/assistant-teacher role-semantic mismatch. No SQL written or applied.
- **13 Sep 2026:** Phase 0B snapshot #2 captured: 39 migration-history rows (31 Attendance, 8 Science), 13 Attendance codes, and 6 absence reasons. Identified production roster/pilot seed migrations that must not be copied into the public repository and creator-identity metadata that is unnecessary for reconstruction. Re-verified Git `main` and the Phase 0B branch both at `c578b4240a2bd9899db602fa818bda99bd6ff3cd`. Proposed a sanitized cumulative baseline layout; no SQL committed or applied.
- **13 Sep 2026:** Captured Supabase Auth URL configuration: Site URL `https://srlumapas.netlify.app/`, allowed Redirect URL `https://srlumapas.netlify.app/**`. This matches frontend `window.location.origin + '/'` redirects. Phase 0B evidence capture is complete; implementation remains unstarted pending approval.
- **13 Sep 2026:** Phase 0B backend-source checkpoint implemented on clean head `ad532b719e5c5a7cacbc3f3ce705b601ac5bb37b`. Temporary staging history was normalized away; final branch is one commit ahead of `main` with exactly six approved files. Draft PR #2 opened. Exact-head CI green: Phase 0B Backend Contract run `34732039528` and Phase 0A Integrity run `34732039525`. PR remains unmerged pending explicit user approval.
- **13 Sep 2026:** Phase 0B completed. PR #2 was re-verified at exact head `ad532b719e5c5a7cacbc3f3ce705b601ac5bb37b`, with exactly six approved files and both Phase 0A/Phase 0B CI gates green, then marked ready and merged with an expected-head SHA guard. Verified current `main` at merge commit `71bfd7dc9b6a14fd6c80021ba034e0ead8edc2e2`; merged tree contains the backend baseline/verification files and no temporary staging artifacts. No live Supabase or frontend changes. Phase 1 not started.
- **13 Sep 2026:** User approved the persistent GitHub roadmap and subsequent Phase 1A Playwright foundation. Created documentation branch `docs/persistent-cleanup-roadmap` from exact `main` `71bfd7dc9b6a14fd6c80021ba034e0ead8edc2e2`. The root roadmap file will become canonical across future chats once merged; Phase 1A remains unstarted until that documentation PR is merged and verified.
- **13 Sep 2026:** Persistent roadmap PR #3 merged with expected head `7f3f7ebc137e8142b94b0d0e6df0337fdc68adce`; verified new `main` at `ffcdb460b991c5873f22a97ac8767a2826c077a0`. Repository-root `ATTENDANCE_TECH_DEBT_CLEANUP_ROADMAP.md` is now canonical across future sessions. Created `cleanup/phase-1a-playwright-foundation` from that exact SHA and began only the previously approved browser-regression foundation; no production/runtime change.
- **13 Sep 2026:** Phase 1A browser foundation implemented in draft PR #4. Initial Playwright 1.55.0 run passed 12 browser tests but npm reported 2 high-severity dependency vulnerabilities; upgraded the test-only dependency to current stable Playwright 1.63.0 after checking npm. Exact head `8176f45ea9c9c503f26647b00cf3e5dd6003cd06` then passed Phase 1 Playwright run `34733865915`, Phase 0A run `34733865920`, and Phase 0B run `34733865914`; npm audit reported 0 vulnerabilities. No frontend or live-system change.
- **13 Sep 2026:** Phase 1A completed. PR #4 was re-verified at exact head `08e10ed7952e1e21adaafcf458d89f75e56b75ef`, with exactly eight approved files and all three CI gates green, then marked ready and merged with an expected-head SHA guard. Verified current `main` at merge commit `c3c2068f428156a1071f4961f810bebdcbf597bd`. The Playwright browser-regression foundation is now on `main`; live v0.7 and production Supabase remain unchanged. Phase 1B and Phase 2 have not started.
- **13 Sep 2026:** User approved Phase 1B. Created `cleanup/phase-1b-admin-reporting-playwright` from exact `main` `3acbd8fc0a6bff99afac7868719d8ce69a044d9f` and opened draft PR #6. Added browser coverage for student movements, teacher administration, monthly statistics, dashboard, Term/YTD reports, CSV export, official 3A reporting fixtures, and separate missing-register semantics. After correcting an internal fixture consistency issue and a hidden-selector test-order issue without changing application code or weakening assertions, exact implementation head `3f96b934645ba6f1df743f4bcf9364a1304c7da5` passed all three gates: Playwright run `34735346538` (23 passed), Phase 0A `34735346559`, Phase 0B `34735346539`; npm reported 0 vulnerabilities. PR remains draft/unmerged pending final roadmap-head CI and explicit user merge approval.
- **13 Sep 2026:** Final Phase 1B review re-verified the three-file-only diff, exact-head CI on `cd4633c5deba48fadb153ea66e5141d01a242481`, and absence of review threads/comments. One test-quality gap was found: Transfer Out mocked the preserved-attendance count without asserting the visible preservation response. The admin-management spec was strengthened to assert both Transfer Out preserved-record messaging and Move Class preserved-record/backfill messaging. The roadmap was updated to make exact-current-head CI—not an older embedded SHA—the merge gate. No application, Supabase, Science, Netlify/Auth, or production-data change was made.
- **13 Sep 2026:** Phase 1 completed. PR #6 was re-verified at exact head `4aa3bcc86697ead0935e382fcb1d7ffe8f8b0557`, with exactly three approved files and all three gates green (Playwright `34736212462`: 23/23 passed; Phase 0A `34736212438`; Phase 0B `34736212441`), then merged with an expected-head SHA guard. Verified new `main` at merge commit `a0176fa922ff6d524b2844ff761865081000c5cd`. Live v0.7 and production Supabase remain unchanged. Phase 2 has not started.
- **13 Sep 2026:** Documentation closure PR #7 updated the canonical roadmap to mark Phase 1 complete and Phase 2 not started, then merged into `main` at `9b43a4ce3dd949c37a609c4203654f526ec3b865`. No runtime, backend, Science, Netlify/Auth, deployment, or production-data change.
- **13 Sep 2026:** Phase 2 impact map completed from exact `main` `9b43a4ce3dd949c37a609c4203654f526ec3b865`; user approved Phase 2A only. PR #8 created on `cleanup/phase-2a-password-recovery`. The confirmed recovery race was fixed by giving a recovery link/state priority over normal startup routing and, after a successful password update, signing out the recovery session and returning to normal login. Two former expected-failure recovery tests became mandatory passing assertions. Initial implementation head `0f8645c3fcaa010d6315b24a346c3f19de1b3cf6` passed all 23 Playwright tests and the Phase 0B backend contract; the old Phase 0A current-file checksum guard failed by design because Phase 2 is the first intentional runtime change.
- **13 Sep 2026:** User approved evolution of the Phase 0A integrity gate. Commit `1806fe73e14563811ea5fc4ed6ae25469b652dec` preserves the original v1.0 freeze by verifying the historical `index.html` from baseline merge `c578b4240a2bd9899db602fa818bda99bd6ff3cd` against the original SHA-256, while separately syntax-checking the current frontend.
- **13 Sep 2026:** Phase 2A final acceptance coverage was strengthened without production credentials: the synthetic Auth harness models password replacement, and the recovery browser test proves old-password failure, new-password success, and preserved teacher class scope. Exact PR #8 head `053598a0445b0f3aa3bc17a498c2db37d54834af` passed all three gates: Phase 0A `34738533610`, Phase 0B `34738533605`, and Playwright `34738534139` (23/23, 0 vulnerabilities).
- **13 Sep 2026:** Phase 2A completed. PR #8 was merged with an expected-head SHA guard at exact head `053598a0445b0f3aa3bc17a498c2db37d54834af`. Verified signed merge commit and new cleanup `main` at `f4f141bb46890715eb0c35a692c4bad4e7dd969c`. No Supabase SQL/RPC/grant/RLS, Science, `netlify.toml`, reporting-formula, attendance-history, or production-data change was included. No explicit production promotion was performed. Phase 2B/2C/2D remain unstarted.
- **13 Sep 2026:** Documentation closure PR #9 recorded the merged Phase 2A checkpoint and was merged into `main` at `235fdb4277e20be230ae012b0101176a68c15d5c`. The merge was documentation-only; Phase 2A runtime remained unchanged and Phase 2B/2C/2D remained unstarted.
- **13 Sep 2026:** Phase 2B impact mapping and fresh live RPC verification completed from exact `main` `235fdb4277e20be230ae012b0101176a68c15d5c`. Confirmed that unbound pending-signup `localStorage` could be submitted, pre-filled, or deleted by a different account on a shared browser. Live `attendance_submit_teacher_request` remains bound internally to `auth.uid()` and `attendance_teacher_status` returns `user_id`, so the checkpoint is frontend/test-only with no backend authorization change required.
- **13 Sep 2026:** Phase 2B implemented on `cleanup/phase-2b-pending-signup-binding`: pending signup state is versioned and bound to Auth user ID, written only after successful signup, consumed/cleared only for the matching account, and legacy unbound state fails closed. Browser tests were expanded for same-account verification return, failed signup, cross-account isolation, preservation during manual gate submission, authorized-account isolation, and legacy-state handling. Whole-file `index.html` reconstruction drift was caught during diff review and corrected before PR/CI. Verification remains pending; do not merge or start Phase 2C until exact-head gates are green and explicit approval is given.
- **13 Sep 2026:** Phase 2B completed. PR #10 was re-verified at exact head `136ae88e46ff62a961d684cbb6ed92d705a13033` with exactly three changed files and all three required CI gates green (Phase 0A `34744925819`, Phase 0B `34744925796`, Playwright `34744925879`: 27/27 passed, 0 npm vulnerabilities), then merged with an expected-head SHA guard. Verified signed new cleanup `main` at `41d88be698d1e7bd7cd0eaca1956323d2941290c`, with parents `235fdb4277e20be230ae012b0101176a68c15d5c` and `136ae88e46ff62a961d684cbb6ed92d705a13033`. No backend, Science, `netlify.toml`, attendance-history/reporting, production-data, or explicit production-promotion change was included. Phase 2C/2D remain unstarted.
- **13 Sep 2026:** Documentation closure PR #11 recorded the merged Phase 2B checkpoint and was merged into `main` at `7f1d25307678800e9083c8e519faac60c9883726`. The merge was documentation-only; Phase 2B runtime remained unchanged and Phase 2C/2D remained unstarted.
- **13 Sep 2026:** Phase 2C impact mapping completed from exact signed `main` `7f1d25307678800e9083c8e519faac60c9883726`. Confirmed real stale-response races in `attendance_load_register`, monthly statistics, admin dashboard, report options, and Term/YTD rendering, including a report-options race that could pair an old class ID with newly populated term controls. Fresh live read-only Supabase verification confirmed the five RPC signatures, SECURITY DEFINER modes/authorization checks, and latest Attendance migration `20260906125521 attendance_v12_term_ytd_reporting` remain unchanged; no backend mutation is required.
- **13 Sep 2026:** User approved Phase 2C implementation. Created `cleanup/phase-2c-stale-response-guards` from exact `main` `7f1d25307678800e9083c8e519faac60c9883726`. Added per-surface request serial/selection guards and report-selection invalidation in `index.html`; extended the synthetic Supabase harness with deterministic named deferred RPC releases; added six Playwright race regressions covering attendance date, attendance A → B → A, monthly statistics, dashboard month, report options, and Term→YTD ordering. Immediate base-vs-branch diff review shows `index.html` limited to 47 additions / 6 deletions with no unrelated reconstruction drift. Exact-head CI and final PR review remain pending; do not merge or start Phase 2D until all gates are green and explicit approval is given.
- **13 Sep 2026:** Phase 2C completed. PR #12 was re-verified at exact head `de2499b2c981a681aaf3ce94e34577c80ea70f0a` with exactly five changed files and all three required gates green (Phase 0A `34748002787`; Phase 0B `34748002795`; Playwright `34748002818`: 33/33 passed, Node `v22.23.2`, npm `10.9.8`, 0 vulnerabilities), then merged with an expected-head SHA guard. Verified signed new cleanup `main` at `a8b73bc55cbdeea3b4edd6dace7f505a625f36d2`, with parents `7f1d25307678800e9083c8e519faac60c9883726` and `de2499b2c981a681aaf3ce94e34577c80ea70f0a`. No backend, Science, `netlify.toml`, attendance-history/reporting-formula, production-data, or explicit production-promotion change was included. Phase 2D remains unstarted.
- **13 Sep 2026:** Documentation closure PR #13 recorded the merged Phase 2C checkpoint and was merged into signed cleanup `main` at `ee962e793b267e63938133b95df8e36a8439f37a`. The merge was documentation-only; Phase 2D remained unstarted and no production promotion was performed.
- **13 Sep 2026:** Phase 2D impact/evidence mapping completed from exact signed `main` `ee962e793b267e63938133b95df8e36a8439f37a`. Current frontend signup and password recovery both request `window.location.origin + '/'`. Fresh user-provided Supabase Dashboard evidence confirms hosted Site URL `https://srlumapas.netlify.app/` and sole Redirect URL `https://srlumapas.netlify.app/**`, matching the Phase 0B record and current frontend; no hosted Auth configuration mutation is required. Current Supabase guidance recommends exact production redirect paths where practical, but wildcard narrowing is classified as optional hardening rather than a correctness fix, and Netlify preview-host allow-list design is deferred to pilot/staging scope.
- **13 Sep 2026:** User approved Phase 2D closure. Created `cleanup/phase-2d-auth-redirect-verification` from exact signed `main` `ee962e793b267e63938133b95df8e36a8439f37a` and opened draft PR #14. Test-only head `6829e9976d179bc8f6f4f370f3f0b790178969da` added two redirect-contract assertions with no runtime change and passed all required gates: Phase 0A `34749695923`, Phase 0B `34749695938`, and Playwright `34749696089` with 34/34 Chromium tests in 14.7s, Node `v22.23.2`, npm `10.9.8`, and 0 vulnerabilities. This roadmap update closes Phase 2 on the branch; exact-final-head CI remains mandatory before merge. Do not begin Phase 3 automatically.
- **13 Sep 2026:** Phase 2 completed. PR #14 was re-verified at exact head `f949407811e170da82052613905ba190fea8c29e` with exactly two changed files and all three required gates green (Phase 0A `34749816234`; Phase 0B `34749816250`; Playwright `34749816255`: 34/34 passed in 10.8s, Node `v22.23.2`, npm `10.9.8`, 0 vulnerabilities), then merged with an expected-head SHA guard. Verified signed new cleanup `main` at `97a17d87863cc8eb6c25263b57c139091c1e7189`, with parents `ee962e793b267e63938133b95df8e36a8439f37a` and `f949407811e170da82052613905ba190fea8c29e`. No runtime, backend, hosted Auth, Science, Netlify, production-data, or explicit production-promotion change was included. Phase 3 remains unstarted.
- **13 Sep 2026:** Phase 3A impact mapping completed from exact signed `main` `88fffe819a4b287bd05c48c0933ebf14c7d913d2`. Confirmed `index.html` owns the full frontend inline ES module; no Attendance wrapper chain exists; all 18 live frontend RPC signatures remain present; 11 are SECURITY DEFINER and 7 SECURITY INVOKER; Attendance table RLS/direct-DML risk remains unchanged and deferred to Phase 5. User approved Phase 3A. On `cleanup/phase-3a-extract-main-module`, the inline module was extracted verbatim to `assets/js/main.js`, `index.html` now uses a same-origin external module loader, and Phase 0A was strengthened to prove exact source/HTML transformation equivalence. First implementation head `608eea92ea2559b48057c9ea08bde8bedea826ce` passed Phase 0A `34755738953`, Phase 0B `34755738940`, and Playwright `34755738981` (34/34 in 13.4s, 0 vulnerabilities). Netlify account-level auto-publish verification remains a mandatory pre-merge gate; Phase 3B is unstarted.
- **13 Sep 2026:** Live Netlify Build & deploy settings evidence supplied by the user shows the SR Lumapas site's `Current repository` as `Not linked`. This clears the Phase 3A Git-linked auto-publish blocker: merging GitHub `main` does not by itself trigger a Netlify continuous deployment for the protected live site. No Netlify setting was changed. GitHub `main` remains unprotected with no repository rulesets, so PR-only/explicit-approval discipline remains mandatory.