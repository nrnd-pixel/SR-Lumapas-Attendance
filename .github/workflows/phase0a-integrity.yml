name: Phase 0A Integrity

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-baseline-and-current:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository with frozen baseline history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify expected current files
        shell: bash
        run: |
          set -euo pipefail
          test -f index.html
          test -f assets/js/main.js
          test -f assets/js/supabase-client.js
          test -f assets/js/app-state.js
          test -f assets/js/date-helpers.js
          test -f assets/js/ui-helpers.js
          test -f assets/js/auth-session.js
          test -f assets/js/attendance-register.js
          test -f assets/js/student-management.js
          test -f assets/js/teacher-admin.js
          test -f assets/js/teacher-access.js
          test -f assets/js/statistics.js
          test -f assets/js/admin-dashboard.js
          test -f assets/js/period-reports.js
          test -f assets/js/reporting-population.js
          test -f assets/js/app-bootstrap.js
          test -f assets/js/app-navigation.js
          test -f netlify.toml
          test -f README.txt

      - name: Verify frozen Phase 0A index checksum
        shell: bash
        run: |
          set -euo pipefail
          git cat-file -e c578b4240a2bd9899db602fa818bda99bd6ff3cd^{commit}
          git show c578b4240a2bd9899db602fa818bda99bd6ff3cd:index.html > /tmp/attendance-v1-frozen.html
          echo "199d93f81c1cdb1742544f0dbde3f9da2e264b9d2dc16e4ce93cd53140387086  /tmp/attendance-v1-frozen.html" | sha256sum --check --strict

      - name: Verify historical Phase 3A external-module extraction
        shell: python
        run: |
          import re
          import subprocess

          pre3_sha = "88fffe819a4b287bd05c48c0933ebf14c7d913d2"
          phase3a_sha = "0526944153c4176d47ea1a2b0891e3ed56fe479c"
          for sha in (pre3_sha, phase3a_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)

          before = subprocess.check_output(["git", "show", f"{pre3_sha}:index.html"], text=True)
          matches = list(re.finditer(r'<script\s+type="module"[^>]*>\n(.*?)</script>', before, flags=re.S | re.I))
          if len(matches) != 1:
              raise SystemExit(f"Expected exactly one pre-Phase-3 inline ES module, found {len(matches)}")

          match = matches[0]
          expected_module = match.group(1)
          phase3a_module = subprocess.check_output(["git", "show", f"{phase3a_sha}:assets/js/main.js"], text=True)
          if phase3a_module != expected_module:
              raise SystemExit("Historical Phase 3A main.js is not byte-for-byte equivalent to the pre-Phase-3 inline module")

          loader = '<script type="module" src="./assets/js/main.js"></script>'
          expected_html = before[:match.start()] + loader + before[match.end():]
          phase3a_html = subprocess.check_output(["git", "show", f"{phase3a_sha}:index.html"], text=True)
          if phase3a_html != expected_html:
              raise SystemExit("Historical Phase 3A index.html contains changes beyond the exact external-module extraction")

      - name: Verify historical Phase 3B1 Supabase client split
        shell: python
        run: |
          import subprocess

          phase3a_sha = "0526944153c4176d47ea1a2b0891e3ed56fe479c"
          phase3b1_sha = "ad2026b1056e311e41b50746601398938b73e16a"
          for sha in (phase3a_sha, phase3b1_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)

          before_main = subprocess.check_output(["git", "show", f"{phase3a_sha}:assets/js/main.js"], text=True)
          old_import = "import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.114.0/+esm';\n"
          new_import = "import { createAttendanceClient } from './supabase-client.js';\n"
          old_client = "const sb=createClient(\n  'https://rojetehazryfpcxlwtbi.supabase.co',\n  'sb_publishable_gc1-vVGBZOSgw32SCJumyw__vm0e3Jd',\n  {auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}}\n);\n"
          new_client = "const sb=createAttendanceClient();\n"
          if before_main.count(old_import) != 1 or before_main.count(old_client) != 1:
              raise SystemExit("Historical Phase 3A source does not contain the expected unique Supabase client blocks")

          expected_main = before_main.replace(old_import, new_import, 1).replace(old_client, new_client, 1)
          phase3b1_main = subprocess.check_output(["git", "show", f"{phase3b1_sha}:assets/js/main.js"], text=True)
          if phase3b1_main != expected_main:
              raise SystemExit("Historical Phase 3B1 main.js is not the exact approved client extraction")

          expected_client = "\n".join([
              "import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.114.0/+esm';",
              "",
              "export function createAttendanceClient(){",
              "  return createClient(",
              "    'https://rojetehazryfpcxlwtbi.supabase.co',",
              "    'sb_publishable_gc1-vVGBZOSgw32SCJumyw__vm0e3Jd',",
              "    {auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}}",
              "  );",
              "}",
              ""
          ])
          phase3b1_client = subprocess.check_output(["git", "show", f"{phase3b1_sha}:assets/js/supabase-client.js"], text=True)
          if phase3b1_client != expected_client:
              raise SystemExit("Historical Phase 3B1 supabase-client.js is not the exact approved client factory")

          phase3a_html = subprocess.check_output(["git", "show", f"{phase3a_sha}:index.html"], text=True)
          phase3b1_html = subprocess.check_output(["git", "show", f"{phase3b1_sha}:index.html"], text=True)
          if phase3b1_html != phase3a_html:
              raise SystemExit("Historical Phase 3B1 changed index.html")

      - name: Verify historical Phase 3B2 shared-state split
        shell: python
        run: |
          import subprocess

          phase3b1_sha = "ad2026b1056e311e41b50746601398938b73e16a"
          phase3b2_sha = "ece37a0cfeaf996a20c10a3f0cc638d35cea3c56"
          for sha in (phase3b1_sha, phase3b2_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b1_sha}:assets/js/main.js"], text=True)

          client_import = "import { createAttendanceClient } from './supabase-client.js';\n"
          state_import = "import { state, requestSerial, beginRequest, isLatestRequest } from './app-state.js';\n"
          state_decl = "const state={bootstrap:null,teacherStatus:null,register:null,rows:[],loading:false,saving:false,currentClassId:null,currentDate:null,adminRoster:null,selectedAdminStudent:null,teacherRequests:[],teachers:[],monthlyStats:null,adminDashboard:null,reportOptions:null,periodReport:null};\n"
          serial_decl = "const requestSerial={register:0,monthlyStats:0,adminDashboard:0,reportOptions:0,periodReport:0};\n"
          helper_block = "function beginRequest(kind){requestSerial[kind]+=1;return requestSerial[kind];}\nfunction isLatestRequest(kind,serial){return requestSerial[kind]===serial;}\n"

          for label, text in [('client import', client_import), ('state declaration', state_decl), ('request serial declaration', serial_decl), ('request helper block', helper_block)]:
              if before_main.count(text) != 1:
                  raise SystemExit(f"Historical Phase 3B1 source does not contain exactly one {label}")

          expected_main = before_main.replace(client_import, client_import + state_import, 1)
          expected_main = expected_main.replace(state_decl, '', 1).replace(serial_decl, '', 1).replace(helper_block, '', 1)
          phase3b2_main = subprocess.check_output(["git", "show", f"{phase3b2_sha}:assets/js/main.js"], text=True)
          if phase3b2_main != expected_main:
              raise SystemExit("Historical Phase 3B2 main.js is not the exact approved state/request-serial extraction")

          expected_state = "\n".join([
              "export const state={bootstrap:null,teacherStatus:null,register:null,rows:[],loading:false,saving:false,currentClassId:null,currentDate:null,adminRoster:null,selectedAdminStudent:null,teacherRequests:[],teachers:[],monthlyStats:null,adminDashboard:null,reportOptions:null,periodReport:null};",
              "export const requestSerial={register:0,monthlyStats:0,adminDashboard:0,reportOptions:0,periodReport:0};",
              "",
              "export function beginRequest(kind){requestSerial[kind]+=1;return requestSerial[kind];}",
              "export function isLatestRequest(kind,serial){return requestSerial[kind]===serial;}",
              ""
          ])
          phase3b2_state = subprocess.check_output(["git", "show", f"{phase3b2_sha}:assets/js/app-state.js"], text=True)
          if phase3b2_state != expected_state:
              raise SystemExit("Historical Phase 3B2 app-state.js is not the exact approved shared-state module")

          phase3b1_client = subprocess.check_output(["git", "show", f"{phase3b1_sha}:assets/js/supabase-client.js"], text=True)
          phase3b2_client = subprocess.check_output(["git", "show", f"{phase3b2_sha}:assets/js/supabase-client.js"], text=True)
          if phase3b2_client != phase3b1_client:
              raise SystemExit("Historical Phase 3B2 changed supabase-client.js")

          phase3b1_html = subprocess.check_output(["git", "show", f"{phase3b1_sha}:index.html"], text=True)
          phase3b2_html = subprocess.check_output(["git", "show", f"{phase3b2_sha}:index.html"], text=True)
          if phase3b2_html != phase3b1_html:
              raise SystemExit("Historical Phase 3B2 changed index.html")

      - name: Verify historical Phase 3B3A date-helper split
        shell: python
        run: |
          import subprocess

          phase3b2_sha = "ece37a0cfeaf996a20c10a3f0cc638d35cea3c56"
          phase3b3a_sha = "8427442405d6d5a20debe430163b21c243f167ae"
          for sha in (phase3b2_sha, phase3b3a_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b2_sha}:assets/js/main.js"], text=True)

          state_import = "import { state, requestSerial, beginRequest, isLatestRequest } from './app-state.js';\n"
          date_import = "import { bruneiToday, displayDate, formatMonthLabel, shortDay } from './date-helpers.js';\n"
          brunei_block = "function bruneiToday(){\n  const parts=new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Brunei',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date());\n  const o={}; parts.forEach(p=>o[p.type]=p.value); return o.year+'-'+o.month+'-'+o.day;\n}\n"
          display_block = "function displayDate(s){\n  if(!s)return'';\n  const [y,m,d]=s.split('-').map(Number);\n  return new Intl.DateTimeFormat('en-GB',{weekday:'long',day:'numeric',month:'long',year:'numeric',timeZone:'UTC'})\n    .format(new Date(Date.UTC(y,m-1,d)));\n}\n"
          month_block = "function formatMonthLabel(v){if(!v)return'';const [y,m]=v.split('-').map(Number);return new Intl.DateTimeFormat('en-GB',{month:'long',year:'numeric',timeZone:'UTC'}).format(new Date(Date.UTC(y,m-1,1)));}\n"
          short_block = "function shortDay(v){if(!v)return'';const [y,m,d]=v.split('-').map(Number);return new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'short',timeZone:'UTC'}).format(new Date(Date.UTC(y,m-1,d)));}\n"

          for label, text in [('state import', state_import), ('bruneiToday', brunei_block), ('displayDate', display_block), ('formatMonthLabel', month_block), ('shortDay', short_block)]:
              if before_main.count(text) != 1:
                  raise SystemExit(f"Historical Phase 3B2 source does not contain exactly one {label}")

          expected_main = before_main.replace(state_import, state_import + date_import, 1)
          for block in (brunei_block, display_block, month_block, short_block):
              expected_main = expected_main.replace(block, '', 1)
          phase3b3a_main = subprocess.check_output(["git", "show", f"{phase3b3a_sha}:assets/js/main.js"], text=True)
          if phase3b3a_main != expected_main:
              raise SystemExit("Historical Phase 3B3A main.js is not the exact approved date-helper extraction")

          expected_date = (
              brunei_block.replace("function bruneiToday", "export function bruneiToday", 1)
              + display_block.replace("function displayDate", "export function displayDate", 1)
              + month_block.replace("function formatMonthLabel", "export function formatMonthLabel", 1)
              + short_block.replace("function shortDay", "export function shortDay", 1)
          )
          phase3b3a_date = subprocess.check_output(["git", "show", f"{phase3b3a_sha}:assets/js/date-helpers.js"], text=True)
          if phase3b3a_date != expected_date:
              raise SystemExit("Historical Phase 3B3A date-helpers.js is not the exact approved date-helper module")

          for path in ("assets/js/app-state.js", "assets/js/supabase-client.js", "index.html"):
              before = subprocess.check_output(["git", "show", f"{phase3b2_sha}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b3a_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B3A changed {path}")

      - name: Verify historical Phase 3B3B UI-helper split
        shell: python
        run: |
          import subprocess

          phase3b3a_sha = "8427442405d6d5a20debe430163b21c243f167ae"
          phase3b3b_sha = "53ed00e309f221a0f19e2ec446a41e01144177b0"
          for sha in (phase3b3a_sha, phase3b3b_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b3a_sha}:assets/js/main.js"], text=True)

          date_import = "import { bruneiToday, displayDate, formatMonthLabel, shortDay } from './date-helpers.js';\n"
          ui_import = "import { $, esc, fillGroupedClasses } from './ui-helpers.js';\n"
          dollar_decl = "const $=id=>document.getElementById(id);\n"
          esc_decl = "function esc(v=''){return String(v).replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',\"'\":'&#39;','\"':'&quot;'}[c]));}\n"
          fill_block = "function fillGroupedClasses(select,classes,{blankLabel=null,excludeId=null,compact=false}={}){\n  select.innerHTML='';\n  if(blankLabel!==null){const o=document.createElement('option');o.value='';o.textContent=blankLabel;select.appendChild(o);}\n  const groups=new Map();\n  (classes||[]).filter(c=>c.id!==excludeId).forEach(c=>{const y=c.year_level||0;if(!groups.has(y))groups.set(y,[]);groups.get(y).push(c);});\n  [...groups.keys()].sort((a,b)=>a-b).forEach(y=>{\n    const g=document.createElement('optgroup');g.label=y===0?'Prasekolah':'Year '+y;\n    groups.get(y).sort((a,b)=>a.class_code.localeCompare(b.class_code)).forEach(c=>{\n      const o=document.createElement('option');o.value=c.id;\n      o.textContent=compact?(y===0?c.class_code.replace('PRA ','Pra '):c.class_code):(c.class_code+' — '+c.class_name);\n      g.appendChild(o);\n    });select.appendChild(g);\n  });\n}\n"

          for label, text in [('date import', date_import), ('dollar helper', dollar_decl), ('escape helper', esc_decl), ('class grouping helper', fill_block)]:
              if before_main.count(text) != 1:
                  raise SystemExit(f"Historical Phase 3B3A source does not contain exactly one {label}")

          expected_main = before_main.replace(date_import, date_import + ui_import, 1)
          expected_main = expected_main.replace(dollar_decl, '', 1).replace(esc_decl, '', 1).replace(fill_block, '', 1)
          phase3b3b_main = subprocess.check_output(["git", "show", f"{phase3b3b_sha}:assets/js/main.js"], text=True)
          if phase3b3b_main != expected_main:
              raise SystemExit("Historical Phase 3B3B main.js is not the exact approved UI-helper extraction")

          expected_ui = (
              dollar_decl.replace("const $", "export const $", 1)
              + "\n"
              + esc_decl.replace("function esc", "export function esc", 1)
              + "\n"
              + fill_block.replace("function fillGroupedClasses", "export function fillGroupedClasses", 1)
          )
          phase3b3b_ui = subprocess.check_output(["git", "show", f"{phase3b3b_sha}:assets/js/ui-helpers.js"], text=True)
          if phase3b3b_ui != expected_ui:
              raise SystemExit("Historical Phase 3B3B ui-helpers.js is not the exact approved helper module")

          for path in ("assets/js/date-helpers.js", "assets/js/app-state.js", "assets/js/supabase-client.js", "index.html"):
              before = subprocess.check_output(["git", "show", f"{phase3b3a_sha}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b3b_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B3B changed {path}")

      - name: Verify historical Phase 3B4 auth-session split
        shell: python
        run: |
          import hashlib
          import subprocess

          phase3b4_base = "cf753418b51e92ff91b223d448bbee2a783d3736"
          phase3b4_sha = "4cce7446a51671e35443247a755672d1f422d6c0"
          for sha in (phase3b4_base, phase3b4_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b4_base}:assets/js/main.js"], text=True)

          ui_import = "import { $, esc, fillGroupedClasses } from './ui-helpers.js';\n"
          auth_import = "import { initAuthSession, hideEntryViews, showLogin } from './auth-session.js';\n"
          recovery_state = "const initialRecoveryLink=\n  new URLSearchParams(window.location.hash.slice(1)).get('type')==='recovery'\n  || new URLSearchParams(window.location.search.slice(1)).get('type')==='recovery';\nconst sb=createAttendanceClient();\nlet passwordRecoveryActive=initialRecoveryLink;\n"
          client_only = "const sb=createAttendanceClient();\n"
          entry_block = "async function init(){\n  $('dateInput').value=bruneiToday();\n  sb.auth.onAuthStateChange((event,session)=>{\n    if(event==='PASSWORD_RECOVERY'){passwordRecoveryActive=true;showRecovery();return;}\n    if(event==='SIGNED_OUT'){passwordRecoveryActive=false;showLogin();return;}\n  });\n  const {data:{session}}=await sb.auth.getSession();\n  if(passwordRecoveryActive){showRecovery();return;}\n  if(session)await enterApp(); else showLogin();\n}\nfunction hideEntryViews(){\n  ['loginView','signupView','teacherGateView','recoveryView','appView'].forEach(id=>$(id).classList.add('hidden'));\n}\nfunction showLogin(){hideEntryViews();$('loginView').classList.remove('hidden');}\n"
          entry_replacement = "async function init(){\n  $('dateInput').value=bruneiToday();\n  await initAuthSession(sb,enterApp);\n}\n"
          show_recovery = "function showRecovery(){hideEntryViews();$('recoveryView').classList.remove('hidden');}\n"
          login_listener = "$('loginForm').addEventListener('submit',async e=>{\n  e.preventDefault();$('loginMsg').textContent='';$('loginBtn').disabled=true;$('loginBtn').innerHTML='<span class=\"spinner\"></span> Signing in';\n  const {error}=await sb.auth.signInWithPassword({email:$('email').value.trim(),password:$('password').value});\n  $('loginBtn').disabled=false;$('loginBtn').textContent='Sign In';\n  if(error){$('loginMsg').textContent=error.message;return;}await enterApp();\n});\n\n"
          recovery_listeners = "$('forgotBtn').addEventListener('click',async()=>{\n  const email=$('email').value.trim();\n  if(!email){$('loginMsg').textContent='Enter your email address first.';return;}\n  $('forgotBtn').disabled=true;\n  $('loginMsg').textContent='Sending password reset email…';\n  const {error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:window.location.origin+'/'});\n  $('forgotBtn').disabled=false;\n  if(error){$('loginMsg').textContent=error.message;return;}\n  $('loginMsg').style.color='#147a4b';\n  $('loginMsg').textContent='Password reset email sent. Open the email on this phone and tap the recovery link.';\n});\n$('recoveryForm').addEventListener('submit',async e=>{\n  e.preventDefault();\n  $('recoveryMsg').textContent='';\n  if($('newPassword').value!==$('confirmPassword').value){\n    $('recoveryMsg').textContent='The two passwords do not match.';return;\n  }\n  $('recoveryBtn').disabled=true;\n  const {error}=await sb.auth.updateUser({password:$('newPassword').value});\n  if(error){$('recoveryBtn').disabled=false;$('recoveryMsg').textContent=error.message;return;}\n  const {error:signOutError}=await sb.auth.signOut();\n  $('recoveryBtn').disabled=false;\n  if(signOutError){\n    $('recoveryMsg').textContent='Password updated, but the recovery session could not be signed out. Please try again.';return;\n  }\n  passwordRecoveryActive=false;\n  $('recoveryForm').reset();\n  showLogin();\n  $('loginMsg').style.color='#147a4b';\n  $('loginMsg').textContent='Password updated successfully. Sign in with your new password.';\n});\n$('signOutBtn').addEventListener('click',async()=>{await sb.auth.signOut();showLogin();});\n"

          for label, text in [
              ('UI import', ui_import),
              ('recovery/client state', recovery_state),
              ('entry routing block', entry_block),
              ('recovery view helper', show_recovery),
              ('login listener', login_listener),
              ('recovery listeners', recovery_listeners),
          ]:
              if before_main.count(text) != 1:
                  raise SystemExit(f"Phase 3B4 base does not contain exactly one expected {label}")

          expected_main = before_main.replace(ui_import, ui_import + auth_import, 1)
          expected_main = expected_main.replace(recovery_state, client_only, 1)
          expected_main = expected_main.replace(entry_block, entry_replacement, 1)
          expected_main = expected_main.replace(show_recovery, '', 1)
          expected_main = expected_main.replace(login_listener, '', 1)
          expected_main = expected_main.replace(recovery_listeners, '', 1)

          phase3b4_main = subprocess.check_output(["git", "show", f"{phase3b4_sha}:assets/js/main.js"], text=True)
          if phase3b4_main != expected_main:
              raise SystemExit("Historical Phase 3B4 main.js is not the exact approved auth-session extraction")

          auth_bytes = subprocess.check_output(["git", "show", f"{phase3b4_sha}:assets/js/auth-session.js"])
          if hashlib.sha256(auth_bytes).hexdigest() != "c084de8ca11ba4959f67afc26bf1885b6c20c38688de46f197978fc5a5651b71":
              raise SystemExit("Historical Phase 3B4 auth-session.js does not match the approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b4_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b4_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B4 changed {path}")

      - name: Verify historical Phase 3B5 attendance-register split
        shell: python
        run: |
          import subprocess

          phase3b5_base = "dc274130989e32d354ff4f28d8cc3c302010b7e5"
          phase3b5_sha = "8a3b8173a1e576a8eee39df721bcfcc7a98ce971"
          for sha in (phase3b5_base, phase3b5_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b5_base}:assets/js/main.js"], text=True)

          auth_import = "import { initAuthSession, hideEntryViews, showLogin } from './auth-session.js';\n"
          attendance_import = "import { initAttendanceRegister, setBusy, setBanner, loadRegister, confirmDiscard, markAllPresent, clearAll, updateSummary, save, hasUnsavedChanges } from './attendance-register.js';\n"
          client_decl = "const sb=createAttendanceClient();\n"
          client_replacement = client_decl + "initAttendanceRegister(sb);\n"

          for label, text in [('auth import', auth_import), ('client declaration', client_decl)]:
              if before_main.count(text) != 1:
                  raise SystemExit(f"Phase 3B5 base does not contain exactly one expected {label}")

          def take_span(text, start_marker, end_marker):
              start = text.index(start_marker)
              end = text.index(end_marker, start)
              return text[start:end]

          def remove_span(text, start_marker, end_marker, replacement=''):
              start = text.index(start_marker)
              end = text.index(end_marker, start)
              return text[:start] + replacement + text[end:]

          routine_block = take_span(before_main, "const routineOptions=[", "function periodReportSelectionKey(){").rstrip('\n')
          helper_block = take_span(before_main, "function groupFromCode(code){", "async function getSignupClasses(){").rstrip('\n')
          busy_block = take_span(before_main, "function setBusy(flag){", "async function init(){").rstrip('\n')
          attendance_block = take_span(before_main, "async function loadRegister(){", "function switchPanel(name){").rstrip('\n')

          expected_main = before_main.replace(auth_import, auth_import + attendance_import, 1)
          expected_main = expected_main.replace(client_decl, client_replacement, 1)
          expected_main = remove_span(expected_main, "const routineOptions=[", "function periodReportSelectionKey(){", "\n")
          expected_main = remove_span(expected_main, "function groupFromCode(code){", "async function getSignupClasses(){")
          expected_main = remove_span(expected_main, "function setBusy(flag){", "async function init(){")
          expected_main = remove_span(expected_main, "async function loadRegister(){", "function switchPanel(name){")

          phase3b5_main = subprocess.check_output(["git", "show", f"{phase3b5_sha}:assets/js/main.js"], text=True)
          if phase3b5_main != expected_main:
              raise SystemExit("Historical Phase 3B5 main.js is not the exact approved attendance-register extraction")

          helper_exports = helper_block
          helper_exports = helper_exports.replace("function hasUnsavedChanges", "export function hasUnsavedChanges", 1)
          helper_exports = helper_exports.replace("function confirmDiscard", "export function confirmDiscard", 1)
          busy_exports = busy_block
          busy_exports = busy_exports.replace("function setBusy", "export function setBusy", 1)
          busy_exports = busy_exports.replace("function setBanner", "export function setBanner", 1)
          attendance_exports = attendance_block
          for before, after in (
              ("async function loadRegister", "export async function loadRegister"),
              ("function markAllPresent", "export function markAllPresent"),
              ("function clearAll", "export function clearAll"),
              ("function updateSummary", "export function updateSummary"),
              ("async function save", "export async function save"),
          ):
              if attendance_exports.count(before) != 1:
                  raise SystemExit(f"Phase 3B5 attendance block does not contain exactly one {before}")
              attendance_exports = attendance_exports.replace(before, after, 1)

          expected_module = (
              "import { state, beginRequest, isLatestRequest } from './app-state.js';\n"
              "import { displayDate } from './date-helpers.js';\n"
              "import { $, esc } from './ui-helpers.js';\n\n"
              "let sb;\n\n"
              "export function initAttendanceRegister(client){sb=client;}\n\n"
              + routine_block + "\n\n"
              + helper_exports + "\n\n"
              + busy_exports + "\n\n"
              + attendance_exports + "\n"
          )
          phase3b5_module = subprocess.check_output(["git", "show", f"{phase3b5_sha}:assets/js/attendance-register.js"], text=True)
          if phase3b5_module != expected_module:
              raise SystemExit("Historical Phase 3B5 attendance-register.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b5_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b5_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B5 changed {path}")

      - name: Verify historical Phase 3B6 student-management split
        shell: python
        run: |
          import subprocess

          phase3b6_base = "b4fbee48cc91fb9e60f4163c0fd59d1363f5f9db"
          phase3b6_sha = "a915ebf897a29285594bd274725f00c3dfc1bea9"
          for sha in (phase3b6_base, phase3b6_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b6_base}:assets/js/main.js"], text=True)

          attendance_import = "import { initAttendanceRegister, setBusy, setBanner, loadRegister, confirmDiscard, markAllPresent, clearAll, updateSummary, save, hasUnsavedChanges } from './attendance-register.js';\n"
          student_import = "import { initStudentManagement, loadAdminStudents, renderAdminStudents, prepareTransferInDialog, syncTransferInStats, submitTransferIn, submitTransferOut, submitMoveClass } from './student-management.js';\n"
          attendance_init = "initAttendanceRegister(sb);\n"
          student_init = "initStudentManagement(sb,{schoolId,adminClasses,showAdminMsg});\n"

          if before_main.count(attendance_import) != 1 or before_main.count(attendance_init) != 1:
              raise SystemExit("Phase 3B6 base does not contain the expected unique Attendance import/init markers")

          def take_span(text, start_marker, end_marker):
              start = text.index(start_marker)
              end = text.index(end_marker, start)
              return text[start:end]

          def remove_span(text, start_marker, end_marker, replacement=''):
              start = text.index(start_marker)
              end = text.index(end_marker, start)
              return text[:start] + replacement + text[end:]

          core_block = take_span(before_main, "async function loadAdminStudents(){", "async function loadAdminTeachers(){").rstrip('\n')
          prepare_listener = take_span(before_main, "$('transferInBtn').addEventListener('click',()=>{", "$('tiGroup').addEventListener")
          group_listener = take_span(before_main, "$('tiGroup').addEventListener('change',()=>{", "document.querySelectorAll('[data-close]')")
          transfer_in_listener = take_span(before_main, "$('transferInForm').addEventListener('submit',async e=>{", "$('transferOutForm').addEventListener")
          transfer_out_listener = take_span(before_main, "$('transferOutForm').addEventListener('submit',async e=>{", "$('moveClassForm').addEventListener")
          move_listener = take_span(before_main, "$('moveClassForm').addEventListener('submit',async e=>{", "\n\nwindow.addEventListener('beforeunload'")

          expected_main = before_main.replace(attendance_import, attendance_import + student_import, 1)
          expected_main = expected_main.replace(attendance_init, attendance_init + student_init, 1)
          expected_main = remove_span(expected_main, "async function loadAdminStudents(){", "async function loadAdminTeachers(){")
          expected_main = expected_main.replace(prepare_listener, "$('transferInBtn').addEventListener('click',prepareTransferInDialog);\n", 1)
          expected_main = expected_main.replace(group_listener, "$('tiGroup').addEventListener('change',syncTransferInStats);\n", 1)
          expected_main = expected_main.replace(transfer_in_listener, "$('transferInForm').addEventListener('submit',submitTransferIn);\n", 1)
          expected_main = expected_main.replace(transfer_out_listener, "$('transferOutForm').addEventListener('submit',submitTransferOut);\n", 1)
          expected_main = expected_main.replace(move_listener, "$('moveClassForm').addEventListener('submit',submitMoveClass);", 1)

          phase3b6_main = subprocess.check_output(["git", "show", f"{phase3b6_sha}:assets/js/main.js"], text=True)
          if phase3b6_main != expected_main:
              raise SystemExit("Historical Phase 3B6 main.js is not the exact approved student-management extraction")

          core_exports = core_block.replace("async function loadAdminStudents", "export async function loadAdminStudents", 1)
          core_exports = core_exports.replace("function renderAdminStudents", "export function renderAdminStudents", 1)

          def listener_body(block, prefix):
              suffix = "});\n"
              if not block.startswith(prefix) or not block.endswith(suffix):
                  raise SystemExit("Unexpected Phase 3B6 listener wrapper shape")
              return block[len(prefix):-len(suffix)]

          prepare_body = listener_body(prepare_listener, "$('transferInBtn').addEventListener('click',()=>{")
          group_body = listener_body(group_listener, "$('tiGroup').addEventListener('change',()=>{")
          transfer_in_body = listener_body(transfer_in_listener, "$('transferInForm').addEventListener('submit',async e=>{")
          transfer_out_body = listener_body(transfer_out_listener, "$('transferOutForm').addEventListener('submit',async e=>{")
          move_body = listener_body(move_listener + "\n", "$('moveClassForm').addEventListener('submit',async e=>{")

          expected_module = (
              "import { state } from './app-state.js';\n"
              "import { bruneiToday } from './date-helpers.js';\n"
              "import { $, esc, fillGroupedClasses } from './ui-helpers.js';\n\n"
              "let sb;\n"
              "let schoolId;\n"
              "let adminClasses;\n"
              "let showAdminMsg;\n\n"
              "export function initStudentManagement(client,helpers){\n"
              "  sb=client;\n"
              "  schoolId=helpers.schoolId;\n"
              "  adminClasses=helpers.adminClasses;\n"
              "  showAdminMsg=helpers.showAdminMsg;\n"
              "}\n\n"
              + core_exports + "\n\n"
              + "export function prepareTransferInDialog(){" + prepare_body + "}\n"
              + "export function syncTransferInStats(){" + group_body + "}\n"
              + "export async function submitTransferIn(e){" + transfer_in_body + "}\n"
              + "export async function submitTransferOut(e){" + transfer_out_body + "}\n"
              + "export async function submitMoveClass(e){" + move_body + "}\n"
          )
          phase3b6_module = subprocess.check_output(["git", "show", f"{phase3b6_sha}:assets/js/student-management.js"], text=True)
          if phase3b6_module != expected_module:
              raise SystemExit("Historical Phase 3B6 student-management.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "assets/js/attendance-register.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b6_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b6_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B6 changed {path}")

      - name: Verify historical Phase 3B7A teacher-admin split
        shell: python
        run: |
          import subprocess

          phase3b7a_base = "fbe65f87b5a6a106ce1953a9d22a26a6793c7635"
          phase3b7a_sha = "f9fb4f88a98f5e9f03a1d33b92ebe69ea19691ca"
          for sha in (phase3b7a_base, phase3b7a_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b7a_base}:assets/js/main.js"], text=True)

          student_import = "import { initStudentManagement, loadAdminStudents, renderAdminStudents, prepareTransferInDialog, syncTransferInStats, submitTransferIn, submitTransferOut, submitMoveClass } from './student-management.js';\n"
          teacher_import = "import { initTeacherAdmin, loadAdminTeachers } from './teacher-admin.js';\n"
          student_init = "initStudentManagement(sb,{schoolId,adminClasses,showAdminMsg});\n"
          teacher_init = "initTeacherAdmin(sb,{adminClasses,showAdminMsg});\n"

          if before_main.count(student_import) != 1 or before_main.count(student_init) != 1:
              raise SystemExit("Phase 3B7A base does not contain the expected unique student-management import/init markers")

          start = before_main.index("async function loadAdminTeachers(){")
          end = before_main.index("$('openSignupBtn').addEventListener('click',showSignup);", start)
          teacher_block = before_main[start:end].rstrip('\n')

          expected_main = before_main.replace(student_import, student_import + teacher_import, 1)
          expected_main = expected_main.replace(student_init, student_init + teacher_init, 1)
          block_start = expected_main.index("async function loadAdminTeachers(){")
          listener_start = expected_main.index("$('openSignupBtn').addEventListener('click',showSignup);", block_start)
          expected_main = expected_main[:block_start] + "\n" + expected_main[listener_start:]

          phase3b7a_main = subprocess.check_output(["git", "show", f"{phase3b7a_sha}:assets/js/main.js"], text=True)
          if phase3b7a_main != expected_main:
              raise SystemExit("Historical Phase 3B7A main.js is not the exact approved teacher-admin extraction")

          teacher_exports = teacher_block.replace("async function loadAdminTeachers", "export async function loadAdminTeachers", 1)
          expected_teacher = (
              "import { state } from './app-state.js';\n"
              "import { $, esc } from './ui-helpers.js';\n\n"
              "let sb;\n"
              "let adminClasses;\n"
              "let showAdminMsg;\n\n"
              "export function initTeacherAdmin(client,helpers){\n"
              "  sb=client;\n"
              "  adminClasses=helpers.adminClasses;\n"
              "  showAdminMsg=helpers.showAdminMsg;\n"
              "}\n\n"
              + teacher_exports + "\n"
          )
          phase3b7a_teacher = subprocess.check_output(["git", "show", f"{phase3b7a_sha}:assets/js/teacher-admin.js"], text=True)
          if phase3b7a_teacher != expected_teacher:
              raise SystemExit("Historical Phase 3B7A teacher-admin.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "assets/js/attendance-register.js",
              "assets/js/student-management.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b7a_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b7a_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B7A changed {path}")

      - name: Verify historical Phase 3B7B teacher-access split
        shell: python
        run: |
          import subprocess

          phase3b7b_base = "3259ad542b9b1816ab98aafa2a502b833ee69a65"
          phase3b7b_sha = "326e54ea0c529f538e68433f2f30b4588ec2fe71"
          for sha in (phase3b7b_base, phase3b7b_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b7b_base}:assets/js/main.js"], text=True)

          teacher_import = "import { initTeacherAdmin, loadAdminTeachers } from './teacher-admin.js';\n"
          access_import = "import { initTeacherAccess, showSignup, ensureTeacherAccess, submitSignup, submitGateRequest, signOutTeacherGate } from './teacher-access.js';\n"
          teacher_init = "initTeacherAdmin(sb,{adminClasses,showAdminMsg});\n"
          access_init = "initTeacherAccess(sb);\n"
          constants = "const PENDING_SIGNUP_KEY='srlAttendancePendingTeacherSignup';\nconst PENDING_SIGNUP_VERSION=2;\n"

          for label, text in [('teacher import', teacher_import), ('teacher init', teacher_init), ('pending-signup constants', constants)]:
              if before_main.count(text) != 1:
                  raise SystemExit(f"Phase 3B7B base does not contain exactly one expected {label}")

          def take_span(text, start_marker, end_marker):
              start = text.index(start_marker)
              end = text.index(end_marker, start)
              return text[start:end]

          helper_block = take_span(before_main, "async function getSignupClasses(){", "async function init(){").rstrip('\n')
          signup_gate_block = take_span(before_main, "async function showSignup(){", "async function enterApp(){").rstrip('\n')
          enter_start = before_main.index("async function enterApp(){")
          bootstrap_marker = "  hideEntryViews();$('appView').classList.remove('hidden');\n"
          bootstrap_start = before_main.index(bootstrap_marker, enter_start)
          access_prefix = before_main[enter_start:bootstrap_start]

          signup_listener = take_span(before_main, "$('signupForm').addEventListener('submit',async e=>{", "$('gateRequestForm').addEventListener")
          gate_listener = take_span(before_main, "$('gateRequestForm').addEventListener('submit',async e=>{", "$('checkApprovalBtn').addEventListener")
          gate_signout = "$('gateSignOutBtn').addEventListener('click',async()=>{await sb.auth.signOut();showLogin();});\n"
          if before_main.count(gate_signout) != 1:
              raise SystemExit("Phase 3B7B base does not contain the expected unique gate sign-out listener")

          expected_main = before_main.replace(teacher_import, teacher_import + access_import, 1)
          expected_main = expected_main.replace(teacher_init, teacher_init + access_init, 1)
          expected_main = expected_main.replace(constants, '', 1)
          start = expected_main.index("async function getSignupClasses(){")
          end = expected_main.index("async function init(){", start)
          expected_main = expected_main[:start] + expected_main[end:]
          start = expected_main.index("async function showSignup(){")
          end = expected_main.index("async function enterApp(){", start)
          expected_main = expected_main[:start] + expected_main[end:]
          enter_start = expected_main.index("async function enterApp(){")
          bootstrap_start = expected_main.index(bootstrap_marker, enter_start)
          expected_main = expected_main[:enter_start] + "async function enterApp(){\n  if(!(await ensureTeacherAccess()))return;\n" + expected_main[bootstrap_start:]
          expected_main = expected_main.replace(signup_listener, "$('signupForm').addEventListener('submit',e=>submitSignup(e,enterApp));\n", 1)
          expected_main = expected_main.replace(gate_listener, "$('gateRequestForm').addEventListener('submit',submitGateRequest);\n", 1)
          expected_main = expected_main.replace(gate_signout, "$('gateSignOutBtn').addEventListener('click',signOutTeacherGate);\n", 1)

          phase3b7b_main = subprocess.check_output(["git", "show", f"{phase3b7b_sha}:assets/js/main.js"], text=True)
          if phase3b7b_main != expected_main:
              raise SystemExit("Historical Phase 3B7B main.js is not the exact approved teacher-access extraction")

          signup_gate_exports = signup_gate_block.replace("async function showSignup", "export async function showSignup", 1)
          ensure_block = access_prefix.replace("async function enterApp(){", "export async function ensureTeacherAccess(){", 1) + "  return true;\n}\n"

          def listener_body(block, prefix):
              suffix = "});\n"
              if not block.startswith(prefix) or not block.endswith(suffix):
                  raise SystemExit("Unexpected Phase 3B7B listener wrapper shape")
              return block[len(prefix):-len(suffix)]

          signup_body = listener_body(signup_listener, "$('signupForm').addEventListener('submit',async e=>{")
          gate_body = listener_body(gate_listener, "$('gateRequestForm').addEventListener('submit',async e=>{")

          expected_access = (
              "import { state } from './app-state.js';\n"
              "import { $, esc, fillGroupedClasses } from './ui-helpers.js';\n"
              "import { hideEntryViews, showLogin } from './auth-session.js';\n\n"
              + constants + "\n"
              + "let sb;\n\n"
              + "export function initTeacherAccess(client){\n  sb=client;\n}\n\n"
              + helper_block + "\n\n"
              + signup_gate_exports + "\n\n"
              + ensure_block + "\n"
              + "export async function submitSignup(e,enterApp){" + signup_body + "}\n"
              + "export async function submitGateRequest(e){" + gate_body + "}\n"
              + "export async function signOutTeacherGate(){await sb.auth.signOut();showLogin();}\n"
          )
          phase3b7b_access = subprocess.check_output(["git", "show", f"{phase3b7b_sha}:assets/js/teacher-access.js"], text=True)
          if phase3b7b_access != expected_access:
              raise SystemExit("Historical Phase 3B7B teacher-access.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "assets/js/attendance-register.js",
              "assets/js/student-management.js",
              "assets/js/teacher-admin.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b7b_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b7b_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B7B changed {path}")

      - name: Verify historical Phase 3B7C monthly-statistics split
        shell: python
        run: |
          import subprocess

          phase3b7c_base = "1912512eb344b17fa0f219268becfdac31bba00d"
          phase3b7c_sha = "bf7379332a35f1ea9f859ea2ade6b6e67cdf3c73"
          for sha in (phase3b7c_base, phase3b7c_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b7c_base}:assets/js/main.js"], text=True)

          access_import = "import { initTeacherAccess, showSignup, ensureTeacherAccess, submitSignup, submitGateRequest, signOutTeacherGate } from './teacher-access.js';\n"
          statistics_import = "import { initStatistics, loadMonthlyStats } from './statistics.js';\n"
          access_init = "initTeacherAccess(sb);\n"
          statistics_init = "initStatistics(sb);\n"

          for label, text in [('teacher-access import', access_import), ('teacher-access init', access_init)]:
              if before_main.count(text) != 1:
                  raise SystemExit(f"Phase 3B7C base does not contain exactly one expected {label}")

          stats_start = before_main.index("function setStatsBanner(text,type='info'){")
          stats_end = before_main.index("function setDashboardBanner(text,type='info')", stats_start)
          stats_block = before_main[stats_start:stats_end].rstrip('\n')

          expected_main = before_main.replace(access_import, access_import + statistics_import, 1)
          expected_main = expected_main.replace(access_init, access_init + statistics_init, 1)
          stats_start = expected_main.index("function setStatsBanner(text,type='info'){")
          stats_end = expected_main.index("function setDashboardBanner(text,type='info')", stats_start)
          expected_main = expected_main[:stats_start] + expected_main[stats_end:]

          historical_main = subprocess.check_output(["git", "show", f"{phase3b7c_sha}:assets/js/main.js"], text=True)
          if historical_main != expected_main:
              raise SystemExit("Historical Phase 3B7C main.js is not the exact approved monthly-statistics extraction")

          stats_exports = stats_block.replace("async function loadMonthlyStats", "export async function loadMonthlyStats", 1)
          expected_statistics = (
              "import { state, beginRequest, isLatestRequest } from './app-state.js';\n"
              "import { displayDate, formatMonthLabel, shortDay } from './date-helpers.js';\n"
              "import { $, esc } from './ui-helpers.js';\n\n"
              "let sb;\n\n"
              "export function initStatistics(client){sb=client;}\n\n"
              + stats_exports + "\n"
          )
          historical_statistics = subprocess.check_output(["git", "show", f"{phase3b7c_sha}:assets/js/statistics.js"], text=True)
          if historical_statistics != expected_statistics:
              raise SystemExit("Historical Phase 3B7C statistics.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "assets/js/attendance-register.js",
              "assets/js/student-management.js",
              "assets/js/teacher-admin.js",
              "assets/js/teacher-access.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b7c_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b7c_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B7C changed {path}")

      - name: Verify historical Phase 3B7D admin-dashboard split
        shell: python
        run: |
          import subprocess

          phase3b7d_base = "18ddf13a3292772646bb8885b2470f3e4a081f64"
          phase3b7d_sha = "52d4dd245e06cd0a3e42a5c7da15eeff121a9055"
          for sha in (phase3b7d_base, phase3b7d_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b7d_base}:assets/js/main.js"], text=True)

          statistics_import = "import { initStatistics, loadMonthlyStats } from './statistics.js';\n"
          dashboard_import = "import { initAdminDashboard, loadAdminDashboard } from './admin-dashboard.js';\n"
          statistics_init = "initStatistics(sb);\n"
          dashboard_init = "initAdminDashboard(sb,{schoolId,openStatisticsForClass});\n"

          for label, text in [('statistics import', statistics_import), ('statistics init', statistics_init)]:
              if before_main.count(text) != 1:
                  raise SystemExit(f"Phase 3B7D base does not contain exactly one expected {label}")

          dashboard_start = before_main.index("function setDashboardBanner(text,type='info'){")
          dashboard_end = before_main.index("function setReportBanner(text,type='info')", dashboard_start)
          dashboard_block = before_main[dashboard_start:dashboard_end].rstrip('\n')
          old_navigation = "    $('statsClassSelect').value=b.dataset.class;$('statsMonth').value=data.month;switchPanel('statistics');\n"
          if dashboard_block.count(old_navigation) != 1:
              raise SystemExit("Phase 3B7D base does not contain the expected unique Dashboard → Statistics navigation body")

          orchestration_helper = "function openStatisticsForClass(classId,month){\n  $('statsClassSelect').value=classId;$('statsMonth').value=month;switchPanel('statistics');\n}\n"
          expected_main = before_main.replace(statistics_import, statistics_import + dashboard_import, 1)
          expected_main = expected_main.replace(statistics_init, statistics_init + dashboard_init, 1)
          dashboard_start = expected_main.index("function setDashboardBanner(text,type='info'){")
          dashboard_end = expected_main.index("function setReportBanner(text,type='info')", dashboard_start)
          expected_main = expected_main[:dashboard_start] + orchestration_helper + expected_main[dashboard_end:]

          historical_main = subprocess.check_output(["git", "show", f"{phase3b7d_sha}:assets/js/main.js"], text=True)
          if historical_main != expected_main:
              raise SystemExit("Historical Phase 3B7D main.js is not the exact approved admin-dashboard extraction")

          dashboard_exports = dashboard_block.replace("async function loadAdminDashboard", "export async function loadAdminDashboard", 1)
          dashboard_exports = dashboard_exports.replace(old_navigation, "    openStatisticsForClass(b.dataset.class,data.month);\n", 1)
          expected_dashboard = (
              "import { state, beginRequest, isLatestRequest } from './app-state.js';\n"
              "import { displayDate, formatMonthLabel } from './date-helpers.js';\n"
              "import { $, esc } from './ui-helpers.js';\n\n"
              "let sb;\n"
              "let schoolId;\n"
              "let openStatisticsForClass;\n\n"
              "export function initAdminDashboard(client,helpers){\n"
              "  sb=client;\n"
              "  schoolId=helpers.schoolId;\n"
              "  openStatisticsForClass=helpers.openStatisticsForClass;\n"
              "}\n\n"
              + dashboard_exports + "\n"
          )
          historical_dashboard = subprocess.check_output(["git", "show", f"{phase3b7d_sha}:assets/js/admin-dashboard.js"], text=True)
          if historical_dashboard != expected_dashboard:
              raise SystemExit("Historical Phase 3B7D admin-dashboard.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "assets/js/attendance-register.js",
              "assets/js/student-management.js",
              "assets/js/teacher-admin.js",
              "assets/js/teacher-access.js",
              "assets/js/statistics.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b7d_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b7d_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B7D changed {path}")

      - name: Verify historical Phase 3B7E period-reports split
        shell: python
        run: |
          import subprocess

          phase3b7e_base = "c59dd2965b6ab2c6aef0e6340df8ebac6e73ef78"
          phase3b7e_sha = "e9a4338c0723becc5dcd3f30fd207ad28efdd47b"
          for sha in (phase3b7e_base, phase3b7e_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b7e_base}:assets/js/main.js"], text=True)

          old_state_import = "import { state, requestSerial, beginRequest, isLatestRequest } from './app-state.js';\n"
          new_state_import = "import { state } from './app-state.js';\n"
          old_date_import = "import { bruneiToday, displayDate, formatMonthLabel, shortDay } from './date-helpers.js';\n"
          new_date_import = "import { bruneiToday } from './date-helpers.js';\n"
          old_ui_import = "import { $, esc, fillGroupedClasses } from './ui-helpers.js';\n"
          new_ui_import = "import { $, fillGroupedClasses } from './ui-helpers.js';\n"
          dashboard_import = "import { initAdminDashboard, loadAdminDashboard } from './admin-dashboard.js';\n"
          report_import = "import { initPeriodReports, loadReportOptions, syncReportTypeUI, invalidatePeriodReport, loadPeriodReport, exportPeriodReport } from './period-reports.js';\n"
          dashboard_init = "initAdminDashboard(sb,{schoolId,openStatisticsForClass});\n"
          report_init = "initPeriodReports(sb);\n"

          for label, text in (
              ('state import', old_state_import),
              ('date import', old_date_import),
              ('UI import', old_ui_import),
              ('dashboard import', dashboard_import),
              ('dashboard init', dashboard_init),
          ):
              if before_main.count(text) != 1:
                  raise SystemExit(f"Phase 3B7E base does not contain exactly one expected {label}")

          selection_start = before_main.index("function periodReportSelectionKey(){")
          selection_end = before_main.index("async function init(){", selection_start)
          selection_block = before_main[selection_start:selection_end].rstrip('\n')
          report_start = before_main.index("function setReportBanner(text,type='info'){")
          report_end = before_main.index("function schoolId(){", report_start)
          report_block = before_main[report_start:report_end].rstrip('\n')

          expected_main = before_main.replace(old_state_import, new_state_import, 1)
          expected_main = expected_main.replace(old_date_import, new_date_import, 1)
          expected_main = expected_main.replace(old_ui_import, new_ui_import, 1)
          expected_main = expected_main.replace(dashboard_import, dashboard_import + report_import, 1)
          expected_main = expected_main.replace(dashboard_init, dashboard_init + report_init, 1)
          selection_start = expected_main.index("function periodReportSelectionKey(){")
          selection_end = expected_main.index("async function init(){", selection_start)
          expected_main = expected_main[:selection_start] + expected_main[selection_end:]
          report_start = expected_main.index("function setReportBanner(text,type='info'){")
          report_end = expected_main.index("function schoolId(){", report_start)
          expected_main = expected_main[:report_start] + expected_main[report_end:]

          historical_main = subprocess.check_output(["git", "show", f"{phase3b7e_sha}:assets/js/main.js"], text=True)
          if historical_main != expected_main:
              raise SystemExit("Historical Phase 3B7E main.js is not the exact approved period-reports extraction")

          selection_exports = selection_block.replace("function invalidatePeriodReport", "export function invalidatePeriodReport", 1)
          report_exports = report_block
          for before, after in (
              ("function syncReportTypeUI", "export function syncReportTypeUI"),
              ("async function loadReportOptions", "export async function loadReportOptions"),
              ("async function loadPeriodReport", "export async function loadPeriodReport"),
              ("function exportPeriodReport", "export function exportPeriodReport"),
          ):
              if report_exports.count(before) != 1:
                  raise SystemExit(f"Phase 3B7E report block does not contain exactly one {before}")
              report_exports = report_exports.replace(before, after, 1)

          expected_reports = (
              "import { state, requestSerial, beginRequest, isLatestRequest } from './app-state.js';\n"
              "import { bruneiToday, displayDate, formatMonthLabel, shortDay } from './date-helpers.js';\n"
              "import { $, esc } from './ui-helpers.js';\n\n"
              "let sb;\n\n"
              "export function initPeriodReports(client){sb=client;}\n\n"
              + selection_exports + "\n\n"
              + report_exports + "\n"
          )
          historical_reports = subprocess.check_output(["git", "show", f"{phase3b7e_sha}:assets/js/period-reports.js"], text=True)
          if historical_reports != expected_reports:
              raise SystemExit("Historical Phase 3B7E period-reports.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "assets/js/attendance-register.js",
              "assets/js/student-management.js",
              "assets/js/teacher-admin.js",
              "assets/js/teacher-access.js",
              "assets/js/statistics.js",
              "assets/js/admin-dashboard.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b7e_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b7e_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B7E changed {path}")

      - name: Verify historical Phase 3B7F1 app-bootstrap split
        shell: python
        run: |
          import subprocess

          phase3b7f1_base = "09f86472da499811af416dee8176c58ca5e5e06c"
          phase3b7f1_sha = "3c72aca9e043b734640b1d728d1910cf02632af3"
          for sha in (phase3b7f1_base, phase3b7f1_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b7f1_base}:assets/js/main.js"], text=True)

          old_ui_import = "import { $, fillGroupedClasses } from './ui-helpers.js';\n"
          new_ui_import = "import { $ } from './ui-helpers.js';\n"
          old_auth_import = "import { initAuthSession, hideEntryViews, showLogin } from './auth-session.js';\n"
          new_auth_import = "import { initAuthSession, showLogin } from './auth-session.js';\n"
          old_attendance_import = "import { initAttendanceRegister, setBusy, setBanner, loadRegister, confirmDiscard, markAllPresent, clearAll, updateSummary, save, hasUnsavedChanges } from './attendance-register.js';\n"
          new_attendance_import = "import { initAttendanceRegister, loadRegister, confirmDiscard, markAllPresent, clearAll, updateSummary, save, hasUnsavedChanges } from './attendance-register.js';\n"
          old_access_import = "import { initTeacherAccess, showSignup, ensureTeacherAccess, submitSignup, submitGateRequest, signOutTeacherGate } from './teacher-access.js';\n"
          new_access_import = "import { initTeacherAccess, showSignup, submitSignup, submitGateRequest, signOutTeacherGate } from './teacher-access.js';\n"
          report_import = "import { initPeriodReports, loadReportOptions, syncReportTypeUI, invalidatePeriodReport, loadPeriodReport, exportPeriodReport } from './period-reports.js';\n"
          bootstrap_import = "import { initAppBootstrap, enterApp, schoolId, adminClasses, showAdminMsg } from './app-bootstrap.js';\n"
          client_decl = "const sb=createAttendanceClient();\n"
          bootstrap_init = "initAppBootstrap(sb,{switchPanel});\n"

          for label, text in (
              ('UI import', old_ui_import),
              ('auth import', old_auth_import),
              ('Attendance import', old_attendance_import),
              ('teacher-access import', old_access_import),
              ('period-reports import', report_import),
              ('client declaration', client_decl),
          ):
              if before_main.count(text) != 1:
                  raise SystemExit(f"Phase 3B7F1 base does not contain exactly one expected {label}")

          enter_start = before_main.index("async function enterApp(){")
          enter_end = before_main.index("function switchPanel(name){", enter_start)
          enter_block = before_main[enter_start:enter_end].rstrip('\n')
          helpers_start = before_main.index("function schoolId(){")
          helpers_end = before_main.index("$('openSignupBtn').addEventListener('click',showSignup);", helpers_start)
          helpers_block = before_main[helpers_start:helpers_end].rstrip('\n')

          expected_main = before_main.replace(old_ui_import, new_ui_import, 1)
          expected_main = expected_main.replace(old_auth_import, new_auth_import, 1)
          expected_main = expected_main.replace(old_attendance_import, new_attendance_import, 1)
          expected_main = expected_main.replace(old_access_import, new_access_import, 1)
          expected_main = expected_main.replace(report_import, report_import + bootstrap_import, 1)
          expected_main = expected_main.replace(client_decl, client_decl + bootstrap_init, 1)
          enter_start = expected_main.index("async function enterApp(){")
          enter_end = expected_main.index("function switchPanel(name){", enter_start)
          expected_main = expected_main[:enter_start] + "\n" + expected_main[enter_end:]
          helpers_start = expected_main.index("function schoolId(){")
          helpers_end = expected_main.index("$('openSignupBtn').addEventListener('click',showSignup);", helpers_start)
          expected_main = expected_main[:helpers_start] + "\n" + expected_main[helpers_end:]

          historical_main = subprocess.check_output(["git", "show", f"{phase3b7f1_sha}:assets/js/main.js"], text=True)
          if historical_main != expected_main:
              raise SystemExit("Historical Phase 3B7F1 main.js is not the exact approved app-bootstrap extraction")

          enter_exports = enter_block.replace("async function enterApp", "export async function enterApp", 1)
          helper_exports = helpers_block
          for before, after in (
              ("function schoolId", "export function schoolId"),
              ("function adminClasses", "export function adminClasses"),
              ("function showAdminMsg", "export function showAdminMsg"),
          ):
              if helper_exports.count(before) != 1:
                  raise SystemExit(f"Phase 3B7F1 helper block does not contain exactly one {before}")
              helper_exports = helper_exports.replace(before, after, 1)

          expected_bootstrap = (
              "import { state } from './app-state.js';\n"
              "import { bruneiToday } from './date-helpers.js';\n"
              "import { $, fillGroupedClasses } from './ui-helpers.js';\n"
              "import { hideEntryViews } from './auth-session.js';\n"
              "import { setBusy, setBanner, loadRegister } from './attendance-register.js';\n"
              "import { ensureTeacherAccess } from './teacher-access.js';\n\n"
              "let sb;\n"
              "let switchPanel;\n\n"
              "export function initAppBootstrap(client,helpers){\n"
              "  sb=client;\n"
              "  switchPanel=helpers.switchPanel;\n"
              "}\n\n"
              + enter_exports + "\n\n"
              + helper_exports + "\n"
          )
          historical_bootstrap = subprocess.check_output(["git", "show", f"{phase3b7f1_sha}:assets/js/app-bootstrap.js"], text=True)
          if historical_bootstrap != expected_bootstrap:
              raise SystemExit("Historical Phase 3B7F1 app-bootstrap.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "assets/js/attendance-register.js",
              "assets/js/student-management.js",
              "assets/js/teacher-admin.js",
              "assets/js/teacher-access.js",
              "assets/js/statistics.js",
              "assets/js/admin-dashboard.js",
              "assets/js/period-reports.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b7f1_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b7f1_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B7F1 changed {path}")

      - name: Verify historical Phase 3B7F2 app-navigation split
        shell: python
        run: |
          import subprocess

          phase3b7f2_base = "cf939c3582d4db2c2be7a503b4355cfd9066af55"
          phase3b7f2_sha = "cb131514a7b90b81d52882455281a25de6b3609f"
          for sha in (phase3b7f2_base, phase3b7f2_sha):
              subprocess.run(["git", "cat-file", "-e", f"{sha}^{{commit}}"], check=True)
          before_main = subprocess.check_output(["git", "show", f"{phase3b7f2_base}:assets/js/main.js"], text=True)

          bootstrap_import = "import { initAppBootstrap, enterApp, schoolId, adminClasses, showAdminMsg } from './app-bootstrap.js';\n"
          navigation_import = "import { switchPanel, openStatisticsForClass } from './app-navigation.js';\n"
          listener_marker = "$('openSignupBtn').addEventListener('click',showSignup);"
          if before_main.count(bootstrap_import) != 1:
              raise SystemExit("Phase 3B7F2 base does not contain exactly one app-bootstrap import")
          if before_main.count("function switchPanel(name){") != 1 or before_main.count("function openStatisticsForClass(classId,month){") != 1:
              raise SystemExit("Phase 3B7F2 base does not contain the expected unique navigation functions")
          if before_main.count(listener_marker) != 1:
              raise SystemExit("Phase 3B7F2 base does not contain the expected listener marker")

          navigation_start = before_main.index("function switchPanel(name){")
          navigation_end = before_main.index(listener_marker, navigation_start)
          navigation_block = before_main[navigation_start:navigation_end].rstrip('\n')

          expected_main = before_main.replace(bootstrap_import, bootstrap_import + navigation_import, 1)
          navigation_start = expected_main.index("function switchPanel(name){")
          navigation_end = expected_main.index(listener_marker, navigation_start)
          expected_main = expected_main[:navigation_start] + "\n" + expected_main[navigation_end:]

          historical_main = subprocess.check_output(["git", "show", f"{phase3b7f2_sha}:assets/js/main.js"], text=True)
          if historical_main != expected_main:
              raise SystemExit("Historical Phase 3B7F2 main.js is not the exact approved app-navigation extraction")

          navigation_exports = navigation_block
          navigation_exports = navigation_exports.replace("function switchPanel", "export function switchPanel", 1)
          navigation_exports = navigation_exports.replace("function openStatisticsForClass", "export function openStatisticsForClass", 1)
          expected_navigation = (
              "import { $ } from './ui-helpers.js';\n"
              "import { loadAdminDashboard } from './admin-dashboard.js';\n"
              "import { loadMonthlyStats } from './statistics.js';\n"
              "import { loadReportOptions } from './period-reports.js';\n"
              "import { loadAdminStudents } from './student-management.js';\n"
              "import { loadAdminTeachers } from './teacher-admin.js';\n\n"
              + navigation_exports + "\n"
          )
          historical_navigation = subprocess.check_output(["git", "show", f"{phase3b7f2_sha}:assets/js/app-navigation.js"], text=True)
          if historical_navigation != expected_navigation:
              raise SystemExit("Historical Phase 3B7F2 app-navigation.js is not the exact approved module")

          for path in (
              "index.html",
              "assets/js/supabase-client.js",
              "assets/js/app-state.js",
              "assets/js/date-helpers.js",
              "assets/js/ui-helpers.js",
              "assets/js/auth-session.js",
              "assets/js/attendance-register.js",
              "assets/js/student-management.js",
              "assets/js/teacher-admin.js",
              "assets/js/teacher-access.js",
              "assets/js/statistics.js",
              "assets/js/admin-dashboard.js",
              "assets/js/period-reports.js",
              "assets/js/app-bootstrap.js",
              "netlify.toml",
          ):
              before = subprocess.check_output(["git", "show", f"{phase3b7f2_base}:{path}"], text=True)
              after = subprocess.check_output(["git", "show", f"{phase3b7f2_sha}:{path}"], text=True)
              if after != before:
                  raise SystemExit(f"Historical Phase 3B7F2 changed {path}")

      - name: Check current JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          node --check assets/js/main.js
          node --check assets/js/supabase-client.js
          node --check assets/js/app-state.js
          node --check assets/js/date-helpers.js
          node --check assets/js/ui-helpers.js
          node --check assets/js/auth-session.js
          node --check assets/js/attendance-register.js
          node --check assets/js/student-management.js
          node --check assets/js/teacher-admin.js
          node --check assets/js/teacher-access.js
          node --check assets/js/statistics.js
          node --check assets/js/admin-dashboard.js
          node --check assets/js/period-reports.js
          node --check assets/js/reporting-population.js
          node --check assets/js/app-bootstrap.js
          node --check assets/js/app-navigation.js
